# Header Doc
- Purpose: Peta modul lokal untuk direktori `routes` berisi file, simbol publik, dan tanggung jawab ringkas.
- Caller: Pengembang/agent saat tracing flow lintas modul di `ROUTES MODULE MAP`.
- Deps: Analisis statis file dalam `routes`, nama file, dan blok `module.exports`.
- MainFuncs: Inventaris file, simbol publik, dan peran tiap modul.
- SideEffects: Tidak ada; dokumentasi statis.

# ROUTES MODULE MAP
- FlowDir: routes
- CallerUtama: `index.js` saat mount Express routes
- DependensiKunci: `lib/*`, middleware auth, template/rendering, DB wrapper
- SideEffects: Tidak ada; dokumentasi statis.

| File | Ekspor/Publik | Tanggung Jawab |
| --- | --- | --- |
| `routes/accounts.js` | `router` | Router CRUD akun internal/staf. |
| `routes/admin-auth.js` | `ensureAuthenticatedStaff` | Middleware auth staf admin terpusat untuk router admin hasil pemecahan. |
| `routes/admin-auto-outage-routes.js` | `registerAdminAutoOutageRoutes` | Registrar route admin untuk rule auto outage, scan PPPoE MikroTik, dry-run eligible customer, broadcast WhatsApp interaktif, state offline, dan scan log berbasis pelanggan DB `pppoe_username`. |
| `routes/admin-content-routes.js` | `registerAdminContentRoutes, buildAdminEditorAudit` | Registrar route admin domain template, announcement, news, WiFi templates, dan broadcast dengan seleksi user + write path lewat runtime repository/service; owner full editor `/api/templates` untuk notification, WiFi, response, command, error, success, system, menu, dan report templates plus diagnostics audit placeholder/metadata response template. |
| `routes/admin-ops-routes.js` | `registerAdminOpsRoutes` | Registrar route admin untuk utility destruktif seperti delete generic, hapus semua user, dan cleanup foto yatim dengan owner persistence utility di `services/admin-ops.service.js` + `repositories/admin-ops.repository.js`. |
| `routes/admin-database-routes.js` | `registerAdminDatabaseRoutes` | Registrar route admin untuk info DB, backup, reload, restore, operasi schema, debug database, dan migrasi users JSON. |
| `routes/admin-config-routes.js` | `registerAdminConfigRoutes, CRON_ALLOWED_FIELDS` | Pendaftaran route admin untuk konfigurasi sistem, cron, parameter GenieACS, dan setup device MikroTik. |
| `routes/admin-isolir-routes.js` | `registerAdminIsolirRoutes` | Pendaftaran route admin untuk isolir pelanggan. |
| `routes/admin-logs-routes.js` | `registerAdminLogsRoutes` | Registrar route admin untuk audit login dan activity log. |
| `routes/admin-network-assets-routes.js` | `registerAdminNetworkAssetsRoutes` | Registrar route admin untuk peta aset jaringan dan kalkulasi route antar titik. |
| `routes/admin-router.js` | `createAdminRouter` | Komposer router admin sebagai entrypoint tunggal yang memasang content/config/isolir/network/wifi/database/voucher/log/admin-ops/auto-outage lewat factory dependency per-domain lalu legacy router sisa. |
| `routes/admin-shared.js` | `generateAssetId, getVoucherProfiles, getVoucherProfileById, buildVoucherProfileSnapshot, sendVoucherTextToPhones` | Helper bersama registrar admin untuk aset jaringan dan voucher dengan pengiriman WA yang sudah lewat delivery service terpusat. |
| `routes/admin.js` | `router` | Router admin legacy fallback yang hanya menyimpan stub `410` untuk endpoint yang sudah dipindah agar tidak terjadi shadow ownership. |
| `routes/admin.routes.js` | `createAdminRoutes` | Router admin tipis (owner endpoint hotspot billing & package change) yang dipasang `admin-router.js` sebelum fallback legacy, delegasi ke `controllers/admin.controller.js`. |
| `routes/admin-voucher-routes.js` | `registerAdminVoucherRoutes` | Registrar route admin untuk CRUD voucher dan seluruh flow admin-agent-voucher termasuk sale, inventory, history, dan top-agents dengan config dibaca dari runtime. |
| `routes/admin-wifi-ops-routes.js` | `registerAdminWifiOpsRoutes` | Registrar route admin untuk WiFi logs, statistik, jam operasional, observability jaringan, dan mutasi WiFi perangkat lewat `services/network-ops.service.js` + `repositories/wifi.repository.js`. |
| `routes/agents.js` | `router` | Router domain agen dan endpoint operasional agen. |
| `routes/arrears.js` | `createArrearsRouter` | Router API read-only untuk rekap tunggakan pelanggan berbasis periode, summary manajerial, dan detail statement pelanggan. |
| `routes/api-network-routes.js` | `createApiNetworkRouter` | Factory router API domain jaringan/MikroTik yang kini mendelegasikan action update profile, manual send WA, dan read-model import PPPoE/GenieACS ke `services/api-network.service.js` + `repositories/api-network.repository.js`, sementara helper runtime WA/MikroTik masih diinjeksikan sebagai adapter. |
| `routes/api-psb-routes.js` | `createApiPsbRouter` | Factory router API domain PSB/provisioning dengan akses users/packages/config/PSB records yang sudah dipusatkan ke runtime getter/repository; submit phase 1, list customers, update status, submit phase 2, submit phase 3, dan delete-all kini mulai turun ke `services/api-psb.service.js` + `repositories/api-psb.repository.js`, sementara flow upload dan device/provisioning lanjutan lain masih helper-first. |
| `routes/api-route-helpers.js` | `ensureAuthenticated, ensureAdmin, ensureAuthenticatedStaff, normalizeQueryStringParam, redactPppoeFilter, buildMikrotikSyncResult` | Middleware/helper otorisasi dan utilitas standar API. |
| `routes/api-users-routes.js` | `createApiUsersRouter` | Factory router API domain pelanggan/user dengan hotspot CRUD high-traffic yang sudah repo-first via runtime getter; list users, create/update payment-aware upsert, delete user, delete-all users, dan update user detail kini mulai turun ke `services/api-users.service.js` + `repositories/api-users.repository.js`. |
| `routes/api-voucher-routes.js` | `createApiVoucherRouter` | Factory router API domain voucher sebagai adapter tipis untuk profile read-model, sent-history, statistik, generate/send voucher, dan member-credential delivery; flow aktif kini diturunkan ke `services/api-voucher.service.js` + `repositories/api-voucher.repository.js`, sementara helper file/PHP legacy tinggal adapter di bawah owner baru. |
| `routes/api.js` | `router` | Hub API umum; agregasi sub-router dan endpoint REST lintas domain sekaligus injection runtime app ke hotspot hasil stabilisasi. |
| `routes/change-package.js` | `router` | Router perubahan paket dan sinkronisasi jaringan dengan emit event notifikasi pelanggan lewat helper timing WA internal, tanpa import Baileys langsung. |
| `routes/compensation.js` | `router` | Router domain compensation. |
| `routes/discount.js` | `router` | Router diskon pelanggan dengan emit event notifikasi diskon lewat helper timing WA internal, tanpa import Baileys langsung. |
| `routes/expenses.js` | `router` | Router pencatatan pengeluaran. |
| `routes/gaji.js` | `router` | Router penggajian teknisi/staf. |
| `routes/invoice.js` | `router` | Router invoice dan penagihan. |
| `routes/kasbon.js` | `router` | Router kasbon teknisi. |
| `routes/message-templates.js` | `router` | Router compat `/api/message-templates` untuk notification templates lama; diagnostics menunjuk source `template-service`, bukan full editor route. |
| `routes/monitoring-api.js` | `router` | Router API monitoring atau bridge ke skrip PHP. |
| `routes/monitoring-dashboard.js` | `router` | Router dashboard monitoring internal termasuk restart WhatsApp via helper reconnect bootstrap. |
| `routes/monitoring-dummy.js` | `router` | Router data dummy untuk monitoring/dev. |
| `routes/olt.js` | `router` | Router monitoring/integrasi OLT dan data jaringan aktif. |
| `routes/packages.js` | `router` | Router manajemen paket layanan. |
| `routes/pages.js` | `router` | Router halaman web non-API atau rendering halaman. |
| `routes/partial-payment.js` | `router` | Router pembayaran parsial pelanggan dengan emit event notifikasi approval/admin lewat helper timing WA internal, tanpa import Baileys langsung. |
| `routes/payment-status.js` | `router` | Router status pembayaran. |
| `routes/public.js` | `router` | Router publik/customer portal, autentikasi OTP, dan endpoint self-service pelanggan dengan gating WhatsApp via gateway; notifikasi outbound WA Speed on Demand owner memakai `responseTemplates`. |
| `routes/rekap-keuangan.js` | `router` | Router rekap dan laporan keuangan. |
| `routes/requests.js` | `router` | Router permintaan pelanggan/admin yang menunggu proses dengan broadcast owner berbasis gateway readiness dan template admin untuk notifikasi pengajuan/cancel pembayaran. |
| `routes/saldo.js` | `router` | Router saldo/topup dan bukti pembayaran terkait; notifikasi WhatsApp saldo agent memakai `responseTemplates`. |
| `routes/speed-requests.js` | `router` | Router permintaan speed boost/perubahan kecepatan dengan notifikasi owner/customer berbasis gateway readiness dan `responseTemplates` untuk bukti bayar, verifikasi, approval, serta reject. |
| `routes/stats.js` | `router` | Router statistik/rekap dashboard dan data monitoring dengan status bot dibaca dari gateway serta start/stop lewat boundary lifecycle bootstrap. |
| `routes/technician-settlement.js` | `router` | Router settlement koleksi/teknisi. |
| `routes/tickets.js` | `router` | Router tiket gangguan dan proses teknisi; notifikasi WhatsApp customer untuk tiket yang dibuat dari web admin/staff serta notice jam kerja memakai `responseTemplates`. |
| `routes/tickets-shared.js` | `renderResponseTemplate, formatTicketPriority, buildWorkingHoursNotice, ensureAuthenticatedStaff, ensureAdmin, upload, createTicketPhotoUpload, DEBUG` | Shared module sub-router tiket: helper render template, formatter prioritas, working-hours notice, middleware auth staff/admin, dan dua multer storage foto teknisi/pembuatan tiket. |
| `routes/tickets-admin-routes.js` | `router` | Sub-router tiket KHUSUS admin (`ensureAdmin`): admin-create dan admin-cancel ticket. |
| `routes/tickets-customer-routes.js` | `router` | Sub-router placeholder tiket self-service customer (auth customer-only), disediakan terpisah untuk use case portal publik di masa depan. |
| `routes/tickets-list-routes.js` | `router` | Sub-router READ tiket: staff list (`/tickets`) dan admin list (`/admin/tickets`). |
| `routes/tickets-photo-routes.js` | `router` | Sub-router upload foto staff (`ensureAuthenticatedStaff`): dokumentasi pasca-OTP dan foto saat pembuatan tiket. |
| `routes/tickets-workflow-routes.js` | `router` | Sub-router workflow state-transition staff: process, OTW, arrived, verify-otp, complete, resolve, dan staff-side ticket create. |
| `routes/users.js` | `router` | Router manajemen pelanggan/user dengan welcome/bulk import status WA dibaca dari gateway runtime. |

- Catatan: `router` berarti ekspor instance `express.Router()`, sedangkan `Not found` berarti simbol publik tidak terdeteksi statis atau file berisi data murni.
