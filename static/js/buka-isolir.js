/*
 * Header Doc
 * Purpose: Perilaku halaman views/sb-admin/buka-isolir.php —
 *          dipindahkan dari blok <script> inline (CLAUDE.md: JS halaman eksternal).
 * Caller : views/sb-admin/buka-isolir.php lewat <script src>, pada posisi yang sama dengan blok aslinya.
 * SideEffects: memanipulasi DOM halaman tsb + memanggil API internal.
 */

const state={visibleItems:[],selectedItems:new Map(),genieacsStatus:null,pagination:{page:1,limit:20,totalPages:1,totalItems:0},historyPagination:{page:1,limit:10,totalPages:1,totalItems:0},search:'',lastExecution:null,resultFilter:'all'};
document.addEventListener('DOMContentLoaded',()=>{bindEvents();loadUsers();loadHistory();loadGenieacsStatus();});
function bindEvents(){document.getElementById('refreshBtn').addEventListener('click',loadUsers);document.getElementById('selectCurrentPageBtn').addEventListener('click',selectCurrentPage);document.getElementById('clearSelectionBtn').addEventListener('click',clearSelection);document.getElementById('btnBukaIsolir').addEventListener('click',bukaIsolir);document.getElementById('refreshHistoryBtn').addEventListener('click',loadHistory);document.getElementById('prevPageBtn').addEventListener('click',()=>changePage(-1));document.getElementById('nextPageBtn').addEventListener('click',()=>changePage(1));document.getElementById('historyPrevPageBtn').addEventListener('click',()=>changeHistoryPage(-1));document.getElementById('historyNextPageBtn').addEventListener('click',()=>changeHistoryPage(1));document.getElementById('rebootAfterOpen').addEventListener('change',updateSummary);document.getElementById('resultAllBtn').addEventListener('click',()=>setResultFilter('all'));document.getElementById('resultFailedBtn').addEventListener('click',()=>setResultFilter('failed'));document.getElementById('resultSkippedBtn').addEventListener('click',()=>setResultFilter('skipped'));document.getElementById('searchInput').addEventListener('input',debounce(()=>{state.search=document.getElementById('searchInput').value.trim();state.pagination.page=1;loadUsers();},250));}
function debounce(callback,delay){let timer=null;return(...args)=>{window.clearTimeout(timer);timer=window.setTimeout(()=>callback(...args),delay);};}
function getSelectedRecords(){return Array.from(state.selectedItems.values());}
function getVisibleSelectedCount(){return state.visibleItems.filter((item)=>state.selectedItems.has(item.id)).length;}
function getGlobalSelectedCount(){return state.selectedItems.size;}
function indexVisibleItems(){for(const item of state.visibleItems){if(state.selectedItems.has(item.id)){state.selectedItems.set(item.id,item);}}}
function renderUsers(){const list=document.getElementById('userList');if(!state.visibleItems.length){list.innerHTML='<div class="isolir-empty">Tidak ada pelanggan terisolir untuk ditampilkan.</div>';updateFilterSummary();updatePageInfo();updateSummary();applyGenieacsAvailability();return;}list.innerHTML=state.visibleItems.map((item)=>{const selected=state.selectedItems.has(item.id);const capabilityClass=item.genieacsCapable?'ready':'warn';const capabilityText=item.genieacsCapable?'Reboot siap':(item.genieacsReason||'Reboot akan di-skip');return `<div class="isolir-item user-card ${selected?'selected':''}" onclick="toggleUser(${item.id})"><div class="isolir-item-shell"><div><div class="isolir-item-title"><input type="checkbox" ${selected?'checked':''} onclick="event.stopPropagation();" onchange="toggleUser(${item.id})"> <span>${escapeHtml(item.name||'-')}</span></div><div class="isolir-item-meta"><div><strong>PPPoE</strong> ${escapeHtml(item.pppoe_username||'-')}</div><div><strong>Paket</strong> ${escapeHtml(item.subscription||'-')}</div><div><strong>Profile saat ini</strong> <span class="isolir-pill profile">${escapeHtml(item.currentProfile||'N/A')}</span></div></div></div><div class="isolir-item-side"><span class="isolir-pill danger"><i class="fas fa-ban"></i>Terisolir</span><span class="isolir-pill ${capabilityClass}">${escapeHtml(capabilityText)}</span></div></div></div>`;}).join('');updateFilterSummary();updatePageInfo();updateSummary();applyGenieacsAvailability();}
function updateFilterSummary(){const filteredCount=state.pagination.totalItems||state.visibleItems.length;const hiddenCount=getGlobalSelectedCount()-getVisibleSelectedCount();document.getElementById('filterSummary').textContent=`${filteredCount} pelanggan terisolir cocok, ${getGlobalSelectedCount()} dipilih global, ${getVisibleSelectedCount()} tampil di halaman ini.${hiddenCount>0?` ${hiddenCount} selection tidak tampil di halaman ini.`:''}`;}
function updatePageInfo(){document.getElementById('pageInfo').textContent=`Halaman ${state.pagination.page||1} / ${state.pagination.totalPages||1}`;document.getElementById('prevPageBtn').disabled=(state.pagination.page||1)<=1;document.getElementById('nextPageBtn').disabled=(state.pagination.page||1)>=(state.pagination.totalPages||1);}
function changePage(delta){const nextPage=(state.pagination.page||1)+delta;if(nextPage<1||nextPage>(state.pagination.totalPages||1))return;state.pagination.page=nextPage;loadUsers();}
async function loadUsers(){const list=document.getElementById('userList');list.innerHTML='<div class="isolir-empty">Memuat pelanggan terisolir...</div>';const params=new URLSearchParams({page:String(state.pagination.page),limit:String(state.pagination.limit),onlyIsolated:'true'});if(state.search)params.set('search',state.search);const response=await fetch(`/api/isolir/candidates?${params.toString()}`,{credentials:'include'});const payload=await response.json();if(!response.ok){list.innerHTML=`<div class="isolir-empty text-danger">${escapeHtml(payload.message||'Gagal memuat pelanggan terisolir.')}</div>`;document.getElementById('filterSummary').textContent='Gagal memuat pelanggan.';return;}state.visibleItems=payload.data?.items||[];state.pagination=payload.data?.pagination||state.pagination;indexVisibleItems();renderUsers();}
function toggleUser(userId){const visibleRecord=state.visibleItems.find((item)=>item.id===userId);if(state.selectedItems.has(userId)){state.selectedItems.delete(userId);}else if(visibleRecord){state.selectedItems.set(userId,visibleRecord);}renderUsers();}
function selectCurrentPage(){state.visibleItems.forEach((item)=>state.selectedItems.set(item.id,item));renderUsers();}
function clearSelection(){state.selectedItems.clear();renderUsers();}
function updateSummary(){const selected=getSelectedRecords();const summary=document.getElementById('selectionSummary');const button=document.getElementById('btnBukaIsolir');const rebootRequested=!document.getElementById('rebootAfterOpen').disabled&&document.getElementById('rebootAfterOpen').checked;if(!selected.length){summary.textContent='Belum ada pelanggan dipilih.';button.disabled=true;return;}const rebootCapable=selected.filter((item)=>item.genieacsCapable).length;const hiddenCount=selected.length-getVisibleSelectedCount();summary.innerHTML=`<strong>${selected.length}</strong> pelanggan dipilih global.<br>Restore profile akan dijalankan untuk seluruh selection.<br>Disconnect: <strong>Ya</strong>.<br>Reboot diminta: <strong>${rebootRequested?'Ya':'Tidak'}</strong>. Estimasi skip reboot: <strong>${rebootRequested?selected.length-rebootCapable:0}</strong>.${hiddenCount>0?`<br>${hiddenCount} selection tidak tampil pada halaman ini.`:''}`;button.disabled=false;}
async function loadHistory(){const body=document.getElementById('historyBody');body.innerHTML='<tr><td colspan="4" class="text-center text-muted py-3">Memuat riwayat...</td></tr>';const params=new URLSearchParams({page:String(state.historyPagination.page),limit:String(state.historyPagination.limit),actionType:'open_isolir'});const response=await fetch(`/api/isolir/history?${params.toString()}`,{credentials:'include'});const payload=await response.json();if(!response.ok||!Array.isArray(payload.data)){body.innerHTML='<tr><td colspan="4" class="text-center text-danger py-3">Gagal memuat riwayat.</td></tr>';return;}state.historyPagination=payload.meta?.pagination||state.historyPagination;if(!payload.data.length){body.innerHTML='<tr><td colspan="4" class="text-center text-muted py-3">Belum ada riwayat buka isolir.</td></tr>';updateHistoryPageInfo();return;}body.innerHTML=payload.data.map((entry)=>{const summary=entry.summary||{};return `<tr><td data-label="Waktu">${escapeHtml(formatDate(entry.createdAt))}</td><td data-label="Actor">${escapeHtml(entry.actor?.username||'system')}</td><td data-label="Ringkasan">${summary.successCount||0} restore / ${summary.failedCount||0} gagal / ${summary.rebootSkippedCount||0} reboot skip</td><td data-label="Detail"><button class="btn btn-sm btn-outline-primary" type="button" onclick="showHistoryDetail('${escapeHtml(entry.id)}')">Detail</button></td></tr>`;}).join('');updateHistoryPageInfo();}
function updateHistoryPageInfo(){document.getElementById('historyPageInfo').textContent=`Halaman ${state.historyPagination.page||1} / ${state.historyPagination.totalPages||1}`;document.getElementById('historyPrevPageBtn').disabled=(state.historyPagination.page||1)<=1;document.getElementById('historyNextPageBtn').disabled=(state.historyPagination.page||1)>=(state.historyPagination.totalPages||1);}
function changeHistoryPage(delta){const nextPage=(state.historyPagination.page||1)+delta;if(nextPage<1||nextPage>(state.historyPagination.totalPages||1))return;state.historyPagination.page=nextPage;loadHistory();}
async function loadGenieacsStatus(){const notice=document.getElementById('genieacsNotice');const response=await fetch('/api/genieacs/feature-status',{credentials:'include'});const payload=await response.json();state.genieacsStatus=payload.data||null;if(!response.ok||!state.genieacsStatus){notice.className='alert alert-warning border';notice.textContent='Status GenieACS tidak dapat dimuat. Reboot akan diverifikasi saat eksekusi.';applyGenieacsAvailability();return;}notice.className=state.genieacsStatus.available?'alert alert-success border':'alert alert-warning border';notice.textContent=state.genieacsStatus.reason||'GenieACS tersedia untuk reboot admin.';applyGenieacsAvailability();}
function applyGenieacsAvailability(){const rebootCheckbox=document.getElementById('rebootAfterOpen');const notice=document.getElementById('genieacsNotice');const selected=getSelectedRecords();const selectedCapable=selected.filter((item)=>item.genieacsCapable).length;const hiddenCount=selected.length-getVisibleSelectedCount();if(state.genieacsStatus&&state.genieacsStatus.available===false){rebootCheckbox.checked=false;rebootCheckbox.disabled=true;notice.className='alert alert-warning border';notice.textContent=state.genieacsStatus.reason||'GenieACS tidak tersedia untuk reboot admin.';updateSummary();return;}rebootCheckbox.disabled=false;if(selected.length>0){const skipped=selected.length-selectedCapable;notice.className=skipped>0?'alert alert-warning border':'alert alert-success border';notice.textContent=skipped>0?`${selectedCapable} pelanggan siap reboot, ${skipped} akan skip reboot karena device/capability tidak tersedia.${hiddenCount>0?` ${hiddenCount} selection tidak tampil pada halaman ini.`:''}`:`Semua pelanggan terpilih siap untuk reboot opsional.${hiddenCount>0?` ${hiddenCount} selection tidak tampil pada halaman ini.`:''}`;}updateSummary();}
async function bukaIsolir(){const userIds=Array.from(state.selectedItems.keys());const reboot=!document.getElementById('rebootAfterOpen').disabled&&document.getElementById('rebootAfterOpen').checked;if(!userIds.length){Swal.fire('Validasi','Pilih minimal satu pelanggan terisolir.','warning');return;}const confirm=await Swal.fire({title:'Buka isolir pelanggan?',html:`<div class="text-left"><div><strong>Pelanggan:</strong> ${userIds.length}</div><div><strong>Disconnect:</strong> Ya</div><div><strong>Reboot:</strong> ${reboot?'Ya':'Tidak'}</div></div>`,icon:'question',showCancelButton:true,confirmButtonText:'Jalankan',cancelButtonText:'Batal',confirmButtonColor:'#16a34a'});if(!confirm.isConfirmed)return;Swal.fire({title:'Memproses...',allowOutsideClick:false,didOpen:()=>Swal.showLoading()});const response=await fetch('/api/isolir/open',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'include',body:JSON.stringify({userIds,reboot})});const payload=await response.json();if(!response.ok){Swal.fire('Gagal',payload.message||'Buka isolir gagal dijalankan.','error');return;}state.lastExecution=payload.data||null;renderLastExecution();state.selectedItems.clear();const summary=payload.data?.summary||{};await Swal.fire('Selesai',`${summary.successCount||0} restore, ${summary.failedCount||0} gagal, ${summary.rebootSkippedCount||0} reboot skip.`,(summary.successCount||0)>0?'success':'warning');await Promise.all([loadUsers(),loadHistory()]);}
function setResultFilter(filter){state.resultFilter=filter;document.querySelectorAll('.result-filter .btn').forEach((button)=>button.classList.remove('active'));if(filter==='all')document.getElementById('resultAllBtn').classList.add('active');if(filter==='failed')document.getElementById('resultFailedBtn').classList.add('active');if(filter==='skipped')document.getElementById('resultSkippedBtn').classList.add('active');renderLastExecution();}
function renderLastExecution(){const container=document.getElementById('lastExecutionResult');if(!state.lastExecution){container.textContent='Belum ada eksekusi pada sesi ini.';return;}const summary=state.lastExecution.summary||{};let details=Array.isArray(state.lastExecution.results)?state.lastExecution.results:[];if(state.resultFilter==='failed')details=details.filter((item)=>!item.ok);if(state.resultFilter==='skipped')details=details.filter((item)=>item.rebootRequested&&!item.rebootApplied);container.innerHTML=`<div class="isolir-result-summary"><span class="isolir-result-chip">${summary.successCount||0} restore</span><span class="isolir-result-chip">${summary.failedCount||0} gagal</span><span class="isolir-result-chip">${summary.rebootSkippedCount||0} reboot skip</span>${state.lastExecution.historyId?`<button type="button" class="btn btn-link btn-sm p-0 align-baseline" onclick="showHistoryDetail('${escapeHtml(state.lastExecution.historyId)}')">Lihat audit</button>`:''}</div><div class="isolir-table-wrap"><table class="table table-sm isolir-table mb-0"><thead><tr><th>Pelanggan</th><th>Status</th><th>Disconnect</th><th>Reboot</th></tr></thead><tbody>${details.length?details.map((item)=>`<tr><td data-label="Pelanggan">${escapeHtml(item.name||item.pppoe_username||'-')}</td><td data-label="Status">${escapeHtml(item.ok?'Restore sukses':(item.message||'Gagal'))}</td><td data-label="Disconnect">${escapeHtml(item.disconnectApplied?'Applied':(item.disconnectRequested?(item.disconnectNote||'Skipped'):'No'))}</td><td data-label="Reboot">${escapeHtml(item.rebootApplied?'Applied':(item.rebootSkippedReason||(item.rebootRequested?'Skipped':'No')))}</td></tr>`).join(''):'<tr><td colspan="4" class="text-center text-muted">Tidak ada hasil untuk filter ini.</td></tr>'}</tbody></table></div>`;}
async function showHistoryDetail(historyId){const response=await fetch(`/api/isolir/history/${encodeURIComponent(historyId)}`,{credentials:'include'});const payload=await response.json();if(!response.ok||!payload.data){Swal.fire('Gagal',payload.message||'Detail riwayat tidak dapat dimuat.','error');return;}const entry=payload.data;const results=Array.isArray(entry.results)?entry.results:[];await Swal.fire({title:'Detail Riwayat Buka Isolir',width:920,html:`<div class="text-left mb-3"><div><strong>Waktu:</strong> ${escapeHtml(formatDate(entry.createdAt))}</div><div><strong>Actor:</strong> ${escapeHtml(entry.actor?.username||'system')}</div><div><strong>Reason:</strong> ${escapeHtml(entry.reason||'-')}</div></div><div class="table-responsive text-left"><table class="table table-sm mb-0"><thead><tr><th>Pelanggan</th><th>Status</th><th>Disconnect</th><th>Reboot</th></tr></thead><tbody>${results.map((item)=>`<tr><td>${escapeHtml(item.name||item.pppoe_username||'-')}</td><td>${escapeHtml(item.ok?'Restore sukses':(item.message||'Gagal'))}</td><td>${escapeHtml(item.disconnectApplied?'Applied':(item.disconnectRequested?(item.disconnectNote||'Skipped'):'No'))}</td><td>${escapeHtml(item.rebootApplied?'Applied':(item.rebootSkippedReason||(item.rebootRequested?'Skipped':'No')))}</td></tr>`).join('')}</tbody></table></div>`});}
function formatDate(value){if(!value)return'-';const date=new Date(value);return Number.isNaN(date.getTime())?'-':date.toLocaleString('id-ID');}
function escapeHtml(value){return String(value||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#39;');}
window.toggleUser=toggleUser;window.showHistoryDetail=showHistoryDetail;
