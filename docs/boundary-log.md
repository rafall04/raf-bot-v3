# Log Boundary & Fitur (riwayat kepemilikan)

> **Ini CHANGELOG, bukan peta.** Peta arsitektur ada di [SYSTEM_MAP.md](../SYSTEM_MAP.md) — baca itu dulu.
> File ini dibaca **hanya saat menelusuri satu fitur tertentu**; jangan dimuat seluruhnya.
> Cari lewat indeks di SYSTEM_MAP.md, lalu buka anchor yang relevan (mis. `#b55`).
>
> **Aturan menulis entri baru:** tambahkan di BAWAH, satu entri per perubahan kepemilikan/boundary.
> Sebutkan: file owner, apa yang dimiliki sekarang, status path lama (stub 410 / fallback / dihapus),
> gate config, dan test. Bahasa Indonesia, padat, faktual.

<a id="b01"></a>
### `routes/bill-payment.js` + `lib/bill-pay-token.js` + `lib/services/bill-payment-settlement.js`

- `routes/bill-payment.js` + `lib/bill-pay-token.js` + `lib/services/bill-payment-settlement.js`: owner flow **bayar tagihan bulanan self-service**. Pelanggan pascabayar terima link `/bayar/:token` (token HMAC stateless tanpa login, dibuat `buildBillPayUrl` — disisipkan cron `reminder`/`isolir-notification` & handler `cektagihan`/`bayar` di `billing-management-handler.js`). Halaman (`static/bill-payment.html`) ambil channel aktif via `ipaymu.getPaymentChannels()` lalu `POST /api/bayar/:token/charge` → `ipaymu.payDirect()` (multi-channel) + `addPayment` tag `tagihan` (persist `payment.json`, simpan `userId`/`periodMonth`/`periodYear`). Callback `POST /callback/payment` cabang `tagihan` (`routes/public.js`) verify S2S lalu `bill-payment-settlement.settleTagihanPayment` → `applyPaymentStatusChange` (ledger, sumber kebenaran paid) + auto-reaktivasi `IsolirService.executeProfileAction` HANYA bila profil live MikroTik == `isolir_profile` (deteksi via `getPPPoEUserProfile`). Fail-closed: catat-lunas gagal → 500 (iPaymu retry); reaktivasi best-effort (gagal → lunas tetap + alert admin). Menggantikan reaktivasi manual `/buka-isolir`.

<a id="b02"></a>
### `lib/services/advance-payment-service.js`

- `lib/services/advance-payment-service.js` + endpoint `POST /api/payment-status/advance` (`routes/payment-status.js`) + UI `static/js/payment-status.js` (tombol "Bayar di Muka" + modal): owner flow **bayar di muka (prabayar cash oleh admin)**. Admin pilih N bulan (1–12) ke depan untuk satu pelanggan; service loop `applyPaymentStatusChange` per periode masa depan (CASH, idempoten — periode yang sudah lunas masuk `skipped`) ke ledger periodik. SYARAT: tagihan periode berjalan WAJIB sudah lunas (`getPaymentPositionForPeriod` current → `is_fully_paid`; kalau belum → 409). **Tanpa onFinalPaid** → tidak menyentuh profil MikroTik (ini periode masa depan, pelanggan masih aktif). Setelah tercatat, kirim SATU struk WA `tagihan_prabayar_struk` (best-effort, tak menggagalkan pencatatan). Kebenaran cron tanpa perubahan: karena current wajib lunas → `user.paid=true` → cron `set-unpaid` tgl 1 me-resync user itu dari ledger → menemukan baris prabayar → tetap paid → cron isolir melewatinya.

<a id="b03"></a>
### Beli Voucher Online (publik)

- **Beli Voucher Online (publik)** — halaman `static/voucher-buy.html` di route `GET /voucher` + API `/app/*` (OWNER `routes/public-anonymous.js`, dipindah dari `routes/public.js`; di-mount di app utama via `lib/routes-registry.js` DAN listener publik `lib/public-site-app.js`): pembeli umum/anonim pilih paket (`GET /app/voucher` = `global.voucher`) → charge iPaymu QRIS (`GET /app/buy/:prof?phone=&email=`, email auto-generate `<nomor>@voucher.rafnet.local`) → QR tampil via `GET /app/qr/:reff` (PNG dari `qrStr`) → polling `GET /app/statustrx/:reff`. Callback tag `buynowweb` (`POST /callback/payment`, tetap di `routes/public.js` port utama): verify S2S → `getvoucher` → kode tampil di halaman (field `ket`) **DAN kirim WA** (`sendCritical` + template `voucher_beli_web`, nomor form dinormalisasi ke JID, best-effort try/catch). Gagal-terbit → `recordVoucherOrphan` + `alertAdmins` + tetap mark paid (non-idempotent). Voucher/topup gateway = iPaymu (belum multi-gateway). **Konsekuensi kepemilikan path `/voucher`:** karena surface beli publik ini memiliki `GET /voucher` di app utama (mount lebih dulu di `lib/routes-registry.js` + masuk `PUBLIC_PATHS` `lib/http-auth-bootstrap.js`), **katalog paket voucher admin dipindah ke `GET /paket-voucher`** (`routes/pages.js`, guard `checkRole` admin, render `views/sb-admin/paket-voucher.php`; dulu `voucher.php` di `/voucher` yang jadi dead-code terbayangi). Link sidebar `views/sb-admin/_navbar.php` (`href` + `isActive`/`isParentActive`) ikut `/paket-voucher`; nama file view sengaja diselaraskan agar `$current_page` (diturunkan dari filename di php-express) menyalakan menu dengan benar.

<a id="b04"></a>
### Site Publik Anonim (listener HTTP kedua, 1 proses)

- **Site Publik Anonim (listener HTTP kedua, 1 proses)** — `lib/public-site-app.js` (factory Express minimal) + `routes/public-site.js` (landing `GET /`, form `GET /daftar`, `POST /api/public/register`) + `routes/public-anonymous.js` (voucher, di-share dua app): `index.js` membuka listener HTTP KEDUA pada `config.publicSite.port` (default 3011; `config.publicSite.host` default `0.0.0.0`, mode PORTAL set `127.0.0.1` = internal-only; flag `config.publicSite.enabled`, override env `PUBLIC_SITE_ENABLED`/`PUBLIC_PORT`/`PUBLIC_HOST`) dalam PROSES yang SAMA — SENGAJA TANPA `registerHttpAuth`/view-engine `.php`/Socket.IO/cron/WA kedua (invariant single-instance terjaga: hanya `createServer(publicApp).listen`; tidak memanggil `initializeBackgroundServices`/`startWhatsApp`). Tujuan: pisahkan surface publik dari login admin (kurangi attack surface; brute-force login tetap ditangani `authLimiter` + Cloudflare Access di edge, bukan oleh nomor port). Helmet CSP varian Turnstile via `buildHelmetOptions({allowTurnstile:true})` (`lib/http-security.js`) — CSP admin tak berubah; static subset (tanpa `/uploads` penuh & `/temp`). **Registrasi self-service** → `lib/services/public-registration-service.js`: verifikasi Cloudflare Turnstile FAIL-CLOSED (`lib/turnstile.js`, `config.turnstile`), lalu `getNextAvailablePSBId`+`insertPSBRecord` status `lead_online` (`created_by:'public_self_service'`) — LEAD TERKARANTINA (tak menyentuh MikroTik/GenieACS/`movePSBToUsers`; `submitPhase2` hanya terima status `phase1_completed`/`teknisi_meluncur`), sinkron snapshot `psbRecords` (`repositories/api-psb.repository.js`), notif admin `getAdminJids`+`sendCritical` (template `registrasi_lead_admin`) + calon pelanggan best-effort (`registrasi_lead_pelanggan`). Anti-abuse: rate-limit 5/15mnt/IP + Turnstile + `publicRegistrationValidation` (`lib/middleware/validation.js`). Landing/form server-rendered dari `lib/public-page-helper.js` (`global.config`+`global.packages`). Cloudflare (ops): untuk single-bot, hostname publik → :port publik; untuk MULTI-BOT surface publik dipersatukan oleh PORTAL (entri berikut) dengan listener publik bot di-set internal-only (127.0.0.1). Admin (:PORT) di-scope Cloudflare Access ke path staf agar customer portal tidak ikut terblokir.

<a id="b05"></a>
### Portal Publik Terpadu (multi-bot, proxy)

- **Portal Publik Terpadu (multi-bot, proxy)** — `portal.js` (entrypoint proses PM2 KE-3 `rafnet-portal`, BUKAN bot; tanpa WA/cron/DB → stateless, tak melanggar single-instance yg per-bot) + `lib/portal-app.js` (factory Express) + `routes/portal.js` (landing `GET /` / `GET /daftar` / `GET /voucher` + `GET /api/areas` + `POST /api/register` + proxy `GET /api/area/:area/:type/:id?`) + `lib/portal-areas.js` (whitelist area anti-SSRF, health-cache, `proxyJson`/`proxyBinary` header-BERSIH) + `static/portal-voucher.html` (varian area-aware `voucher-buy.html`): SATU halaman publik dengan pemilih AREA untuk 2 bot (DANDER :3010 / TANJUNGHARJO :3200). Portal = satu-satunya yang diekspos Cloudflare (1 hostname → :3300); MEM-PROXY registrasi/voucher ke listener publik bot area yang di-isolasi ke `127.0.0.1` (Dander :3011 / Tanjung :3201; di bot: `config.publicSite.host=127.0.0.1`, `turnstile.enabled=false`). Turnstile + rate-limit (5/15mnt/IP) DI PORTAL; `registerLimiter` bot di-skip utk loopback (`routes/public-site.js`) agar tak 429-palsu. Registrasi → proxy `POST /api/public/register` (lead PSB bot area). Voucher: `/api/area/:area/:type` → bot `/app/:type` (katalog/buy/qr/status); `reff` di-namespace per-area di RUTE (anti-bocor antar-area); **callback iPaymu tetap LANGSUNG ke bot** (port utama exposed, `config.ipaymuCallback` per-bot) — portal tak menyentuh uang. Config `portal.config.json` (gitignored; contoh `portal.config.example.json`): `portal.areas[{id,label,baseUrl,enabled}]` + `turnstile`. Guard: area tak dikenal → 404, disabled/not-ready → 503, `:type` di-allowlist (`packages|voucher|buy|detailtrx|statustrx|qr`), `baseUrl` SELALU dari config (bukan input). Tanjung `enabled:false` sampai migrasi v3-nya siap (deteksi `getReadyAreas` via health `GET /app/packages`).

<a id="b06"></a>
### Tandai Gratis (waiver)

- **Tandai Gratis (waiver)** — `applyFreeMonth` + tabel `payment_waivers` (`lib/payment-finance-service.js`) + endpoint `POST /api/payment-status/free` + tombol/badge di `static/js/payment-status.js`: menandai sebuah (pelanggan, periode) sebagai GRATIS/bebas tagihan. Waiver disimpan di tabel TERPISAH (pola `payment_reversals`) sehingga **tidak pernah ikut dijumlahkan sebagai pemasukan** (laporan revenue baca `payment_history`, bukan waivers). `getPaymentPositionForPeriod` kini meng-OR `is_waived` ke `is_fully_paid` (outstanding→0) → periode dihitung lunas, `user.paid=1` → aman isolir + sinkron rollover, TANPA menggembungkan pemasukan. Idempoten via `event_key` per (user, periode). Untuk mengoreksi periode yang terlanjur dicatat lunas cash → kombinasi `applyFreeMonth` (waiver) + `applyPaymentStatusChange paid:false` (reverse) → net pemasukan 0 tapi tetap lunas.

<a id="b07"></a>
### `repositories/message-log.repository.js`

- `repositories/message-log.repository.js` + hook di `message/raf.js`: logger pesan MASUK WA read-only (fire-and-forget, tak pernah throw/blokir jalur pesan) yang menulis ke `database/message_logs.sqlite` (tabel `inbound_messages`). Tujuan: kumpulkan korpus bahasa pelanggan untuk evaluasi fitur AI. Pakai konteks sender yang sudah di-resolve (`canonicalContext`) — tidak re-resolve agar tak meracuni `lid-mappings.json`; grup & pesan `fromMe` dilewati. Toggle `config.messageLogging.enabled` (default aktif). Review via `scripts/export-message-logs.js` (JSON+CSV, anotasi intent keyword best-effort).

<a id="b08"></a>
### Log Gangguan OLT + LOS→grup

- **Log Gangguan OLT + LOS→grup** (`repositories/olt-event.repository.js` → `database/olt_events.sqlite` + funnel `lib/olt-event-logger.js` + extend `lib/olt-los-broadcaster.js`): (1) **LOG DURABLE kejadian OLT ter-ENRICH pelanggan** — `recordOltEventSafe` (never-throw, fire-and-forget) di-hook di `olt-syslog-receiver.emitEvent` (SEMUA tipe DG/LOS/discovery) & `olt-log-scraper.dispatchLosTransitions` (backstop los/discovery); resolusi pelanggan by-MAC via `olt-customer-resolver`. Dedup **BERBASIS-STATE** (1 baris 'down' per outage, 1 'discovery' per pemulihan — kebal ganda syslog+scraper yg terpaut menit), 'discovery' memasangkan **durasi down→up**. Baca via `GET /api/olt/event-log` (filter from/to/type/q/oltId/mac + stats) → halaman `/olt-log` (`views/sb-admin/olt-log.php`, nav Jaringan→"Log Gangguan OLT"). Prune retensi harian (`config.oltEventLog.retentionDays` default 90) di `app-runtime`. Toggle `config.oltEventLog.enabled` default **true** (log internal, tanpa egress). (2) **LOS→GRUP**: `broadcastGroup` kini juga post ke GRUP alarm khusus (`config.oltLosBroadcast.notifyGroup`+`groupId`) via delivery boundary (`whatsapp-delivery-service.sendMessage`, pola `repair-group-notifier`) — INDEPENDEN dari japri teknisi (`notifyTeknisi`, default true, bisa OFF = cukup grup). **DG TIDAK ke grup** (bukan tugas teknisi). Picker grup di `/los-broadcast` (reuse `GET /api/whatsapp/groups`); `routes/admin-los-broadcast-routes.js normalizeLosConfig` menambah field grup. Default egress OFF (deploy gelap → aktifkan setelah pilih grup).

<a id="b09"></a>
### Auto-tiket dari LOS

- **Auto-tiket dari LOS** (`lib/los-ticket-service.js`, di-hook dari `olt-los-broadcaster.broadcastGroup` (create) + `cancelActiveIncident` (cancel)): saat LOS **TERKONFIRMASI** (bukan tiap kedip) → `ticket-workflow.createBaseTicket` (source `los`, issueType `LOS_FIBER`, priority HIGH, laporan ter-enrich OLT via `olt-name-resolver`) → **auto-assign teknisi least-loaded** (`processTicket`) → `report-notification-service.notifyNewReport` (teknisi+admin+grup perbaikan, **BUKAN pelanggan** — ditahan sampai teknisi OTW). **Dedup** 1 tiket terbuka per pelanggan/MAC; **ONU pulih** sebelum ditangani (status baru/process) → `cancelTicket` otomatis (kedip, bukan putus). Never-throw & fire-and-forget (tak menjatuhkan deteksi LOS). Config `oltLosBroadcast.autoTicket {enabled(default OFF),assignTeknisi,priority}` — di-set dari toggle di halaman `/los-broadcast` (form config utama), dinormalisasi di `routes/admin-los-broadcast-routes.js normalizeLosConfig` (**wajib**: `normalizeLosConfig` menulis ulang seluruh `oltLosBroadcast`, jadi tiap sub-key termasuk `autoTicket` harus ikut dinormalisasi atau ke-drop saat admin Simpan). Reuse penuh model tiket + notifikasi lifecycle yang sudah ada.

<a id="b10"></a>
### Verifikasi LOS↔DG via scrape web OLT

- **Verifikasi LOS↔DG via scrape web OLT** (`lib/olt-los-verifier.js`, di-hook di AWAL `olt-los-broadcaster.broadcastGroup`): syslog UDP bisa salah-vonis DG→LOS saat **mati listrik massal** (paket `dying-gasp` drop di transit → hanya `Lost` sampai → dikira LOS). Web log OLT (`sys_log_page.asp`) simpan `dying-gasp`+`Lost` PERMANEN (kebal packet-loss) = sumber otoritatif. `verifyLosBatch(macs)` reuse `olt-log-scraper.fetchOltLog`+`processLog`+`normalizeMAC` (dipromosikan jadi export publik) + `olt-manager.getOltDevices` (host+kredensial web per-OLT); scrape 10–20 halaman, korelasi, klasifikasi otoritatif per-MAC. **Prinsip aman:** broadcaster HANYA membuang LOS yang scrape buktikan **`dying-gasp`** (status insiden `suppressed_dg_scrape`); selain itu (los/gagal/busy/tak-ada-device/absen → `unknown`) **lanjut** (fallback ke syslog). Guard anti-tabrakan `isScraperBusy()` (jangan fetch bareng siklus scraper periodik → ECONNREFUSED). NEVER-THROW. Config `oltLosBroadcast.verifyViaScrape {enabled(default ON),maxPages,timeWindowMinutes}` (dinormalisasi di `normalizeLosConfig` + toggle di `/los-broadcast`). **Gerbang GANGGUAN-AREA (lapis-2, universal):** auto-tiket per-ONU DILEWATI bila ≥`clusterThreshold` LOS serentak/1 OLT (dugaan area/backbone atau sisa mati-listrik yang lolos verify) → cukup 1 alarm grup, cegah banjir tiket. Kelayakan: butuh `config.olt.devices` berisi host+`webUsername`/`webPassword` OLT (Dander sudah; Tanjung ditambah).

<a id="b11"></a>
### `lib/app-runtime.js`

- `lib/app-runtime.js`: runtime context tunggal untuk state global, repository state, dan background services.

<a id="b12"></a>
### `lib/routes-registry.js`

- `lib/routes-registry.js`: registry mount router HTTP agar urutan route dapat diuji tanpa membaca `index.js` penuh.

<a id="b13"></a>
### `lib/domain-events.js`

- `lib/domain-events.js` dan `lib/domain-notification-listeners.js`: event bridge tipis untuk domain notifikasi awal seperti perubahan paket, diskon, pembayaran parsial, dan update status pelanggan.

<a id="b14"></a>
### `lib/whatsapp-gateway.js`

- `lib/whatsapp-gateway.js`: kontrak runtime WA terpusat agar perubahan shape socket Baileys terlokalisasi sekaligus menyediakan observability stabil (`hasSocket`, `hasAuthenticatedSession`, `getSocketDiagnostics`, `getStatusSnapshot`, `closeActiveSocket`).

<a id="b15"></a>
### `lib/whatsapp-bootstrap.js`

- `lib/whatsapp-bootstrap.js`: boundary lifecycle WA untuk start, reconnect, sinkronisasi state/socket gateway, dan cleanup runtime.

<a id="b16"></a>
### `lib/whatsapp-inbound-adapter.js`

- `lib/whatsapp-inbound-adapter.js`: owner normalisasi event/payload pesan masuk dari Baileys ke kontrak internal bot agar perubahan shape inbound tidak bocor ke router domain.

<a id="b17"></a>
### `lib/process-lifecycle.js`

- `lib/process-lifecycle.js`, `lib/http-security.js`, `lib/http-auth-bootstrap.js`, dan `lib/http-socket-bootstrap.js`: boundary bootstrap baru untuk process handler, security/auth middleware, serta HTTP+Socket.IO startup agar `index.js` turun menjadi composition root tipis.

<a id="b18"></a>
### `message/handlers/reply-runtime.js`

- `message/handlers/reply-runtime.js`: helper reply/contact card agar `message/raf.js` tidak lagi memegang primitive kirim berbasis socket mentah; handler upload bukti bayar aktif kini juga mengunduh media lewat `lib/whatsapp.adapter.js`, bukan import Baileys langsung.

<a id="b19"></a>
### `routes/admin-router.js`

- `routes/admin-router.js`: komposer admin per bounded context dengan dependency factory terpisah agar registrar tidak lagi menerima bucket `deps` raksasa.

<a id="b20"></a>
### `services/payment-approval.service.js` + `routes/requests.js`

- `services/payment-approval.service.js` + `routes/requests.js`: owner approval payment request yang menulis ledger melalui `lib/payment-finance-service.js`; service kini membaca user/account/DB lewat runtime cache repository dan route legacy admin hanya dikunci sebagai fallback non-aktif.

<a id="b21"></a>
### `services/payment-flow.service.js` + `repositories/payment.repository.js` + `message/handlers/payment-processor-handler.js`

- `services/payment-flow.service.js` + `repositories/payment.repository.js` + `message/handlers/payment-processor-handler.js` / `message/handlers/topup-handler.js`: owner orchestration bot-side untuk create request payment/topup, voucher purchase flow, dan proof upload kini aktif di service+repository; handler payment/topup tinggal adapter tipis, sementara approval/status final tetap ditahan oleh `services/payment-approval.service.js` dan ledger.

<a id="b22"></a>
### `routes/api-users-routes.js` + `services/api-users.service.js` + `repositories/api-users.repository.js`

- `routes/api-users-routes.js` + `services/api-users.service.js` + `repositories/api-users.repository.js`: owner aktif domain users/customer API untuk list users, create/update payment-aware upsert, integrity check runtime-vs-SQLite, payment status update, create/edit user, delete user tunggal, delete-all users, serta template/export/import Excel pelanggan dengan preview validasi; route kini mendelegasikan flow utama users ke service/repository owner. **Hapus user tunggal (`deleteUserById`) kini juga membersihkan MikroTik TUNTAS:** putus sesi aktif (`deleteActivePPPoEUser` → `/ppp/active/remove`) + HAPUS secret (`removePPPoESecret` → `views/delete_pppoe_secret.php` `/ppp/secret/remove`, idempoten, best-effort never-throw) supaya pelanggan yang dihapus tak bisa reconnect. Di-wire `routes/api.js`→`api-users-routes.js`→service.

<a id="b23"></a>
### `routes/api-voucher-routes.js` + `services/api-voucher.service.js` + `repositories/api-voucher.repository.js`

- `routes/api-voucher-routes.js` + `services/api-voucher.service.js` + `repositories/api-voucher.repository.js`: owner aktif domain voucher API untuk profile read-model, sent-history, statistik, generate/send voucher, dan member-credential delivery; route kini mendelegasikan flow GET/POST utama voucher ke service/repository owner, sementara fallback helper file/PHP/delivery tetap dipakai sebagai adapter di bawah owner baru.

<a id="b24"></a>
### Voucher: dashboard penjualan + notif terjual + badge Terlaris

- **Voucher: dashboard penjualan + notif terjual + badge Terlaris** (2026-07-07): (1) **Dashboard** — API `GET /api/voucher/sales-stats` (`routes/api-voucher-routes.js`, staff-guard, read-only) agregasi `global.payment` (record berbayar tag `buynowweb`/`buynow`) → terjual hari/7hari/total + pendapatan + paket terlaris + terbaru; peta harga→nama dari `global.voucher`. `lib/payment.js addPayment` kini stempel `createdAt` (Date.now(), additive; record lama tanpa tanggal tetap dihitung di total). Halaman `/voucher-sales` (`views/sb-admin/voucher-sales.php` + `static/js/voucher-sales.js`, tema modern reuse `paket-voucher.css`); route `routes/pages.js` gated `config.voucherSalesDashboard.enabled` (default true; OFF→404) + link sidebar "Penjualan Voucher" (`_navbar.php`, +isParentActive `/voucher-sales`). (2) **Notif admin terjual** — hook cabang SUKSES `buynowweb` (`routes/public.js`) → `alertAdmins` template `voucher_terjual_admin` (`database/message_templates.json`), gated `config.voucherSaleNotif.enabled` (default false, anti-spam), best-effort never-throw. (3) **Badge "Terlaris" manual** — `config.voucherFeatured=<prof>` → `routes/public-anonymous.js` `/app/voucher` tandai record `featured` → `static/voucher-buy.html` render badge amber "Terlaris" (selain "Hemat" auto teal).

<a id="b25"></a>
### Cetak Voucher mandiri

- **Cetak Voucher mandiri** (`services/voucher-print/*` + `services/voucher-print.service.js` + `repositories/voucher-print.repository.js`, route di `routes/api-voucher-routes.js` prefix `/api/voucher/print/*`, halaman `/voucher-print` = `views/sb-admin/voucher-print.php` + `static/js/voucher-print.js`): engine render kartu voucher (layout HTML ber-placeholder + QR via paket `qrcode` + peta harga→warna), settings/layout custom di `database/voucher_print_settings.json`/`voucher_layouts.json`, impor template Mikhmon (konversi PHP→placeholder tanpa eksekusi), dan **batch generate efisien** lewat `lib/mikrotik.js addHotspotUsersBatch` → bridge `views/get_hotspot_batch_add.php` (SATU koneksi RouterOS untuk N add; format kode length/chartype/prefix; mode custom via daftar usernames). Tujuan: lepas ketergantungan Mikhmon untuk generate+cetak (expiry tetap di MikroTik).

<a id="b26"></a>
### `routes/api-network-routes.js` + `services/api-network.service.js` + `repositories/api-network.repository.js`

- `routes/api-network-routes.js` + `services/api-network.service.js` + `repositories/api-network.repository.js`: owner awal domain network API untuk manual send WA, read-model PPPoE tak terdaftar, devices-for-import GenieACS, dan skeleton source export MikroTik yang customizable untuk PPPoE secrets/profile serta Hotspot profile; route kini mendelegasikan flow read-model/import aktif ke service/repository owner, sementara adapter runtime WA dan helper MikroTik masih diinjeksikan dari route sebagai dependency boundary. (Legacy `POST /api/action` di-shadow dan kini dihapus dari router ini — ownership tunggal ada pada stub `410` `routes/admin.js`; update PPPoE profile yang live dimiliki `routes/api-users-routes.js`.)

<a id="b27"></a>
### `routes/api-psb-routes.js` + `services/api-psb.service.js` + `repositories/api-psb.repository.js`

- `routes/api-psb-routes.js` + `services/api-psb.service.js` + `repositories/api-psb.repository.js`: owner awal domain PSB API untuk snapshot/read-model PSB aktif seperti list customers, submit phase 1, update status teknisi, submit phase 2, submit phase 3, dan delete-all; route kini mulai mendelegasikan read-model serta mutasi phase 1/status/pemasangan/provisioning awal/cleanup PSB ke service/repository owner, sementara upload murni dan handoff notifikasi/device flow lain masih menunggu ekstraksi lanjut.

<a id="b28"></a>
### `routes/admin.routes.js`

- `routes/admin.routes.js`, `controllers/admin.controller.js`, `services/admin.service.js`, `services/billing.service.js`, `repositories/admin.repository.js`, dan `repositories/billing.repository.js`: boundary baru untuk hotspot admin billing/package-change agar router tipis dan persistence terisolasi.

<a id="b29"></a>
### `repositories/voucher.repository.js`

- `repositories/voucher.repository.js`, `repositories/saldo.repository.js`, dan `repositories/ticket.repository.js`: owner aktif untuk katalog voucher, saldo user, dan draft laporan/ticket id boundary; consumer bot prioritas sekarang membaca concern ini lewat repository dengan helper legacy tinggal fallback compatibility.

<a id="b30"></a>
### `routes/admin-wifi-ops-routes.js` + `services/network-ops.service.js` + `repositories/wifi.repository.js`

- `routes/admin-wifi-ops-routes.js` + `services/network-ops.service.js` + `repositories/wifi.repository.js`: owner observability jaringan dan mutasi WiFi perangkat; lookup customer sekarang lewat runtime cache repository, logging/history WiFi web turun ke repository owner, dan route legacy admin hanya menyisakan stub `410` untuk path network yang sudah dipindah.

<a id="b31"></a>
### `routes/admin-config-routes.js` + `services/genieacs-parameter-config.service.js` + `services/mikrotik-device-config.service.js`

- `routes/admin-config-routes.js` + `services/genieacs-parameter-config.service.js` + `services/mikrotik-device-config.service.js`: owner konfigurasi parameter GenieACS dan setup device MikroTik; CRUD legacy di `routes/admin.js` hanya menyisakan stub `410`.

<a id="b32"></a>
### `routes/admin-content-routes.js` + `services/wifi-template-config.service.js` + `services/admin-broadcast.service.js`

- `routes/admin-content-routes.js` + `services/wifi-template-config.service.js` + `services/admin-broadcast.service.js`: owner content/config lanjutan untuk WiFi templates dan broadcast admin dengan seleksi user dari runtime repository dan tanpa primitive WA langsung di router.

<a id="b33"></a>
### `routes/admin-content-routes.js` + `views/sb-admin/templates.php`

- `routes/admin-content-routes.js` + `views/sb-admin/templates.php`: owner full editor `/api/templates` untuk template pesan WhatsApp; admin web kini mengedit notification, WiFi, response, command, error, success, system, menu, dan report templates dengan save metadata-safe, `/api/templates/diagnostics` membawa audit placeholder/metadata responseTemplates, sementara `routes/message-templates.js` tetap compat API notification-template lama.

<a id="b34"></a>
### `database/response_templates.json`

- `database/response_templates.json` + WiFi state handler + `message/handlers/agent-voucher-handler.js`: migrasi awal pesan WhatsApp hardcoded ke template admin; prompt/confirm/success prioritas WiFi name/password serta flow agent voucher beli/jual, quantity, payment, customer phone, confirmation, success/failure, inventory empty, dan history empty memakai `responseTemplates` dengan fallback runtime aman.

<a id="b35"></a>
### `repositories/auto-outage.repository.js` + `services/auto-outage-*.service.js` + `routes/admin-auto-outage-routes.js` + `views/sb-admin/auto-outage.php`

- `repositories/auto-outage.repository.js` + `services/auto-outage-*.service.js` + `routes/admin-auto-outage-routes.js` + `views/sb-admin/auto-outage.php`: owner auto outage broadcast berbasis PPPoE MikroTik; scan hanya mencocokkan `pppoe_username` pelanggan di DB, memakai `/ppp active` untuk online, `last-logged-out` PPP secret untuk offline_since, rule admin customizable, dry-run eligible, broadcast WhatsApp interaktif, dan konfirmasi tiket sebelum pembuatan laporan.

<a id="b36"></a>
### `database/response_templates.json` + `message/handlers/raf-intent-dispatch.js` + `message/handlers/agent.js` + `message/handlers/monitoring-handler.js`

- `database/response_templates.json` + `message/handlers/raf-intent-dispatch.js` + `message/handlers/agent.js` + `message/handlers/monitoring-handler.js`: Wave 3 migrasi pesan WhatsApp hardcoded untuk dispatcher global, agent umum, dan monitoring; pesan user-facing prioritas memakai `responseTemplates` dengan fallback runtime aman tanpa mengubah logic bisnis/list dinamis.

<a id="b37"></a>
### `database/response_templates.json` + `message/handlers/agent.js`

- `database/response_templates.json` + `message/handlers/agent.js`: Wave 4 migrasi pesan WhatsApp hardcoded untuk agent transaction/self-service seperti konfirmasi transaksi, status topup, ganti PIN, update profil, toggle outlet, dan error profil sendiri; pesan statis prioritas memakai `responseTemplates` dengan fallback runtime aman tanpa mengubah saldo, credential, atau delivery WA.

<a id="b38"></a>
### `database/response_templates.json` + `message/handlers/states/other-state-handler.js` + `message/handlers/states/report-state-handler.js`

- `database/response_templates.json` + `message/handlers/states/other-state-handler.js` + `message/handlers/states/report-state-handler.js`: Wave finalisasi lanjutan untuk state legacy cancel tiket, reboot, power WiFi, Speed on Demand, perubahan paket, dan laporan gangguan 6 langkah; pesan user-facing kini dirender dari `responseTemplates` agar bisa diedit lewat admin template.

<a id="b39"></a>
### `database/response_templates.json` + `message/handlers/domains/reporting.domain.js` + `message/handlers/steps/general-steps.js`

- `database/response_templates.json` + `message/handlers/domains/reporting.domain.js` + `message/handlers/steps/general-steps.js`: Wave finalisasi general/reporting untuk progress cek perangkat, reboot, bukti bayar, komplain, proses tiket, upload foto, resolusi teknisi, notifikasi customer, dan pertanyaan umum; pesan user-facing kini dirender dari `responseTemplates`.

<a id="b40"></a>
### `database/response_templates.json` + `routes/public.js` + `routes/saldo.js` + `routes/tickets.js` + `services/admin.service.js` + `lib/report-notification-service.js`

- `database/response_templates.json` + `routes/public.js` + `routes/saldo.js` + `routes/tickets.js` + `services/admin.service.js` + `lib/report-notification-service.js`: Wave routes/outbound WA untuk notifikasi Speed on Demand owner, topup saldo agent, tiket web-created customer, request/approval perubahan paket, tiket baru ke tim, dan caption foto laporan; payload WhatsApp prioritas kini dirender dari `responseTemplates`, sementara response JSON API tetap literal controller biasa.

<a id="b41"></a>
### `database/response_templates.json` + `lib/topup-expiry.js` + `lib/psb-notification.js` + `lib/alert-system.js` + `routes/tickets.js`

- `database/response_templates.json` + `lib/topup-expiry.js` + `lib/psb-notification.js` + `lib/alert-system.js` + `routes/tickets.js`: Wave sisa outbound WA untuk reminder/expired topup, section kontak teknisi PSB, alert admin, daily report, dan notice jam kerja tiket; render `responseTemplates` distandarkan ke string `.text` agar payload WhatsApp tidak mengirim object diagnostik.

<a id="b42"></a>
### `database/response_templates.json` + `lib/services/profile-update-service.js` + `lib/services/customer-service.js` + `lib/services/speed-request-service.js` + `lib/services/report-service.js` + `routes/speed-requests.js` + `routes/requests.js`

- `database/response_templates.json` + `lib/services/profile-update-service.js` + `lib/services/customer-service.js` + `lib/services/speed-request-service.js` + `lib/services/report-service.js` + `routes/speed-requests.js` + `routes/requests.js`: Wave service/route web outbound WA untuk default profile update, request ubah paket, request/bukti/approval Speed on Demand, submit laporan customer, dan pengajuan pembayaran teknisi; pesan prioritas kini memakai `responseTemplates` dan tetap dikirim lewat delivery service.

<a id="b43"></a>
### `database/response_templates.json` + `message/handlers/smart-report-text-menu.js` + `message/handlers/smart-report-handler.js` + `message/handlers/ticket-process-handler.js` + `message/handlers/teknisi-workflow-handler.js`

- `database/response_templates.json` + `message/handlers/smart-report-text-menu.js` + `message/handlers/smart-report-handler.js` + `message/handlers/ticket-process-handler.js` + `message/handlers/teknisi-workflow-handler.js`: Wave smart-report text-menu, smart-report legacy, ticket-process legacy, dan teknisi workflow proses/OTW/sampai/OTP/foto/completion untuk pesan start report, validasi pelanggan, active report, menu gangguan, kontak CS, cek perangkat mati/lemot, auto-redirect offline, invalid pilihan, hasil troubleshooting, konfirmasi/cancel, upload foto, speedtest, tiket WiFi/lemot, OTP proses tiket, verifikasi OTP, completion code, final confirmation, sukses tiket, status perjalanan/kedatangan teknisi, prompt dokumentasi foto, review resolusi, dan penutupan tiket; payload WhatsApp prioritas kini memakai `responseTemplates` agar bisa dikustomisasi dari admin web.

<a id="b44"></a>
### `services/wifi-management.service.js` + `repositories/wifi.repository.js` + `message/handlers/wifi-management-handler.js`

- `services/wifi-management.service.js` + `repositories/wifi.repository.js` + `message/handlers/wifi-management-handler.js`: owner orchestration rename/password WiFi kini aktif di service, write-path log/history WiFi turun ke repository owner, sementara handler bot mempertahankan validasi/reply adapter dan signature kompatibel untuk flow state legacy.

<a id="b45"></a>
### `routes/admin-ops-routes.js` + `services/admin-ops.service.js` + `repositories/admin-ops.repository.js`

- `routes/admin-ops-routes.js` + `services/admin-ops.service.js` + `repositories/admin-ops.repository.js`: owner utility admin destruktif seperti delete generic, hapus semua user, dan cleanup foto yatim dengan cache users/accounts/catalog dibaca lewat runtime cache repository, sementara persistence `mikrotik_devices.json` turun ke repository owner.

<a id="b46"></a>
### `routes/admin-database-routes.js` + `services/admin-database-ops.service.js`

- `routes/admin-database-routes.js` + `services/admin-database-ops.service.js`: owner utility admin untuk debug database dan migrasi users berbasis upload JSON; DB dan cache users kini dibaca lewat dependency runtime/repository.

<a id="b47"></a>
### `routes/admin.js`

- `routes/admin.js`: untuk endpoint package change, bulk payment update, WiFi templates, broadcast, utility admin, debug database, migrate users, list/read-model ringan, dan setup device MikroTik yang sudah punya owner baru, file legacy ini hanya menyimpan stub `410` agar tidak terjadi shadow ownership.

<a id="b48"></a>
### `routes/payment-status.js` + `lib/payment-finance-service.js`

- `routes/payment-status.js` + `lib/payment-finance-service.js`: source of truth payment status berbasis ledger periodik; route lain tidak boleh menulis `users.paid` sebagai sumber final.

<a id="b49"></a>
### `lib/paid-flag-reconcile.js`

- `lib/paid-flag-reconcile.js` (`reconcilePaidFlags`/`runStartupPaidReconcileSafe`) + cron `lib/cron/jobs/paid-reconcile.js` (`initPaidReconcileTask`, default harian 03:00, aktif-tanpa-config): **self-heal** flag cache `users.paid` ↔ ledger periode berjalan (dua arah, skip infra+whitelist, reuse `syncUserPaidStatusForPeriod`, READ-ONLY thd ledger — tak menulis pembayaran/pemasukan/WA). Dipanggil saat **boot** (`lib/database.js`, dirantai setelah backfill migrasi) **dan harian**. Menutup celah: cron `set-unpaid` (tgl 1) bisa TERLEWAT bila proses mati saat itu (node-cron tak mengejar tick lewat) → flag nyangkut di potret bulan lalu → Broadcast Tagihan/reminder/isolir memakai status basi (insiden Juli 2026: flag=potret Juni, 35 penunggak "lunas" palsu). Kill-switch `paid_reconcile_enabled=false` (default ON). Test: `lib/__tests__/paid-flag-reconcile.test.js` + `lib/cron/__tests__/paid-reconcile.test.js`.

<a id="b50"></a>
### `routes/arrears.js` + `services/arrears.service.js` + `repositories/arrears.repository.js`

- `routes/arrears.js` + `services/arrears.service.js` + `repositories/arrears.repository.js`: owner read-model tunggakan pelanggan berbasis periode untuk tab operasional/manajerial, summary collection rate, dan detail statement pelanggan tanpa bergantung pada histori `users.paid`.

<a id="b51"></a>
### `lib/runtime-state.js` + `lib/runtime-repositories.js`

- `lib/runtime-state.js` + `lib/runtime-repositories.js`: runtime state/repository kini juga menutup akses ke `db`, `io`, `config`, dan `monitoringConfig` sebagai compatibility mirror `global.*`.

<a id="b52"></a>
### `repositories/runtime-cache.repository.js`

- `repositories/runtime-cache.repository.js`: adapter cache bersama untuk service legacy aktif agar purge `global.*` dapat dilakukan konsisten tanpa menduplikasi wrapper users/accounts/catalog per service.

<a id="b53"></a>
### `message/handlers/bot-context.js`

- `message/handlers/bot-context.js`, `message/handlers/bot-pipeline.js`, `message/handlers/intent-owner-map.js`, `message/handlers/domain-handlers.js`, dan `message/handlers/domain-services.js`: boundary tipis context/pipeline/ownership agar `message/raf.js` tetap menjadi composition router, menerima runtime eksplisit, dan facade domain memprioritaskan repository owner saldo/ticket/voucher dari runtime.

<a id="b54"></a>
### `message/handlers/conversation-state-owner-map.js`

- `message/handlers/conversation-state-owner-map.js`, `message/handlers/conversation-state-router.js`, dan `message/handlers/state-domains/*`: owner map + router state percakapan agar branch state reporting/WiFi/teknisi/payment/agent-voucher dipusatkan per bounded context; fallback reporting aktif dan state lokasi perjalanan teknisi sudah keluar dari `message/raf.js` sehingga router utama tinggal menjadi orchestrator state.

<a id="b55"></a>
### `message/handlers/simple-location-handler.js`

- `message/handlers/simple-location-handler.js`: owner update live-location tiket aktif non-state; `message/raf.js` kini hanya mendelegasikan payload lokasi ke helper ini tanpa lookup ticket langsung.

<a id="b56"></a>
### `message/handlers/teknisi-workflow-handler.js`

- `message/handlers/teknisi-workflow-handler.js`: workflow teknisi aktif kini tidak lagi membuat `createScopedStateProxy('teknisi')` sendiri dan memakai API state terkelola dari `conversation-handler`.

<a id="b57"></a>
### `message/raf.js`

- `message/raf.js`: router bot tidak lagi membuat mirror `createScopedStateProxy('legacy-temp')` maupun mengisi `global.teknisiStates`; compatibility proxy tidak lagi punya caller aktif di router/context dan kini tersisa sebagai boundary transisional di `conversation-handler` + test compatibility.

<a id="b58"></a>
### `message/handlers/conversation-state-handler.js` + `message/handlers/states/*.js`

- `message/handlers/conversation-state-handler.js` + `message/handlers/states/*.js`: chain managed conversation state kini tidak lagi meneruskan parameter `temp` zombie; sub-handler prioritas berbicara langsung ke `userState` dan owner API state, sedangkan compatibility proxy tersisa terisolasi di boundary state lama/test.

<a id="b59"></a>
### `message/handlers/template-helpers.js` + `database/response_templates.json`

- `message/handlers/template-helpers.js` + `database/response_templates.json` + 17 handler bot (voucher-management, network-management, wifi-power, reboot-modem, wifi-check, legacy-wifi-state, billing-management, package-management, balance-management, access-management, states/wifi-password-state-handler, states/wifi-name-state-handler, steps/general-steps, conversation-state-handler, utility-handler, monitoring-handler): Wave F1-F5 finalisasi migrasi hardcoded WA messages ke `responseTemplates` dengan ~100 key baru (voucher/statik profile CRUD, network binding/queue/ppp, wifi power/reboot/check/confirm, billing/package/SOD, balance topup/delsaldo/transfer, access management, state errors, utility, monitoring stats); helper sentralisasi `renderResponseTemplate(key, fallback, data)` menjamin fallback aman saat admin belum customize, sehingga semua pesan WhatsApp user-facing kini editable lewat admin web `/api/templates` tab Response tanpa mengubah logika bisnis.

<a id="b60"></a>
### `message/handlers/saldo-handler.js` + `message/handlers/wifi-history-handler.js` + `message/handlers/photo-upload-queue.js` + `message/handlers/photo-workflow-handler.js` + `message/handlers/states/wifi-name-state-handler.js`

- `message/handlers/saldo-handler.js` + `message/handlers/wifi-history-handler.js` + `message/handlers/photo-upload-queue.js` + `message/handlers/photo-workflow-handler.js` + `message/handlers/states/wifi-name-state-handler.js`: Wave F6 sisa handler customer-facing critical (saldo cek/topup/transfer/voucher, wifi change history dengan perbaikan UTF-8 mojibake, photo queue + workflow reminder, wifi name state target missing); migrasi menambahkan 22 key baru (`saldo_*`, `wifi_history_*`, `photo_*`, `wifi_name_bulk_target_missing`) semua pakai `renderResponseTemplate` pola fallback yang sama.

<a id="b61"></a>
### `lib/response-template-helper.js`

- `lib/response-template-helper.js` (new) + `lib/approval-logic.js` + `lib/device-status.js`: Wave F7 outbound notification lib (sibling helper `lib/response-template-helper.js` membungkus `template-service.renderCategoryTemplate` untuk dipakai lapisan `lib/`, approval notification DISETUJUI/DITOLAK + PEMBAYARAN BERHASIL DIPROSES, device offline message) — admin kini bisa customize notifikasi internal teknisi dan notifikasi device offline lewat key `approval_teknisi_status_notification`, `approval_teknisi_payment_confirmation`, `device_offline_message`.

<a id="b62"></a>
### `message/handlers/raf-intent-dispatch.js` + `message/handlers/raf-intent-dispatch/{index,conversation-intents,menu-intents,reporting-intents,ticket-customer-intents,ticket-teknisi-intents,status-mikrotik-intents,saldo-intents,agent-intents,wifi-intents,network-admin-intents,customer-service-intents,owner-admin-intents,verification-intents}.js`

- `message/handlers/raf-intent-dispatch.js` + `message/handlers/raf-intent-dispatch/{index,conversation-intents,menu-intents,reporting-intents,ticket-customer-intents,ticket-teknisi-intents,status-mikrotik-intents,saldo-intents,agent-intents,wifi-intents,network-admin-intents,customer-service-intents,owner-admin-intents,verification-intents}.js`: Priority 3A pemecahan dispatcher intent WhatsApp ke composer map-based per domain; path publik `message/handlers/raf-intent-dispatch.js` tetap dipertahankan untuk caller `message/raf.js`, sementara composer kini enrich context (`runtimeSocket`, alias completion confirmation, renderer template) lalu lookup handler dari registry subfolder; guardrail Wave 3/F6/F7 diperbarui untuk scan composer + submodule sehingga key `raf_dispatch_*` dan `dispatch_*` tetap terverifikasi setelah split.

<a id="b63"></a>
### `lib/template-service.js` + `lib/template-manager.js` + `lib/message-template-helper.js`

- `lib/template-service.js` + `lib/template-manager.js` + `lib/message-template-helper.js`: Priority 1+2B stabilisasi template adapter — `template-service.recordLegacyUsage()` kini rate-limited per unique `(adapter, operation, templateId)` dan dikontrol oleh env `SUPPRESS_TEMPLATE_LEGACY_WARNINGS=1`; 3 adapter dead code (`lib/message-helper.js`, `lib/message-manager.js`, `lib/handler-messages.js`) dihapus permanen; 2 adapter tersisa (`template-manager` multi-category lookup, `message-template-helper` compat notification+formatter) dipromote jadi proper facade dengan Header Doc lengkap dan semua `recordLegacyUsage` call dihilangkan, sehingga startup stderr dari 1701 bytes/6 warnings menjadi 5 bytes/0 warnings permanen.

<a id="b64"></a>
### `services/admin-ops.service.js` + `routes/admin-ops-routes.js`

- `services/admin-ops.service.js` + `routes/admin-ops-routes.js`: Priority 1B seragamkan pola return controller — `deleteEntityByCategory` kini mengembalikan `{ status, message }` flat (sebelumnya `{ status, body: {...} }`) dan router menyalurkan `res.status(result.status).json(result)` seperti endpoint lain; guardrail test `admin-ops.service.runtime-boundary.test.js` mencegah wrapper `body` kembali.

<a id="b65"></a>
### `services/api-users.service.js` + `services/api-users/{default-deps,list-users-integrity,update-user-payment-status,delete-user-by-id,delete-all-users,create-user,create-user-validate,create-user-mikrotik-sync,create-user-persist,update-user-by-id}.js`

- `services/api-users.service.js` + `services/api-users/{default-deps,list-users-integrity,update-user-payment-status,delete-user-by-id,delete-all-users,create-user,create-user-validate,create-user-mikrotik-sync,create-user-persist,update-user-by-id}.js`: Priority 2D pemecahan factory `services/api-users.service.js` (1062 lines, 47.5KB) jadi 11 file by-method/by-phase di sub-folder `services/api-users/` — composer 59 lines (3.6KB; thin factory `createApiUsersService(overrides)` re-export 6 publik methods + `deps` shape persis sama dengan original); Pattern A standalone async function (`async function methodName(deps, args)`) dengan composer wrap setiap method jadi closure yang panggil sub-file dengan `deps` injected; `upsertUserFromAdminPanel` orchestrator (63L) dipecah jadi 3-phase pipeline (`create-user-validate` 148L untuk validasi+ID generation+credentials prep+payment method validation, `create-user-mikrotik-sync` 100L untuk mikrotik sync per registration mode `new`/`import`/`legacy`, `create-user-persist` untuk DB insert+paid status apply dengan rollback+**waiver bulan pertama opsional** (toggle form `userData.free_first_month`→`applyFreeMonth`/`lib/payment-finance-service` lazy-require overridable via `deps.applyFreeMonth`; best-effort, mutual-eksklusif dgn paid, hanya paket bertagihan>0; UI checkbox `#create_free_first_month` di `views/sb-admin/users.php` + payload di `static/js/users.js`; test `services/api-users/__tests__/create-user-persist.test.js`)+activity log+welcome msg) — `this.updateUserById` self-reference di-replace dengan `service.updateUserById(...)` via parent ref pass; lazy-require path di-update dari `../lib/X` ke `../../lib/X` (psb-helper, genieacs-helper, utils). Path `services/api-users.service.js` dipertahankan persis karena `routes/api-users-routes.js` + 6 test cases di `services/__tests__/api-users.service.test.js` require by exact path. Verifikasi: 6 publik methods + `deps` shape match original; 6/6 test cases PASS (`listUsersWithIntegrityCheck`, `updateUserPaymentStatus`, `deleteUserById`, `deleteAllUsers`, `updateUserById`, `upsertUserFromAdminPanel`); smoke-core regression 19 suites (53 tests) PASS; full project test 184/185 suites PASS (1 pre-existing fail unchanged); startup `npm start` stderr 0 bytes, 0 hard errors.

<a id="b66"></a>
### `lib/saldo-manager.js` + `lib/saldo/{shared,saldo-repository,balance-operations,transfer-operations,transactions-store,topup-store}.js` + `message/handlers/balance-management-handler.js`

- `lib/saldo-manager.js` + `lib/saldo/{shared,saldo-repository,balance-operations,transfer-operations,transactions-store,topup-store}.js` + `message/handlers/balance-management-handler.js`: Priority 2C pemecahan monolit `lib/saldo-manager.js` (1387 lines, 66.1KB) jadi 7 file by-domain di sub-folder `lib/saldo/` — composer 89 lines (5.8KB; thin facade re-export 26 keys persis sama dengan original `module.exports` termasuk alias `getSaldo`), shared 103 lines (5.0KB; path constants + `normalizeUserJid` + `formatCurrency` + singleton `getSaldoDb`), saldo-repository 150 lines (6.1KB; SQLite read helpers low-level), balance-operations 462 lines (28.5KB; `addSaldo`/`deductSaldo`/`getUserSaldo`/`createUserSaldo`/`updatePushname`/stats/getAll dengan BEGIN/COMMIT/ROLLBACK pattern), transfer-operations 221 lines (16.5KB; atomic dual-update `transferSaldo`), transactions-store 115 lines (5.1KB; JSON ledger + `addTransaction`/`pushTransactionRecord`), topup-store 235 lines (10.3KB; JSON store + workflow `verifyTopupRequest`+`processAgentConfirmation` dengan lazy-require `addSaldo` untuk hindari circular dep). Path `lib/saldo-manager.js` dipertahankan persis karena `payment-topup-boundary-baseline.test.js` assert string match terhadap require path. Side-fix bug pre-existing: `balance-management-handler.js:186` panggil `saldoManager.getAllSaldoDataFromDb()` yang **tidak di-export** (silent failure di-wrap try-catch ignore) — fixed dengan rename ke `getAllSaldoData()` (functionally identical wrapper). Verifikasi: 26 export keys match original; semua 10 saldo test suite (17 tests) PASS termasuk `topup-lid-flow`, `saldo-canonical`, `payment-topup-boundary-baseline`, `wave-2-helper-ownership-baseline`, `bot-hardening`, `saldo-payment-domain`, `saldo-repository-owner`, `saldo-proof-route`, `saldo-ticket-repositories`, `payment-flow.service`; smoke-core regression 18 suites (47 tests) PASS; full project test 184/185 suites PASS (1 pre-existing failure di `wa-hardcoded-message-migration-wave3.test.js` tidak related dengan refactor saldo); startup `npm start` stderr 0 bytes, 0 hard errors, 0 saldo errors.

<a id="b67"></a>
### `lib/cron.js` + `lib/cron/shared.js` + `lib/cron/jobs/{reminder,set-unpaid,isolir,isolir-notification,compensation-revert,speed-revert,redaman-check,telegram-backup}.js`

- `lib/cron.js` + `lib/cron/shared.js` + `lib/cron/jobs/{reminder,set-unpaid,isolir,isolir-notification,compensation-revert,speed-revert,redaman-check,telegram-backup}.js`: Priority 3C pemecahan monolit `lib/cron.js` (1121 lines, 54.5KB) jadi 10 file by-job-domain di sub-folder `lib/cron/` — composer 59 lines (3.3KB), shared 110 lines (4.3KB; helpers `delay`/`isValidCron`/`replacer`/`getNestedValue`/`loadCronConfig`/`safeSendMessage`), 8 job file dengan state holder ter-encapsulasi per file (reminder 155L, set-unpaid 40L, isolir 71L, isolir-notification 78L, compensation-revert 159L, speed-revert 183L, redaman-check 203L, telegram-backup 60L); kontrak module export composer dipertahankan persis (`initializeAllCronTasks`, semua individual init, `startCheck`, `isValidCron`, `__testHooks.safeSendMessage`) sehingga `index.js`, `routes/admin-config-routes.js`, dan guardrail test `lib/__tests__/cron-whatsapp.test.js` (4/4 PASS) tidak perlu diubah; startup `npm start` stderr 0 bytes (turun dari 5) dengan 8 cron init lines log lengkap.

<a id="b68"></a>
### `lib/security.js` + `lib/__tests__/security.test.js`

- `lib/security.js` + `lib/__tests__/security.test.js`: Hotfix `setInterval(cleanupRateLimits, 600000)` di top-level kini dibungkus dengan `.unref()` agar tidak menahan event loop di test/CLI script — sebelumnya `node -e require('./lib/security')` (atau test jest yang require chain ke security) akan hang ~10 menit menunggu interval pertama; production behavior unchanged karena ada HTTP listener aktif. Guardrail test `security.test.js` patch `global.setInterval` saat module load untuk verify `.unref()` benar-benar dipanggil pada handle yang dibuat.

<a id="b69"></a>
### `routes/tickets.js` + `routes/tickets-shared.js` + `routes/tickets-list-routes.js` + `routes/tickets-workflow-routes.js` + `routes/tickets-photo-routes.js` + `routes/tickets-admin-routes.js` + `routes/tickets-customer-routes.js`

- `routes/tickets.js` + `routes/tickets-shared.js` + `routes/tickets-list-routes.js` + `routes/tickets-workflow-routes.js` + `routes/tickets-photo-routes.js` + `routes/tickets-admin-routes.js` + `routes/tickets-customer-routes.js`: Priority 2A pemecahan monolit `routes/tickets.js` (1773 lines, 69KB) jadi 7 file by-role/by-audience — composer 31 lines, shared 242 lines (helper + middleware + 2 multer storage), list 137 lines (GET tickets/admin tickets), workflow 859 lines (7 endpoint staff write: process/otw/arrived/verify-otp/complete/resolve/create), photo 402 lines (2 endpoint upload), admin 246 lines (admin/ticket/create + cancel khusus admin), customer 23 lines placeholder untuk endpoint customer self-service masa depan; semua path mount asli (`/api/tickets`, `/api/admin/tickets`, `/api/ticket/...`, `/api/admin/ticket/...`) tetap tidak berubah karena composer mount sub-router di root `/`, integration test `tickets-workflow.integration.test.js` (3 tests) lulus tanpa modifikasi sebagai bukti kontrak API tidak berubah.

<a id="b70"></a>
### `lib/olt-ssh-client.js` + `lib/olt-zte-provision.js` + `lib/olt-provision-store.js` + `lib/olt-backup.js` + `lib/cron/jobs/olt-backup.js` + `routes/olt-provisioning.js` + `views/sb-admin/admin-olt-provision.php`

- `lib/olt-ssh-client.js` + `lib/olt-zte-provision.js` + `lib/olt-provision-store.js` + `lib/olt-backup.js` + `lib/cron/jobs/olt-backup.js` + `routes/olt-provisioning.js` + `views/sb-admin/admin-olt-provision.php` (+ `static/js/admin-olt-provision.js`, `static/css/admin-olt-provision.css`): fitur provisioning OLT ZTE C320 via SSH — klien shell interaktif (prompt ZXAN, `--More--`, enable mode, algoritma legacy, lock per-host anti sesi config dobel), service registrasi ONU (scan `show gpon onu uncfg`, okupansi ONU ID dari running-config interface, render template per "tipe modem" dengan validasi anti injeksi CLI: tolak newline/`?`/spasi pada identifier, preview wajib sebelum eksekusi, hasil per-perintah + verifikasi detail-info/pon power, rollback `no onu`), store profil tipe modem di `database/olt_onu_types.json` (seed 3 profil bawaan: router full/router PPPoE+TR069/bridge), dan backup `show running-config` ke `backups/olt/<deviceId>/` dengan retensi + opsi kirim Telegram + cron `config.olt.backup.schedule` (default 02:30 WIB). Router di-mount di `/api/olt` (path `/api/olt/provision/*`) berdampingan dengan `routes/olt.js`; kredensial SSH per device (`sshPort/sshUsername/sshPassword`) disimpan di `config.olt.devices[]` lewat form Konfigurasi → OLT. Akses: operasi provisioning untuk staff (termasuk teknisi), CRUD profil/setting backup/download khusus admin. Halaman `/admin-olt-provision` (menu Jaringan → Provisioning OLT). **DIKALIBRASI & DIVERIFIKASI LIVE di C320 V2.1.0 asli (610 ONU)**: siklus penuh registrasi→status→viewer→rollback lulus; perilaku firmware yang ditangani: keepalive ssh2 wajib OFF (tidak dijawab → koneksi putus saat perintah lama), baris `!` script = `exit` (config-if tidak menerima `int gpon-onu_` langsung), pesan `%Info` bukan error, `show running-config` streaming ~15-20 menit (timeout backup berbasis IDLE 2 menit, pagar 45 menit). Tambahan endpoint: `GET .../facts` (port PON dari `show card`, tipe ONU, profil tcont/traffic, VLAN — dropdown form, cache 10 mnt), `GET .../onu-config` (viewer interface + pon-onu-mng), flag `saveConfig` di register/delete (jalankan `write` — config ZXAN tidak persist reboot tanpa write), profil bawaan ke-4 `zte-router-wanip` (gaya `wan-ip 1 mode pppoe ... vlan-profile`). **Guard pra-eksekusi anti-miss**: preview & register memvalidasi nilai vs fakta OLT (port/tipe ONU/profil/VLAN harus ada) + cek okupansi (ONU ID/SN belum terpakai) → tolak 409 sebelum script jalan, bisa di-`force` bila fakta basi. **Backup 2 metode** (`lib/olt-ftp-receiver.js` + `config.olt.backup.method`): `ftp` (default, ±10 dtk) menyalakan FTP receiver on-demand di host bot lalu `file upload cfg-startup startrun.dat ftp ...` → terima file .dat byte-identik (butuh `ftpSelfHost`=IP bot dari sisi OLT + route OLT→bot); `capture` (lambat) rekam running-config .cfg; fallback ftp→capture opsional. **Hardening live**: `openOltShell` kini retry koneksi berbackoff (transport-only, bukan auth — ZXAN punya proteksi brute-force SSH yang mengunci port 22 bila ditembak beruntun; verif live).

<a id="b71"></a>
### Fitur 2026-06-16 (ACS/TR069 retrofit dari web admin)

- **Fitur 2026-06-16 (ACS/TR069 retrofit dari web admin)** — `lib/olt-zte-provision.js` (+`classifyVendorTier` prefix-SN→tier, `applyTr069Addon`/`applyTr069AddonBulk`/`removeTr069Addon` blok 5-baris ADITIF [service-port mgmtVlan + service TR069 + `tr069-mgmt unlock/acs/tag`], `listAllOnus` enumerasi semua ONU 1 sesi), `routes/olt-provisioning.js` (`GET/POST .../acs-settings` per-OLT → `config.olt.devices[].acs` {url,user,pass,mgmtVlan}, password tak diekspos; `GET .../tr069/status` inventaris × GenieACS × tier → aksi adaptif; `POST .../tr069/apply` **guard ZTEG-only** [clone 409 "set di modem"]; `.../apply-bulk` 1-sesi auto-pilih ZTEG-belum-inform + `write` sekali di akhir; `.../remove` rollback), tab "ACS/TR069" di `admin-olt-provision.php` + `.js` (form setting + tabel status 3-warna + filter/cari + Aktifkan/Lepas + bulk). Verifikasi pakai `lib/genieacs.queryDevices` — **sukses = INFORM di GenieACS, BUKAN command-OK** (ZTE ex-ISP terkunci bisa ber-SN ZTEG tapi tak phone-home → aksi tetap olt-push sampai benar inform). **VERIF LIVE VANS 172.17.1.2 (612 ONU)**: ZTE asli (ZTEG, hw F670L) DUKUNG OLT-push (`tr069-mgmt` + vlan 100 → inform <20 dtk); clone RTEG/ZXIC TIDAK (firmware abaikan OMCI → in-band/set di modem); 477 ZTEG Track-A vs 135 non-ZTE Track-B (RTEG 103/ZICG 14/ZXIC 10/CIOT 1/HWTC 7). ACS = GenieACS 172.17.11.2 (CWMP :7547 utk push, NBI :7557 utk verify = `genieacsBaseUrl`), VLAN 100 (sudah dibuat di OLT + trunk uplink + DHCP Mikrotik), kredensial acs/acs123. Bulk 1-sesi (anti spam-koneksi ZXAN) + `write` di sesi yang sama (hindari deadlock `withHostLock`). 64/64 test OLT (8 route + 4 lib ACS baru); full suite 1216 hijau. **Kalibrasi rollout LIVE (batch 10 ZTEG)**: ~2,9 dtk/ONU + `write` ~11 dtk → bulk DI-PECAH ~50 ONU/batch di UI (`ACS_BULK_BATCH`, tiap batch 1 sesi+write, persist inkremental) bukan 476 sekaligus (≈23 mnt/1 sesi = riskan putus + timeout HTTP). **Inform ~80% batch-pertama**; sisa ZTEG online+config-ada tapi tak inform = **ex-ISP/firmware terkunci** (abaikan OMCI tr069-mgmt) → status tetap "belum inform" (benar, tak palsu-ok) → tangani di modem/swap. Prasyarat skala: pool DHCP VLAN 100 Mikrotik ≥ jumlah ONU ZTE. **Rollout penuh per-port DIJALANKAN LIVE (2026-06-16): apply 462/464 ZTEG OK (~21 mnt), inform 253/477 (53%) & beku. AKAR (terklasifikasi, BUKAN DHCP — pool /21 lega): BATCH FIRMWARE ONU tertentu abaikan OMCI tr069-mgmt — prefix SN ZTEGC7/C5/CA/C0/C9 ~0% inform (~180 unit) vs ZTEGD* 96%. Tail 224 = 141 batch-FW (upgrade/modem-side/swap) + 66 offline (cek fisik) + 17 individual. Bottleneck #1 = batch FW, bukan metode/pool. Daftar teknisi: tmp/staging_inactive_modems.csv (gitignored).** Detail di memori `olt-vans-acs-tr069-rollout`.

<a id="b72"></a>
### Fitur 2026-06-16 (profil tipe modem DIKALIBRASI ulang dari konfig NYATA VANS + auto-pilih anti-salah-klik)

- **Fitur 2026-06-16 (profil tipe modem DIKALIBRASI ulang dari konfig NYATA VANS + auto-pilih anti-salah-klik)** — survei live OLT VANS (612 ONU): **587 F609 (96%) + 25 ALL (4%)**; tak ada yang diregistrasi sbg F670L (F670L/F660 masuk F609/ALL). Pola nyata: **F609 = router penuh OMCI** (`wan-ip mode pppoe` + service INET vlan300 / HS vlan310 / TR069 vlan100 + `tr069-mgmt` ACS + `ssid ctrl wifi_0/2`); **clone (RTEG/ZICG/ZXIC)** = sama TANPA `tr069-mgmt` (ACS di modem); **Huawei (HWTC)** = transport saja (modem dial sendiri). WiFi VANS = **model HOTSPOT/voucher** (wifi_0/2 di-bridge ke VLAN 310, SSID seragam) → SSID via OMCI memang sengaja (terkunci/seragam); offload ke GenieACS hanya bila perlu kelola terpusat. **`lib/olt-provision-store.js` DEFAULT_ONU_TYPES diganti** dari contoh UNNET → **3 profil VANS jelas** (`vans-zte-router`/`vans-clone-router`/`vans-bridge`), tiap profil bawa **`vendorMatch`** (tier vendor yg cocok). **`lib/olt-zte-provision.js` + `vendorTierTable()`** (prefix SN→tier, dikirim ke UI lewat `GET .../onu-types`). UI (`admin-olt-provision.js`/`.php`): scan/ketik SN → **badge vendor + auto-pilih profil rekomendasi** (`classifySn`/`recommendedTypeFor`/`onSnInput`), + peringatan saat profil≠vendor di preview → **teknisi tak bisa salah-klik**. ZTE Router lipat `tr069-mgmt` ke registrasi (1-langkah inform). **VERIF LIVE: siklus zero-footprint di port kosong 1/2/16:1 (SN dummy, TANPA write) — 30/30 perintah template ZTE diterima OLT (`configState=success`), rollback `no onu` bersih (okupansi 0 lagi); ke-3 profil lolos guard fakta OLT.** 97/97 test OLT (4 suite) + lint bersih. Detail data: memori `olt-multibrand-roadmap`.

<a id="b73"></a>
### Revamp UX provisioning (2026-06-16, lanjutan)

- **Revamp UX provisioning (2026-06-16, lanjutan)** — tanggapan feedback live: (1) **form disederhanakan** — buang field Nama ONU & Deskripsi/ODP; nama ONU = `{{pppoeUser}}` (template diubah; konvensi nyata VANS); placeholder contoh UNNET → VANS (`1/2/1`, `home@vans`); autofill pelanggan isi username+password PPPoE. (2) **Push 1-klik** — tombol "Daftar & Push ke OLT": tetap validasi fakta OLT (guard 409) lalu push via SSH dengan SATU konfirmasi ringkas (script disembunyikan default, tanpa checkbox gate). (3) **Browser "ONU Terdaftar per Port"** (kartu full-width gantikan input port+id sempit) — `lib/olt-zte-provision.listPortOnus(device, ponPort, {withNames})` (1 sesi SSH: `show running-config interface gpon-olt_<port>` + opsional `name` per ONU; `parseInterfaceName` murni) + route `GET .../port-onus?ponPort&names` (klasifikasi vendor per baris); UI: pilih port → tabel ONU|SN|Vendor|Nama/PPPoE|**Pelanggan (match `usersData` by pppoe)**|Aksi(Status/Config/Hapus per baris), + pencarian + toggle "baca Nama/PPPoE". **VERIF LIVE port 1/2/7: 5 ONU 6,7 dtk, nama (dengok@anton dst) + tier benar.** (4) **Fix `@import` yatim `/css/modern-ui.css`** di `sb-admin-2.min.css` (file dihapus saat refactor tema → 404 HTML → console MIME error di SEMUA halaman admin) — import dihapus (nol perubahan visual). (5) "cek slot" beri umpan balik di tempat (dekat tombol) + fokus Port PON. **CATATAN: JS/CSS halaman ini belum cache-bust → wajib hard-refresh (Ctrl+F5) setelah deploy.** 1223 test hijau, eslint + php -l bersih.

<a id="b74"></a>
### Fitur 2026-06-15 (CCTV discovery dari netwatch, READ-ONLY)

- **Fitur 2026-06-15 (CCTV discovery dari netwatch, READ-ONLY)** — `views/get_netwatch_full.php` + `lib/mikrotik.js::getNetwatchFull` (ambil netwatch lengkap termasuk `up-script`/`down-script`, on-demand; poller 60s tetap pakai `get_netwatch_list.php` yang ringan) + `lib/cctv-netwatch-discovery.js` (classifier MURNI: parse `:local cctv`/`:local area` dari script → kandidat CCTV "sesuai"; comment `/CCTV|NVR/i` tanpa script standar → "belum standar"; `:local dev`/teks "KONEKSI PUTUS/PULIH" → infra; sisanya noise; hanya klass `cctv` dikembalikan) + `routes/cctv.js GET /api/cctv/discovery` (fetch→classify→`markRegistered` cross-check registry by host = anti tumpang-tindih) + UI `views/sb-admin/cctv-monitor.php` (kartu "Ditemukan di Netwatch", tombol Adopsi prefill modal nama/host/area → admin cukup isi nomor WA). Field `area` opsional ditambah di `lib/cctv-registry.js`. Tujuan: hilangkan input ulang IP/nama yang sudah ada di netwatch. **Tidak menulis ke router**; trigger broadcast tetap poll-based (`lib/cctv-monitor.js` tak diubah — seed/window konfirmasi/anti-flap/dedup tetap). Verifikasi LIVE di MikroTik 192.168.102.100 (27 entri → 15 CCTV [12 sesuai+area, 3 belum standar], 12 dikecualikan infra/noise) + render `/cctv-monitor` (15 kandidat, adopsi prefill area=DANDER, 0 error console) + `lib/__tests__/cctv-netwatch-discovery.test.js` (8 kasus) PASS. **Pengaturan monitor esensial dari halaman CCTV** (lanjutan): `GET/POST /api/cctv/config` di `routes/cctv.js` (persist seksi `cctvMonitor` di `config.json` + update `global.config.cctvMonitor` live + **hot start/stop** `startCctvMonitor`/`stopCctvMonitor` tanpa restart) + helper murni `lib/cctv-monitor-config.js` (`buildCctvConfigPatch`/`toPublicView`, validasi window 1–1440) + kartu "Pengaturan Monitor" (toggle Aktifkan monitor / Window konfirmasi global / Notifikasi pulih) di `views/sb-admin/cctv-monitor.php`; verifikasi LIVE: on→`running:true` (pill ON) + ter-persist `config.json`, off→`running:false`, window invalid→400. **Provisioning netwatch (CCTV baru) + config Telegram**: `lib/cctv-netwatch-script.js` (generator script SATU BARIS — WAJIB karena `routeros_api.class.php write()` memecah nilai pada `\n`; sanitasi anti-injeksi: escape `\`/`"` + buang control char) + `views/add_netwatch.php` + `mikrotik.addNetwatch()` (param/token via env bukan argv; idempotent: host sudah ada → skip, **tak menimpa** = kebijakan buat-baru) + `POST /api/cctv/provision-netwatch` + config `cctvMonitor.netwatch` {botToken,chatId,interval,timeout,msgUp,msgDown} editable di tab Pengaturan + UI checkbox provisioning + picker nomor dari `/api/users` (resp `{data:[]}`, field `phone_number`). **PRASYARAT ROUTER (penting):** user API MikroTik butuh izin **`policy`** (selain read/write) utk netwatch ber-script — tes-live `addNetwatch` gagal `not enough permissions (9)` dgn user saat ini; KODE benar (script 1-baris 457 char terverifikasi, add idempotent, endpoint surface error rapi → CCTV tetap tersimpan, provisioning gagal anggun). Total 41/41 test CCTV hijau (15 monitor + 8 discovery + 11 config + 7 netwatch-script). **Penyempurnaan (2026-06-16):** (1) **GUARD GANGGUAN MASSAL** di `cctv-monitor.js onConfirm` — bila `countDown() >= massOutageThreshold` (config), broadcast pelanggan DITAHAN (incident `mass_suppressed`, tak set active → tak ada notif pulih) + 1 ringkasan ke `massOutageAdminPhone` (cooldown anti-dup); cegah spam ke banyak pelanggan saat PLN/uplink padam. (2) **Riwayat insiden**: `getCctvIncidents()` + `GET /api/cctv/incidents` + tab Riwayat. (3) **Tombol Tes**: `POST /api/cctv/test-broadcast` (kirim WA percobaan via sendCritical, prefix "PESAN TES"). (4) **Badge "tidak di netwatch"**: `pollOnce` set `s.inNetwatch`, getStatus expose, UI tandai. (5) **Durasi since** di tabel daftar. (6) **Adopsi massal**: checkbox + pilih-semua + nomor WA bersama di tab discovery. Config baru `cctvMonitor`: `massOutageThreshold`/`massOutageAdminPhone`/`messageMassOutage` (editable di Pengaturan, validasi 0–1000). Total 45/45 test CCTV (17 monitor + 8 discovery + 13 config + 7 script). Verifikasi LIVE: config save/validasi/test-broadcast/incidents OK, 0 error console. **Temuan akar:** `views/routeros_api.class.php` memotong nilai atribut pada karakter `=` pertama (parser `preg_match_all('/[^=]+/i')` ambil segmen ke-2) → script Telegram netwatch tampil terpotong di `/tool fetch url` (kosmetik; script di router LENGKAP & berfungsi kirim Telegram; classifier discovery TIDAK terpengaruh karena `:local cctv`/`:local area` ada sebelum `=`). SENGAJA tak diperbaiki — parser dipakai semua operasi MikroTik, berisiko. **UI refactor (lanjutan):** halaman CCTV dirombak jadi 3 tab (Daftar CCTV / Ditemukan di Netwatch / Pengaturan) konsisten pola `nav-tabs` sb-admin + CSS via design token (`--white`/`--slate-*`/`--color-*`/`--indigo-*` mode terang, alias `--d-*` + shade terang `--emerald-400`/`--red-400`/dll mode gelap) → aman light & dark (nav-tabs/table/form-control/textarea sudah di-handle admin-theme.css). Tab Pengaturan kini juga mengedit **template pesan default** (`messageDown`/`messageUp`): `buildCctvConfigPatch`/`toPublicView` diperluas (kosong→default efektif), dan `lib/cctv-monitor.js notify()` di-fallback ke DEFAULTS bila template config kosong (cegah broadcast pesan kosong dari `config.example` `""`). 32/32 test CCTV hijau; verifikasi LIVE: render 3 tab + dark mode + textarea template ter-isi default. **Higiene notifikasi + Uptime/SLA (2026-06-16):** (1) **opt-out** per-CCTV `notifyCustomer` (mode pantau-saja, badge "pantau saja"); (2) **jam tenang** (`quietHoursEnabled`/`quietStart`/`quietEnd`) tunda WA pelanggan → sweep di `pollOnce` kirim saat jam tenang berakhir bila masih mati (incident `deferred_quiet`→`deferred_resolved`), helper murni `isWithinQuiet` (window lewat tengah malam); (3) **gabung per-penerima**: `broadcastDown`→`enqueueDown` + flush jendela `aggregateWindowMs` (default 90s, 0=segera) dikelompokkan per nomor → 1 pesan tunggal/gabungan (`messageDownMulti` {count}/{list}), recovery (UP) tetap instan; (4) **uptime/SLA**: `lib/cctv-uptime.js` murni (downtime = irisan [detectedAt,recoveredAt|now], 24h/7d/30d) + `GET /api/cctv/uptime` + kolom "Uptime 7h" (warna+tooltip) + var template `{uptime_24h/7d/30d}`; `closeLatestIncident` rekam `recoveredAt` utk SEMUA insiden saat pulih (akurasi), cap insiden 500→3000. **Fix bug "menit menit"** — `{minutes_down}` SUDAH memuat unit (fmtDuration); template down/up dirombak (emoji per-baris, scannable). Tab Riwayat ditambah (halaman jadi 4 tab). **GOTCHA:** (a) jangan tambah kata 'menit' setelah `{minutes_down}`; (b) template yang tersimpan di config.json **menimpa** default kode → kosongkan field di Pengaturan untuk pakai default terbaru (begitu Simpan Pengaturan diklik, default lama ikut ke-bake — perlu refactor empty-by-default, MASIH ANTRE). 58/58 test CCTV (5 suite: 19 monitor + 8 discovery + 16 config + 7 script + 7 uptime). Tiap fitur commit terpisah & push raf-bot-v3/main. **Koordinator Area/RT (2026-06-16):** registry `lib/cctv-area-registry.js` (`database/cctv-areas.json`: {name, coordinatorName, coordinatorPhone, enabled}; `matchByName` case-insensitive, murni+testable) + CRUD `GET/POST/PUT/DELETE /api/cctv/areas` + tab "Koordinator" + field area CCTV jadi **datalist**. Broadcast **FLAT**: `recipientsFor(device)` = pelanggan (jika notifyCustomer on) + koordinator area (dep `getAreaCoordinator` injectable) → `flushBatch` kelompokkan per nomor (role koordinator menang) → koordinator dapat **1 pesan ringkas per area** (`messageCoordDown` {coordinator_name}/{area}/{count}/{list}). Opt-out pelanggan DIPINDAH dari onConfirm ke recipientsFor (koordinator tetap dinotif). **Nomor WA CCTV OPSIONAL** bila area punya koordinator aktif (registry relax → wajib name+host saja; `requireRecipient` di route POST/PUT /devices: nomor ATAU koordinator; validasi UI selaras). 64/64 test CCTV (3 skenario koordinator). **Keamanan:** `database/cctv-devices.json`/`cctv-areas.json`/`cctv-incidents.json` ditambahkan ke `.gitignore` (berisi nomor WA asli — cegah bocor ke repo publik; sebelumnya pola per-file melewatkannya, belum ter-track). **Tahan-restart (2026-06-16):** `restoreInFlight()` dipanggil sekali setelah seed poll pertama (flag `restored`) — rekonstruksi state in-flight dari `cctv-incidents.json` utk host yang MASIH mati: `pending`→re-arm timer (sisa window dari detectedAt), `deferred_quiet`→pulihkan (sweep kirim saat jam tenang berakhir), `broadcasted`→pulihkan `active`+`lastBroadcast` (notif pulih & cooldown terjaga); insiden terbuka tapi host sudah UP→tutup (recovered). Cegah notif in-flight HILANG saat `pm2 restart`/deploy. Anti-blast aman (hanya pulihkan insiden yang sudah ada, tak bikin baru utk host mati-saat-boot). Tanpa file state baru (pakai log insiden durable). 67/67 test CCTV (3 skenario restart). **Visibilitas koordinator + dropdown area (2026-06-17, commit dea8147→6a8dc49):** field Area di form CCTV jadi `<select>` (anti-typo yg memutus match-by-name) + hint live koordinator ter-link + opsi "➕ tambah area baru" inline (pola SWAP modal, bukan tumpuk — Bootstrap 4 stacked bocor backdrop). **Broadcast ke GRUP WA RT (2026-06-17, commit ec0df39):** target notif per area/RT = nomor koordinator DAN/ATAU grup WA RT. Kunci: `sendCritical`→`ensureJid` ([lib/whatsapp-critical-delivery.js](lib/whatsapp-critical-delivery.js)) sudah meneruskan JID `@g.us` apa adanya → `recipientsFor` tambah penerima grup (role koordinator) bila `coordinatorGroupId`; jam tenang & guard massal otomatis berlaku (gate di `onConfirm` sebelum recipientsFor). Registry +`coordinatorGroupId`/`coordinatorGroupName`, validasi → wajib nomor ATAU grup. `GET /api/cctv/groups` via `gateway.getSocket().groupFetchAllParticipating()` (pola mirror [services/api-network.service.js](services/api-network.service.js); 503 anggun bila WA off). UI: picker grup (select+Muat+hint, bot harus anggota grup dulu). 69/69 test CCTV. **Opsi "warga sudah di grup" (commit 46e765c):** flag opt-in area `customersInGroup` → bila dicentang + ada grup, japri pribadi pelanggan ditiadakan (cukup grup; default OFF agar warga belum-di-grup tak terlewat). Penerima grup role `'group'` + template terpisah `messageGroupDown` (menyapa warga, tanpa nama koordinator), editable di Pengaturan; flushBatch prioritas coordinator>group>customer. 70/70 test CCTV. **Tes broadcast (commit cabd5ad):** `/test-broadcast` dulu cuma ke `d.phone` & 400 bila kosong → kini lewat `runTestBroadcast(device)` yg resolve penerima via `recipientsFor` (pelanggan+koordinator+grup), 400 hanya bila tak ada penerima; + endpoint `POST /api/cctv/areas/:id/test` (tes koordinator/grup area langsung) + tombol tes per-area di UI + hasil tampil per-penerima. 71/71 test CCTV. **Nonaktif nomor koordinator + notif pulih ke grup (commit 6ddef72):** flag area `coordinatorInGroup` (paralel customersInGroup) → bila dicentang + ada grup, `recipientsFor` lewati nomor pribadi koordinator (terpusat ke grup; default off). Notif PULIH dulu cuma ke `device.phone` → fungsi `notify` jadi `broadcastUp` (kirim ke semua penerima via recipientsFor; koordinator/grup pakai template baru `messageGroupUp`). 73/73 test CCTV. **Snooze/maintenance per-CCTV (commit ff7c012):** field `snoozeUntil` (epoch ms) di registry; `pollOnce` tetap lacak status tapi tahan alert selama snooze, rekonsiliasi sekali saat habis (masih mati→alert; pulih→tutup); `POST /api/cctv/devices/:id/snooze {minutes}` (0=batal) + tombol/badge di UI; CCTV snooze tak ikut `countDown` gangguan massal. 74/74 test CCTV. **Jam tenang per-jenis (commit f872a28):** config `quietApply{Customer,Coordinator,Group}` (default true=perilaku lama); keputusan jam tenang dipindah dari onConfirm ke `broadcastDown` per-penerima — `held` disimpan di `s.deferred.recipients`, sisanya kirim langsung, sweep kirim saat jam tenang usai; `s.active.roles` → notif pulih (`broadcastUp(...,roles)`) hanya ke jenis yg dapat "mati". UI 3 ceklis "Berlaku untuk". 77/77 test CCTV. **Jam tenang per-area (commit 50107d4):** area-registry +`quietMode`(inherit/custom/off)+`quietStart`/`quietEnd`; helper `effectiveQuietNow(device,conf)` override global (off→tak pernah; custom→jendela area sendiri; inherit→global) dipakai di broadcastDown+sweep per-device (toggle per-jenis tetap global); UI select 'Jam tenang area'+jendela+badge tabel. 79/79 test CCTV. **FIX notif tak sinkron (commit c86bf92→dd636e1):** bug — CCTV pulih tapi DOWN tetap dikirim utk CCTV yg sudah recover (UP mendahului/lalu DOWN). Pendekatan berlapis: `flushBatch` saring antrean (skip host yg sudah 'up'), `onConfirm` guard (skip broadcast bila status sudah 'up' saat window habis), `onRecovery` tetap kirim UP. Hasil: pulih sebelum DOWN terkirim → UP saja, DOWN di-skip; multi-CCTV hanya yg masih mati dapat DOWN. 82/82 test CCTV. Detail di memori `cctv-monitor`.

<a id="b75"></a>
### Bugfix 2026-06-15 (konfirmasi voucher_sale)

- **Bugfix 2026-06-15 (konfirmasi voucher_sale)** — `message/handlers/agent.js handleAgentConfirmation`: melengkapi guard 2026-06-14. Penjualan voucher (`transactionType === 'voucher_sale'`) sudah FINAL saat dibuat (`agent-voucher-manager.sellVoucherToCustomer` set sale `completed` + tandai kode `sold` + kurangi stok), record transaksi agent hanya artefak ledger `pending`. Kini `konfirmasi AGT_TRX_<voucher_sale> <pin>` di-short-circuit **sebelum** `confirmTransaction` (tak flip status / tak minta PIN) dengan pesan "Penjualan voucher tidak perlu dikonfirmasi" — bukan lagi error palsu "Gagal memproses saldo" dari `processAgentConfirmation` (topup-only, butuh `topupRequestId`). Tambahan guard generik: tipe non-`topup` apa pun yang lolos sampai jalur saldo (mis. `payment`/`voucher` di komentar `agent-transaction-manager` yang belum dipakai, atau tipe baru) dibalas konfirmasi generik, tak menyentuh `processAgentConfirmation`. 2 template key baru (`agent_transaction_voucher_sale_no_confirm`, `agent_transaction_confirmed_generic`); test `message/__tests__/agent-confirmation-transaction-types.test.js` (3 kasus: voucher_sale, non-topup generik, regresi topup) PASS.

<a id="b76"></a>
### Bugfix 2026-06-14 (reboot + voucher urgent)

- **Bugfix 2026-06-14 (reboot + voucher urgent)** — perbaikan akar `@lid` state-key + voucher: (1) `message/handlers/reboot-modem-handler.js` + `raf-intent-dispatch/wifi-intents.js` + `states/other-state-handler.js`: state `CONFIRM_REBOOT` kini di-key dengan JID kanonik `stateSender` (sebelumnya pakai `sender` mentah → konfirmasi `ya` tidak ketemu state untuk pengirim `@lid` → reboot mati total); intent handler kini meneruskan `raf` (resolusi `@lid`) + `await`; `featureScope` (`adminReboot`/`customerReboot`) diteruskan sesuai peran. (2) `message/handlers/agent-voucher-handler.js` + `domains/agent-voucher.domain.js` + `state-domains/agent-voucher.state.js`: seluruh flow purchase/sale agent voucher kini pakai `stateKey = stateSender || sender` untuk get/set/delete state (akar VB1: flow mati setelah langkah pertama untuk agent `@lid`). (3) `message/handlers/agent.js handleAgentConfirmation`: cabang `transactionType === 'voucher_purchase'` skip `processAgentConfirmation` (khusus topup) → hentikan pesan palsu "Gagal memproses saldo" padahal stok sudah diaktifkan `confirmTransaction`. (4) `lib/agent-voucher-manager.js purchaseVoucherAsReseller`: rollback saat generate voucher gagal — refund saldo (pembayaran saldo) + `cancelTransaction` transaksi pending (cash/transfer) agar tidak ada money-loss / transaksi yatim. (5) `routes/api.js` + `routes/api-voucher-routes.js`: endpoint voucher (`/voucher/profiles|generate-send|sent-history|sent-stats`, `/member/send-credentials`) kini dijaga `ensureAuthenticatedStaff` (fail-closed default) — sebelumnya hanya terlindung auth global (staff ATAU customer) sehingga pelanggan bisa men-generate voucher MikroTik & membaca kredensial plaintext. **Konvensi penting**: semua state percakapan WAJIB di-key dengan `stateSender` (JID kanonik), bukan `sender` mentah, karena router membaca state hanya via `stateSender`.

<a id="b77"></a>
### Fitur 2026-06-20 (self-service "Cek Koneksi" pelanggan — Slice 1)

- **Fitur 2026-06-20 (self-service "Cek Koneksi" pelanggan — Slice 1)** — `message/handlers/connection-check-handler.js` + intent `CEK_KONEKSI` di `message/handlers/raf-intent-dispatch/customer-service-intents.js` + keyword di `database/wifi_templates.json` + template `conncheck_*` di `database/response_templates.json`. Flow: pelanggan WA ("internet saya mati"/"cek status internet"/"kenapa internet lemot"/dst) → `handleCekKoneksi` resolve pelanggan (`resolveCustomerBySender`) → status jalur PPPoE real-time via `lib/mikrotik.getActivePPPoEUsers` (membership by `pppoe_username`, di-cache TTL 30s per routerId agar burst tak menghajar MikroTik) → heuristik gangguan area dari fetch yang sama (≥threshold ATAU ≥30% `global.users` ber-pppoe offline berbarengan; `config.outage_area_threshold` default 5) → `offline_since` dari `repositories/auto-outage.repository.getStateByUserId` (opsional, "terakhir online") → ringkas modem/WiFi `lib/wifi.getSSIDInfo(device_id, skipRefresh=true)` best-effort → pesan kondisional (online / offline-area / offline-single / unknown) + langkah berikut. **READ-ONLY** (tak menyentuh saldo/konfig; reboot ONU & auto-tiket = slice berikutnya). Keyword baru anti-hijack: "lapor ..." tetap ke `LAPOR_GANGGUAN_MATI`, "cek wifi"/"cek koneksi" tetap ke `CEK_WIFI` (terverifikasi routing classifier). Semua teks dari `responseTemplates` (fallback aman, editable admin). Test `message/handlers/__tests__/connection-check-handler.test.js` (6 kasus); regresi `message/` 62 suite/172 test hijau.

<a id="b78"></a>
### Fitur 2026-06-20 (Dashboard Kesehatan OLT — web admin, read-only)

- **Fitur 2026-06-20 (Dashboard Kesehatan OLT — web admin, read-only)** — `lib/olt-health-service.js` + route `GET /api/olt/provision/devices/:id/health` (`routes/olt-provisioning.js`, requireStaff, inject `health` ke deps) + tab "Kesehatan" di `views/sb-admin/admin-olt-provision.php` (+ `static/js/admin-olt-provision.js` `loadHealth`/`renderHealth`). Snapshot SATU sesi SSH **read-only** (via `lib/olt-ssh-client` `openOltShell`+`withHostLock`, cache 60s) dari perintah `show` ZXAN C320: `show system-group` (versi/uptime/identitas), `show fan` (suhu lingkungan + ambang High/Critical + RPM kipas), `show processor` (CPU 5s/1m/5m + memori per-slot), `show card` (status slot, flag non-INSERVICE), `show interface gei_…` (uplink fisik up/util/error; nama port diturunkan dari kartu kontrol non-GPON via `deriveUplinkPorts`), `show ip interface brief` + `show vlan summary`. Plus alert turunan (overheat/kartu offline/CPU-mem tinggi/uplink down) dgn ambang `config.olt.healthThresholds`. Tak ada config/write/reboot. **Penyimpanan/PSU belum tersedia** (firmware ZXAN ini tak mengekspos flash/power via CLI — kandidat SNMP). Parser murni di-unit-test thd output OLT VANS ASLI (`lib/__tests__/olt-health-service.test.js`, 22 kasus) + diverifikasi LIVE di VANS 172.17.1.2. Tujuan: pantau OLT (suhu/kartu/uplink) dari web tanpa buka CLI. Detail di memori `olt-multibrand-roadmap`.

<a id="b79"></a>
### Fitur 2026-06-20 (Health OLT via SNMP bebas-lockout + Konsol `show` read-only)

- **Fitur 2026-06-20 (Health OLT via SNMP bebas-lockout + Konsol `show` read-only)** — lanjutan dashboard kesehatan OLT: (a) `lib/olt-snmp-health.js` (`getSnmpHealth`) membaca identitas/uptime, suhu+ambang, kipas RPM, CPU/memori/status per-kartu, dan EMU power/lingkungan via **SNMP enterprise ZTE** (OID terverifikasi LIVE vs CLI: `3902.1082.10.10.2.1.5`=suhu, `.10.1.2.4.1.{4,7,9,11,13,19}`=kartu realType/port/cpu/mem/status/RAM, `.10.10.2.4.11.1.7`=fan RPM, `.10.10.2.5.2`+`.5.3`=EMU 8 input dry-contact [rectifier/AC/baterai]). `lib/olt-health-service.getHealthSnapshot` kini **SNMP-first (bebas risiko lockout SSH) + SSH untuk uplink fisik/L3/VLAN**, di-merge dgn `source` snmp/ssh/snmp+ssh; salah satu sumber boleh gagal. (b) `lib/olt-show-console.js` (`validateShowCommand`/`runShowCommand`) + route `POST /api/olt/provision/devices/:id/show` (requireStaff, audit) + tab "Konsol" di `admin-olt-provision.php` — jalankan SATU perintah `show ...` read-only via SSH (host-lock); **tulis/konfig/reboot, rangkai-perintah (`;|&`), `?`, & output masif (running-config/startup-config) DITOLAK**. **Storage/flash & voltase PSU internal TAK diekspos firmware ZXAN C320 ini** (CLI maupun SNMP — MIB HOST-RESOURCES/ENTITY-SENSOR tak didukung) → UI tandai N/A jujur. Test: `olt-snmp-health` (6 decoder) + `olt-show-console` (guard); regresi OLT 19 suite/301 test hijau. Peta OID lengkap di memori `olt-multibrand-roadmap`.

<a id="b80"></a>
### Fitur 2026-06-22 (Manajemen VLAN OLT via web — config-write ter-guard)

- **Fitur 2026-06-22 (Manajemen VLAN OLT via web — config-write ter-guard)** — `lib/olt-vlan-manager.js` (generator script + guard; sintaks ZXAN C320 diverifikasi LIVE: buat `vlan <id>`+`name`/`description`, hapus `no vlan <id>`, trunk `interface gei_x/y/z`→`switchport vlan <id> tag` / `no switchport vlan <id> tag`) + route di `routes/olt-provisioning.js`: `GET .../vlans` (list, requireStaff), `POST .../vlan/preview` (requireStaff — generate+guard TANPA eksekusi), `POST .../vlan/apply` (requireAdmin — eksekusi + `write` + audit) + tab "VLAN" di `views/sb-admin/admin-olt-provision.php` (list + buat/hapus + trunk add/remove, **preview WAJIB → konfirmasi → apply**). Guard: id 2–4094 (tolak reserved 1), label token-only anti-injeksi, port wajib `gei_x/y/z`; `executeVlanScript` stopOnError+autoConfirm. **Verif LIVE VANS**: siklus VLAN 999 (create→name→desc→trunk gei_1/3/1→remove→delete) sukses, `show vlan 999` benar (port-tagged gei_1/3/1), rollback bersih (`save:false`, nol footprint). **Service-port per-ONU/pelanggan BELUM** (butuh recon per-ONU + maintenance window). Test `olt-vlan-manager` (30 kasus); 20 suite/331 test OLT hijau. **Catatan koneksi: VANS pindah jaringan → SSH `172.17.231.1:222` (telnet :223, snmp :161)**; `config.olt.devices` `olt-vans` diupdate (host+sshPort). `config.json` gitignored → konfig produksi perlu update terpisah (via Konfigurasi → OLT atau edit manual). Detail di memori `olt-multibrand-roadmap`.

<a id="b81"></a>
### Fitur 2026-06-22 (Service-port per-ONU via web — config-write PER-PELANGGAN ter-guard)

- **Fitur 2026-06-22 (Service-port per-ONU via web — config-write PER-PELANGGAN ter-guard)** — `lib/olt-serviceport-manager.js` (generator + guard + parser; sintaks ZXAN diverifikasi LIVE: `interface gpon-onu_<r>/<s>/<p>:<onu>` → `service-port <idx> vport <v> user-vlan <u> vlan <s>` / `no service-port <idx>`; list = parse `show running-config interface gpon-onu_X:Y`) + route `GET .../onu-serviceports?onu=` (list, requireStaff), `POST .../serviceport/preview` (requireStaff), `POST .../serviceport/apply` (requireAdmin, execute + `write` + audit) + kartu "Service-port per ONU" di tab "VLAN" `admin-olt-provision.php` (muat → tabel idx/vport/user-vlan/svlan + tambah + hapus per-baris, **preview WAJIB → konfirmasi**). Guard: ONU wajib `gpon-onu_x/y/z:n` (anti-injeksi), idx 1–32, vport 1–8, vlan 1–4094. **Verif LIVE: LIST read-only di `gpon-onu_1/2/2:33` (home@kantor) → 3 service-port (300/310/100) ter-parse benar.** ADD/DELETE: sintaks recon-confirmed + jalur write sefamili VLAN (yang sudah terbukti live); write pertama oleh operator lewat preview UI — mengubah service-port pelanggan AKTIF dapat memutus layanan, jadi preview+konfirmasi+audit wajib. Test `olt-serviceport-manager` (18); 21 suite/349 test OLT hijau. **Berikutnya (diminta user): monitoring bandwidth per-PON/uplink** (mayoritas read — counter SNMP/CLI). Detail di memori `olt-multibrand-roadmap`.

<a id="b82"></a>
### Fitur 2026-06-22 (Monitoring bandwidth per-PON & uplink — READ-ONLY + histori/grafik)

- **Fitur 2026-06-22 (Monitoring bandwidth per-PON & uplink — READ-ONLY + histori/grafik)** — `lib/olt-bandwidth-service.js`: `getBandwidthSnapshot` (SATU sesi SSH; derive port dari `show card` → kartu GPON `gpon-olt_*`, kartu kontrol `gei_*`; parse rate/util/ONU), `getBandwidthHistory` (ring in-memory per-port ~288 sampel, di-feed tiap snapshot), `parseGponOltInterface` (murni; `show interface gpon-olt_X` → Input/Output rate instan + throughput% + jumlah ONU registered/capacity). Route `GET .../bandwidth` (snapshot+rekam history) & `GET .../bandwidth/history` (requireStaff). Tab "Bandwidth" di `admin-olt-provision.php`: tabel uplink + per-PON (↓in/↑out Mbps, util in/out, ONU reg/cap) + **sparkline SVG** tren-out (dari history) + auto-refresh 60s + highlight PON util≥70%. Uplink reuse `parsePhysInterface` (olt-health-service). **Verif LIVE VANS: 16 PON + 2 uplink, rate/util benar** (gei_1/3/1 ~209 Mbps/20.9%; PON12 102 ONU; PON14 91 ONU/9.2MBps). Histori in-memory (hilang saat restart) → poller cron persisten (pola `olt-rxpower-history`/SQLite) = peningkatan lanjutan bila perlu 24/7. Test `olt-bandwidth-service` (5); 22 suite/354 test OLT hijau. Detail di memori `olt-multibrand-roadmap`.

<a id="b83"></a>
### Perf/Fix 2026-06-22 (health "stuck" + percepat fetch OLT)

- **Perf/Fix 2026-06-22 (health "stuck" + percepat fetch OLT)** — AKAR stuck: `lib/olt-snmp-health.getSnmpHealth` memakai `device.snmpTimeout` (30s) × retries = **~90 dtk dead-wait** saat SNMP UDP 161 tak terjangkau (VANS pindah IP, 161 belum di-forward) sebelum fallback SSH. Fix: (1) SNMP **fast-fail** untuk health interaktif (timeout 3s, 0 retry; abaikan `device.snmpTimeout` yang ditujukan untuk poller background) + cache **"SNMP down" 5 menit** per-host (lewati SNMP setelah gagal → 0 dtk di muat berikutnya). (2) **SWR (stale-while-revalidate)** di `getHealthSnapshot` & `olt-bandwidth-service.getBandwidthSnapshot`: bila ada cache (walau kedaluwarsa) → kembalikan SEKETIKA + refresh di belakang (tak blok UI); `force`/belum-ada-cache → blocking. **Verif LIVE: health 90s → 10,3s (force pertama) → 0ms (cached) → 6,8s (force, SNMP di-skip).** Diagnosa latensi: per-perintah SSH `show` ~250ms (cepat); biaya tetap = SSH open ~5s; **tak ada perintah bulk-PON** (`show interface gpon-olt_X` per-port). **Saran operasional: forward UDP 161 ke VANS → SNMP jalan → health ~2s + bebas-lockout (tak perlu SSH untuk bulk).** 354 test OLT hijau.

<a id="b84"></a>
### Verif "klop" monitor OLT + percepat fetch SNMP 2026-06-22.

- **Verif "klop" monitor OLT + percepat fetch SNMP 2026-06-22.** Cek live data halaman monitor OLT (`routes/olt.js` /onus → driver `lib/olt-drivers/zte.js`, sumber **SNMP**) vs OLT VANS (SSH ground-truth). SNMP UDP 161 kini sudah di-forward di alamat baru (172.17.231.1, terbukti `OK C320 V2.1.0` 55ms). **HASIL KLOP:** (a) online/offline **cocok** (PON1 12/15; ONU offline 1:7/1:9/1:15 = `OffLine` di `show gpon onu state`); (b) rx-power **cocok EKSAK** — decode `signed16(raw)/500−30` vs `show pon power onu-rx` (1:1 −24.81 vs −24.814; 1:2 −27.45 vs −27.448; selisih ≤0.01 dB → drift optik live). Header driver lama tulis rx "PROVISIONAL" padahal **sudah terverifikasi**. **CATATAN klop (bukan bug data):** LOS vs Dying-Gasp dari SNMP steady-state **tak andal** — OLT sendiri hanya lapor `OffLine`; live 118/119 offline `cause=1` (+1 `cause=9`), enum `ZTE_OFFLINE_CAUSE` (ditandai DUGAAN) tak pernah cocok → praktis semua tampil **"Offline" generik (justru KLOP dgn OLT)**, tak ada false-positive. Dying-gasp sejati butuh syslog/trap (event, bukan poll). **PERF:** ukur per-walk → biang lambat = **walk DDM optik rx (`50.12.1.1.10`) ~28 dtk** (47ms/ONU serial; maxRep 20/40/100 sama saja — OLT query DDM live per-ONU). Inventory/status walk cepat (desc/serial 0.6s, phase/reason 1.5s). Subtree-walk **lebih buruk** (tabel 28.1.1 = 24 kolom → 13.75s). GET per-ONU sama lambat (46ms) TAPI OLT **layani SNMP paralel** (26 OID: 1162ms serial → 285ms konkuren, ~4×). **Solusi:** `fetchOpticsConcurrent` di `zte.js` — optik via **GET batch konkuren** (batch 8 ONU, concurrency 12) hanya ONU **online** (offline=no-signal→N/A); inventory tetap walk. **getOltData 33s → 7.9s** (rx coverage 494/496, nilai tetap eksak vs CLI). 354 test OLT hijau.

<a id="b85"></a>
### Bedah ZTE C320 offline-cause + AKTIFKAN LOS broadcast 2026-06-22.

- **Bedah ZTE C320 offline-cause + AKTIFKAN LOS broadcast 2026-06-22.** Lanjutan verif klop: bedah mendalam SNMP+SSH OLT VANS hidup (ZTE punya data native lengkap — beda HIOSO yang butuh syslog). TEMUAN via `show gpon onu detail-info` + dump kolom SNMP `28.2.1`: **col4 `.28.2.1.4` = phase-state LIVE** (3=working, 1=LOS, 4=DyingGasp, 6=OffLine — cocok baris "Phase state" CLI); **col7 `.28.2.1.7` = penyebab offline TERAKHIR** (1=none, 2=LOS, 3=LOSi, 5=SFi, 9=DyingGasp — cocok histori kolom "Cause" CLI); col5/col6 = waktu authpass/offline (⚠️ jam OLT = 2004, belum NTP → absolut tak andal). **AKAR BUG:** driver lama memakai enum DUGAAN di kolom yang SALAH (col3+enum) → `classifyStatus` **tak pernah** mengembalikan `'LOS'` → poller `lib/olt-snmp-los-poller.js` (yang broadcast WA fiber-putus ke pelanggan) **DORMAN** (lihat poller baris 124: trigger hanya saat `status==='LOS'`). Fix `lib/olt-drivers/zte.js`: `classifyStatus(col4, col7)` → status dari col4 (Online/LOS/Dying Gasp/Offline), `lastDownCause` jadi label dari col7; tambah walk `onuStateDetail` (col4); opsi `skipOptics` (poller memakainya → cycle **13s→3.4s**, tak perlu rx optik). Poller kini broadcast LOS **akurat** (DyingGasp=mati listrik & Offline-generik sengaja TIDAK di-broadcast). Event-classifier (`lib/olt-event-classifier.js`) tetap khusus HIOSO (syslog) — isolasi per-brand benar. **Verif LIVE: STATUS {Online:495, Offline:109, Dying Gasp:6, LOS:2}** (LOS/DG kini muncul; sebelumnya selalu "Offline"). Capabilities `losViaSnmp/dyingGaspViaSnmp` kini benar-benar terpenuhi. Docs `olt-zte-c320-snmp-map` checklist verifikasi SELESAI (rx-scale, enum, pppoe-desc). 355 test OLT hijau.

<a id="b86"></a>
### Jam OLT (NTP) + UI Penyebab/Online-Duration 2026-06-22.

- **Jam OLT (NTP) + UI Penyebab/Online-Duration 2026-06-22.** **(1) JAM OLT:** VANS C320 jamnya `Jun 29 2004 UTC` → di-set LIVE via SSH: `clock timezone WIB 7` (config) + `clock set HH:MM:SS <month> <day> <year>` (exec) + `write` (persist ke flash). Kini `Mon Jun 22 2026 WIB`. NTP tetap off (tak ada server terjangkau yang dikonfirmasi). Sintaks terverifikasi via `?` help (mis. `clock set 12:43:32 jun 22 2026`). **(2) UI MONITOR** (`admin-olt.php`/`teknisi-olt.php` + js): kolom **Penyebab** (`last_down_cause`: LOS/LOSi/SFi/DyingGasp, dari data bulk matched) setelah kolom Status + modal **Penyebab / Online-sejak-durasi / Terakhir-down**. **FIX BUG** (dari commit ee9ca06): `zte.getSingleOnuData` masih kirim col3 ke `classifyStatus` (yg kini harap col4) → modal selalu 'Offline'; diperbaiki ke col4 + tambah col5 (`lastUpAt`)/col6 (`lastDownAt`). `routes/olt.js` propagate `last_down_cause` + `last_up_at`/`last_down_at` (matched, oltByMacPrefix, refresh-single). Modal auto-muat waktu via `refresh-single` (silent, SNMP single ~50ms). Durasi dihitung dari `lastUpAt` (WIB); tahun<2025 (timestamp pra-fix-jam) → '—' (self-heal saat ONU reconnect). Verif LIVE: `getSingleOnuData(1/2/1:1)` → `status:'Online'` (bug fixed), rx −24.95; eslint 0 error, php -l bersih, **357 test OLT hijau** (+2 cleanZteTime). ⚠️ UI belum di-render browser (logika + lint terverifikasi).

<a id="b87"></a>
### Bot Telegram teknisi (Fase 1, READ-ONLY) 2026-06-22.

- **Bot Telegram teknisi (Fase 1, READ-ONLY) 2026-06-22.** Channel diagnosa untuk teknisi via Telegram (pelanggan tetap WhatsApp; Telegram dipilih karena Bot API resmi/stabil & auth via chat_id). Flow: `lib/telegram/telegram-teknisi-bootstrap.js` (long-poll getUpdates, single-consumer cocok dgn invariant single-instance; start dari `lib/app-runtime.js:initializeBackgroundServices`, stop dari `lib/process-lifecycle.js` SIGTERM/SIGINT; guard anti loop-ganda) → `lib/telegram/telegram-intent-dispatch.js` (parse `/cmd`, AUTH GATE whitelist via `repositories/telegram-teknisi.repository.js`, private-chat only) → handler `message/telegram/command-handlers/*` (`/cek` diagnosa lengkap one-shot, `/redaman` dua arah, `/koneksi`, `/modem`, `/olt`, `/pelanggan`, `/terakhir`, `/help`) → service existing (`lib/wifi.js:getCustomerRedaman` force-refresh + `getSSIDInfo`/`getDeviceCoreInfo`, `lib/mikrotik.js:getActivePPPoEUsers`, resolver optik). Client HTTP `lib/telegram/telegram-teknisi-client.js` (axios; `safeSendMessage` never-throw; token diparameterkan, beda dari telegram-backup). Panel admin `routes/admin-telegram-teknisi-routes.js` + `views/sb-admin/telegram-teknisi.php` (kelola whitelist + config + restart bot; privilege admin/owner/superadmin; token tak pernah dikirim ke klien). Config `config.telegramTeknisi` (`enabled`/`botToken`/`pollTimeoutSec`). **Owner baru `lib/olt-optical-resolver.js`** — resolusi pelanggan→ONU optik (`buildOnuIndex`/`matchOnu` prioritas PPPoE→serial→MAC, `resolveByCustomer`, snapshot OLT ber-cache anti spam SNMP); `routes/olt.js` GET /matched kini mendelegasikan inti matching ke resolver ini (write-back MAC cache & cabang offline tetap di route — perilaku identik, 372 test OLT hijau). **UX tombol/list:** tiap hasil & kartu pelanggan membawa inline keyboard aksi (`callback_data` `do:<cmd>:<userId>`) untuk drill-down 1-ketuk antar diagnosa, dan pencarian ambigu menampilkan kandidat sebagai tombol (mempertahankan niat); dispatcher menangani `callback_query` dgn auth-gate whitelist yang sama + `answerCallbackQuery` (tetap READ-ONLY). Flagship `/cek` (=`/diagnosa`) menyatukan PPPoE + gangguan-area + redaman modem + OLT → vonis 🟢/🟡/🔴 + penyebab + saran (mesin murni `message/telegram/command-handlers/diagnosis-verdict.js`); `/terakhir` = recents per-chat in-memory (`recents.js`); teknisi cukup ketik nama tanpa slash → dispatcher reroute ke `/pelanggan`; callback `go:<cmd>` untuk aksi tanpa pelanggan (mis. menu). READ-ONLY: belum ada aksi tulis jaringan (reboot ONU/restart PPPoE = fase berikutnya, akan kena `raf-invariants`). Teks bot di-hardcode (bukan WA customer-facing → template tidak diwajibkan). Full suite 268 suite/1469 test hijau.

<a id="b88"></a>
### Fitur 2026-06-23 (Pemisahan akun infrastruktur dari data pelanggan)

- **Fitur 2026-06-23 (Pemisahan akun infrastruktur dari data pelanggan)** — kolom baru `users.account_type` (`'pelanggan'` default | `'infrastruktur'`, mis. modem PPPoE titik CCTV/monitoring) + migrasi v5 (`lib/database-migration-manager.js` `getExpectedVersion()→5`, addColumn idempotent, back-fill `'pelanggan'`) + `lib/database.js` (CREATE TABLE + normalisasi load). Sumber kebenaran klasifikasi = helper murni `lib/account-classification.js` (`getAccountType`/`isInfrastructure`/`isBillableCustomer`/`partitionAccounts`). Flag mengalir lewat CRUD/Excel (`repositories/api-users.repository.js`, `services/api-users/{create-user-validate,update-user-by-id,users-excel-schema,import-users-excel,export-users-excel}.js`). **Akun tetap di tabel `users`** agar korelasi monitoring (`pppoe_username`/`device_id`/MAC) utuh — yang berubah hanya: (a) **disembunyikan dari sisi pelanggan** — tab "Infrastruktur/CCTV" + badge INFRA di `views/sb-admin/users.php`/`static/js/users.js`, dikecualikan dari statistik (`routes/stats.js` `payableUsers`), tunggakan (`repositories/arrears.repository.js` SQL `account_type != 'infrastruktur'`), dan export pelanggan; (b) **kebal billing** — 4 cron (`lib/cron/jobs/{set-unpaid,isolir,reminder,isolir-notification}.js`) melewati infra via `isInfrastructure`; (c) **halaman pantau sendiri** `/infra-monitor` (`routes/pages.js` + `views/sb-admin/infra-monitor.php` + `static/js/infra-monitor.js`) via endpoint khusus **`GET /api/olt/infra-status`** (`routes/olt.js`): berangkat dari SEMUA akun infra (tak pernah skip), **PPPoE online/offline MikroTik = sinyal utama** + enrichment redaman/LOS OLT best-effort dengan **timeout race 8s** (anti-hang saat satu OLT lambat/timeout 60s; walk OLT tetap jalan di background menghangatkan cache). Penyusun baris = helper murni `lib/infra-status.js` (`classifyInfraStatus` prioritas pppoeOnline>los>dying>offline, `buildInfraRow`). UI: pencarian + filter status + tag `·non-OLT` jujur. Entri menu Jaringan di `_navbar.php`; (d) **badge INFRA** di dashboard OLT (`routes/olt.js` sertakan `account_type` di `/matched` & `/onus`; `static/js/{admin-olt,teknisi-olt}.js`); (e) **alert gangguan dialihkan ke teknisi** — `services/auto-outage-rule.service.js matchRuleTarget` mengecualikan infra dari broadcast pelanggan (tetap dilacak online/offline untuk halaman infra), dan `lib/olt-los-broadcaster.js` melewati notif "fiber putus" ke akun infra (status insiden `skipped_infrastructure`) sementara broadcast LOS ke teknisi tetap jalan + baris diberi tag `[INFRA/CCTV]`. Verifikasi: tes baru `lib/__tests__/account-classification.test.js` + kasus infra di `set-unpaid`/`auto-outage-rule`/`olt-los-broadcaster`; migrasi v5 smoke (kolom + back-fill `'pelanggan'`); php -l + eslint bersih. Detail di memori `infra-account-separation`.

<a id="b89"></a>
### Fitur 2026-06-23 (Role "agen" — penagih pembayaran berbasis fee)

- **Fitur 2026-06-23 (Role "agen" — penagih pembayaran berbasis fee)** — role staf baru `agen` yang menagih pembayaran pelanggan yang **ditugaskan** kepadanya dan mendapat **fee flat terpisah dari teknisi**, lewat panel web `/agen-pembayaran`. Alur **menumpang rantai request→approve→komisi teknisi yang sudah ada** (bukan flow paralel), dengan jalur komisi agen sendiri agar pembukuan terpisah. Owner & wiring: (a) **`lib/agen-collection-settlement.js`** (baru) — ledger `agen_collection_ledger` di DB utama (`global.db`, idempotent via `event_key` unik, mirror `technician-collection-settlement.js`), `evaluateAgenCollectionSettlement` (credit saat lunas final, debit saat revert), config `agenCollectionCommissionEnabled`/`agenCollectionCommissionAmount` (editable di Konfigurasi `routes/admin-config-routes.js` + `config.php`/`config.js`). (b) **`lib/payment-finance-service.js` `applyPaymentStatusChange`** kini menerima `agenId`/`agenName` dan memanggil settlement agen berdampingan dengan teknisi (kolektor = teknisi ATAU agen, tak pernah keduanya — jalur teknisi tetap utuh). (c) **`lib/financial-ledger.js`** — domain `agen_collection_commission` (`syncAgenCollectionEntries`, `shouldRun('agen_collection')`, diklasifikasikan `internal` seperti komisi teknisi). (d) **`routes/requests.js` + `services/payment-approval.service.js`** — request membawa `collector_role:'agen'` + `requested_by_agen_id`; `POST /api/requests` mengizinkan role `agen` (di-scope ke pelanggan ber-`assigned_agen_id` = id agen, else 403), `GET` & `cancel` di-filter milik agen, approve tunggal & bulk meneruskan `agenId` ke `applyPaymentStatusChange`. Notifikasi teknisi (`lib/approval-logic.js`) aman no-op untuk request agen. (e) **`routes/agen.js`** (baru, mount `/api/agen` di `lib/routes-registry.js`) — `GET /customers` (pelanggan assigned milik agen), `/summary` (fee agen), `/report` + `/list` + `/assignments` + `POST /assign` (admin). (f) **Assignment**: kolom `users.assigned_agen_id` (migrasi v6 `lib/database-migration-manager.js` `getExpectedVersion()→6` + `CREATE TABLE` di `lib/database.js`); penulisan via `POST /api/agen/assign`. (g) **Halaman**: `/agen-pembayaran` (agen+admin) + admin `/penugasan-agen` & `/laporan-agen` (`routes/pages.js` `checkRole`), navbar `_navbar.php` (`$isAgenRole` → menu ramping agen, sembunyikan menu admin; link admin Penugasan/Laporan Agen di grup Pembayaran), redirect login agen → `/agen-pembayaran` (`routes/public.js`). (h) **Role plumbing**: `routes/accounts.js` + `scripts/create-admin.js` + `accounts.php` menerima role `agen`. **Scope v1**: akru fee + laporan (payout/payroll agen belum, assignment 1-agen-per-pelanggan, tanpa perintah bot WA). Test `lib/__tests__/agen-collection-settlement.test.js` (5 kasus) + regresi payment/approval 18 test hijau.

<a id="b90"></a>
### Fitur 2026-06-29 (Lifecycle penagihan bertingkat — tahap "masa tenggang")

- **Fitur 2026-06-29 (Lifecycle penagihan bertingkat — tahap "masa tenggang")** — cron baru `lib/cron/jobs/grace-reminder.js` (`initGraceReminderTask`) menyisipkan tahap **peringatan masa tenggang** di antara jatuh tempo (`tanggal_batas_bayar`) dan isolir (`tanggal_isolir`): kirim 1x WA ke pelanggan unpaid (filter sama dgn `reminder.js`: skip paid/whitelist/infrastruktur) bahwa pembayaran lewat tempo & layanan dinonaktifkan bila tak bayar sebelum tanggal isolir — **TANPA menyentuh MikroTik** (murni notifikasi, beda dgn `isolir.js`). Template `masa_tenggang_reminder` (`database/message_templates.json`, slot `jatuh_tempo`/`tanggal_isolir`/`harga`/`link_bayar`/`rekening`) + link bayar mandiri `buildBillPayUrl`. Didaftarkan di composer `lib/cron.js` (`initGraceReminderTask`). Config-driven: key `schedule_masa_tenggang` (default `0 8 11 * *`) + `status_masa_tenggang` (**default ON / opt-out**, beda dgn task lain yang opt-in — supaya jalan pasca-deploy tanpa wajib edit cron.json), keduanya di-whitelist `routes/admin-config-routes.js` (`CRON_SCHEDULE_FIELDS`/`CRON_BOOLEAN_FIELDS`) + UI form `views/sb-admin/cron.php` (load+save). Timeline lengkap: tgl 1 set-unpaid → tgl 2 reminder (jatuh tempo tgl 10) → **tgl 11 masa tenggang** → tgl 16 isolir action + isolir-notification. Test `lib/cron/__tests__/grace-reminder.test.js` (3); 4 suite/15 test cron hijau; php -l + eslint bersih.

<a id="b91"></a>
### Fitur 2026-06-29 (Bayar tagihan mode HOSTED iPaymu — opsi halaman gateway, bukan portal sendiri)

- **Fitur 2026-06-29 (Bayar tagihan mode HOSTED iPaymu — opsi halaman gateway, bukan portal sendiri)** — flag config `billPaymentHosted` memilih tampilan halaman `GET /bayar/:token`: (a) **portal** (default, mis. DANDER) = `static/bill-payment.html` multi-channel sendiri (tak berubah); (b) **hosted** (mis. VANS) = buat sesi iPaymu lalu **302 redirect** ke halaman iPaymu (iPaymu yang menyajikan pilih channel + bayar). Owner & wiring: `lib/ipaymu.js` `payHosted(props)` baru → endpoint **`/payment`** (beda dari `/payment/direct`; kirim product/qty/price + returnUrl/cancelUrl/notifyUrl, balikan `Data.Url`+`SessionID`). `routes/bill-payment.js` branch `GET /bayar/:token` (hosted: `resolveBillContext` → `payHosted` → `addPayment` tag 'tagihan' **trxId NULL** + `{hosted:true, sessionId}` → redirect; non-charge state pakai `statusPage()` HTML minimal) + landing `GET /bayar-status` (returnUrl, info ringan; settlement tetap via callback S2S). `lib/bill-pay-token.js` export `resolveBaseUrl` (untuk return/cancel URL). **Callback `routes/public.js`** (`POST /callback/payment`): verify pakai `effectiveTrxId = pay.trxId || req.body.trx_id || req.body.sid` — di hosted TransactionId baru ada di payload callback; **tetap AMAN** karena diverifikasi server-to-server + cross-check `referenceId` & `amount` vs record kita (tak berubah). `lib/http-auth-bootstrap.js` PUBLIC_PATHS tambah `/bayar-status`. Token + settlement (`settleTagihanPayment`) + struk WA dipakai ulang sepenuhnya — hanya "halaman" yang beda. Test `lib/__tests__/ipaymu.test.js` (payHosted ×6) + `routes/__tests__/bill-payment-hosted.test.js` (6) + update `payment-callback-tagihan.test.js` (fallback trxId); eslint bersih. Aktivasi per-instance via `config.json` `billPaymentHosted:true` (butuh kredensial iPaymu instance tsb).

<a id="b92"></a>
### Fitur 2026-06-30 (Multi-payment-gateway — Tripay sbg adapter ke-2, alur bayar tagihan)

- **Fitur 2026-06-30 (Multi-payment-gateway — Tripay sbg adapter ke-2, alur bayar tagihan)** — sistem pembayaran kini gateway-agnostic via config `paymentGateway` ('ipaymu' default | 'tripay'). Owner & wiring: `lib/payment-gateways.js` (selector: `getActiveName`/`getActive`, interface seragam `chargeRedirect(props)→{url,reference,gateway}` + `verify(ref)`; adapter iPaymu bungkus `payHosted`, adapter Tripay bungkus `createCharge` method default QRIS / `config.tripayDefaultMethod`). `lib/tripay.js` (klien Tripay API v2: `createCharge` POST /transaction/create signature HMAC(merchantCode+merchantRef+amount, privateKey)→checkout_url auto-settle, `getChannels`, `checkTransaction` GET /transaction/detail, `verifyCallbackSignature` HMAC raw-body). `routes/bill-payment.js`: GET /bayar/:token kini `useRedirectFlow()` (tripay SELALU redirect, iPaymu redirect bila `billPaymentHosted`, else portal HTML) lewat `gateways.getActive().chargeRedirect()`; record `addPayment` bawa `gateway` + `reference` (Tripay punya saat creation, iPaymu hosted null). **Callback Tripay `POST /callback/tripay`** (terpisah dari `/callback/payment` iPaymu): keamanan = MODEL SAMA dgn iPaymu — VERIFIKASI S2S `tripay.checkTransaction` + cross-check merchant_ref & amount sebelum settle (TIDAK bergantung signature → tak butuh raw-body / edit body-parser global yg bentrok dgn index.js prod), status non-PAID & record non-tripay di-ACK tanpa kredit, idempotent, reuse `settleTagihanPayment` + struk WA. PUBLIC_PATHS += `/callback/tripay`. Voucher/topup MASIH iPaymu-only (Fase 3). Aktivasi: config `paymentGateway:'tripay'` + `tripayApiKey/tripayPrivateKey/tripayMerchantCode` (+set Callback URL `/callback/tripay` di dashboard Tripay). Test: `lib/__tests__/tripay.test.js` (15) + `lib/__tests__/payment-gateways.test.js` (5) + `routes/__tests__/bill-payment-tripay.test.js` (9) + update bill-payment-hosted; eslint bersih, 89 test gateway hijau. **Bonus: Tripay auto-settle → solusi masalah iPaymu "Unsettled/klarifikasi" (akun iPaymu menahan dana).**

<a id="b93"></a>
### Fitur 2026-07-04 (Broadcast Tagihan — kirim pesan pembayaran ke pelanggan terpilih)

- **Fitur 2026-07-04 (Broadcast Tagihan — kirim pesan pembayaran ke pelanggan terpilih)** — halaman admin khusus `/broadcast-tagihan` untuk blast tagihan manual + terarah (bukan reminder-cron otomatis ke semua unpaid). Owner endpoint baru **`routes/admin-broadcast-tagihan-routes.js`** (didaftarkan di composer `routes/admin-router.js`): `GET /api/broadcast-tagihan/customers?status=unpaid|all` (daftar pelanggan + nominal + status HP, skip infra via `lib/account-classification.isInfrastructure`, sumber `global.users`/`global.packages`), `GET /api/broadcast-tagihan/default-template` (pra-isi editor dari key template, hindari duplikasi teks di FE), `POST /api/broadcast-tagihan/preview` (render 1 pelanggan sampel). **Pengiriman REUSE `POST /api/broadcast`** (`routes/admin-content-routes.js` → `services/admin-broadcast.service.js` `queueBroadcast`) — TANPA jalur kirim WA baru (throttle + opt-in + riwayat `broadcast_history` dipakai ulang). CORE: `formatBroadcastMessage` (`services/admin-broadcast.service.js`) kini menambah placeholder pembayaran **lazy** — `${link_bayar}` (`lib/bill-pay-token.buildBillPayUrl`), `${harga}` (`global.packages` + `rupiah-format`), `${jatuh_tempo}` (`config.tanggal_batas_bayar`), `${periode}`, dan (2026-07-09, diselaraskan dgn jalur cron `unpaid_reminder`) `${rekening}` (`lib/templating.formatBankAccounts` ← `config.bankAccounts`), `${nama_wifi}` (`config.nama`), `${nama_bot}` (`config.namabot`) — additive, hanya dihitung bila template memuatnya (broadcast GAMAS lain tak terpengaruh). FE kirim `force_include_opt_out:true` (tagihan = komunikasi esensial, abaikan opt-out info-gangguan `notify_outage`); pelanggan tanpa HP otomatis ter-skip mesin (`missing_phone_number`) + ditandai di UI. Template `broadcast_tagihan` (`database/response_templates.json`, category `broadcast`) + fallback identik di route — default 2026-07-09 diubah ke **"transfer manual saja"** (rekening `${rekening}`, tanpa link online) selaras template cron, dipakai selama gateway iPaymu belum diverifikasi. Halaman: `routes/pages.js` (`checkRole` admin/owner/superadmin) + `views/sb-admin/broadcast-tagihan.php` + `static/js/broadcast-tagihan.js` + menu grup Pembayaran `_navbar.php`. Invariant (`raf-invariants`): teks user-facing via template + fallback, kirim lewat delivery boundary (no raw Baileys/`sendMessage`), JID kanonik ditangani mesin. Test: `services/__tests__/admin-broadcast.service.test.js` (+5 kasus placeholder pembayaran/rekening).

<a id="b94"></a>
### Fitur 2026-07-04 (Intake PSB via Grup WhatsApp — Fase 1 dari rencana simplifikasi PSB)

- **Fitur 2026-07-04 (Intake PSB via Grup WhatsApp — Fase 1 dari rencana simplifikasi PSB)** — teknisi cukup kirim SATU pesan di grup PSB terpilih: **foto KTP + caption** diawali `#PSB` (baris `Nama/Paket/WiFi/Sandi/HP`) → bot auto-buat pelanggan + secret PPPoE MikroTik + kirim welcome ke pelanggan + balas kredensial PPPoE ke grup (teknisi setel modem). Owner handler **`message/handlers/psb-group-intake.js`** (+ parser murni **`message/handlers/psb-caption-parser.js`**): parse caption (alias key case-insensitive `wifi|ssid`, `sandi|password`, `no hp|nomor`; paket dicocokkan `global.packages` by name/profile + fuzzy ringan; validasi wajib-terisi + HP 9–15 digit + sandi WiFi ≥8), simpan KTP `uploads/psb/YEAR/MONTH/<tempId>/ktp_photo.jpg` (best-effort via `lib/whatsapp.adapter.downloadMedia`), auto-generate `pppoe_username` (dari nama, dedup vs `global.users`) + `pppoe_password`, lalu **REUSE `services/api-users` `upsertUserFromAdminPanel({registration_mode:"new"})`** (create + secret PPPoE + welcome `psb_welcome`) — TANPA jalur create baru. Hook router di **`message/raf.js`** cabang `if (isGroup)`: SCOPED KETAT (`config.psbIntake.enabled===true && from===config.psbIntake.groupId` DAN pengirim = akun ber-`allowedRoles` teknisi/admin/owner via `resolveAuthorizedStaff`) + NON-THROWING (try-catch; SEMUA pesan grup tetap `return`; ditaruh SEBELUM cek `chats===undefined` agar pesan GAMBAR/caption terjangkau). Handler memakai `global.__apiUsersService` (di-ekspos di `routes/api-users-routes.js` setelah service dibangun — hindari duplikasi ~24 deps create). **Pemilih grup**: pane "Intake PSB Grup" (`views/sb-admin/config.php` + `static/js/config.js`) — toggle `psbIntakeEnabled` + dropdown grup diisi dari **`GET /api/whatsapp/groups`** (owner `routes/admin-config-routes.js` → **`lib/whatsapp.adapter.getGroups`** = `conn.groupFetchAllParticipating`); simpan `psbIntake` nested via `POST /api/config` (`allowedRoles` dipertahankan saat merge, pola `welcomeMessage`/`company`). **Default `config.psbIntake.enabled=false`** (deploy GELAP — aktifkan setelah pilih grup + uji dgn 1 teknisi). Invariant (`raf-invariants`): balas grup lewat delivery boundary (`sendReply` `message/handlers/reply-runtime.js`, BUKAN Baileys mentah), welcome pelanggan via template, JID kanonik (`getOptionalJid`), concurrency guard `isProcessing` per pengirim (`lib/state-manager.js`). Fase 2 (auto-set WiFi ke modem via GenieACS PPPoE-match) menyusul. Test: `message/handlers/__tests__/psb-caption-parser.test.js` (4) + `psb-group-intake.test.js` (4) — hijau; router `raf-router.test.js` tetap hijau (cabang grup tak mengganggu routing DM).

<a id="b95"></a>
### Fix 2026-07-04 (GenieACS waktu registrasi: baca `_registered` root, BUKAN `Events.Registered`)

- **Fix 2026-07-04 (GenieACS waktu registrasi: baca `_registered` root, BUKAN `Events.Registered`)** — akar bug "modem baru tidak terdeteksi" pada `filterType:'new'` (PSB device picker web + rencana auto-detect grup). Diverifikasi LANGSUNG ke ACS DANDER (`http://172.17.11.2:7557`, 157 device): `Events.Registered` **KOSONG** pada mayoritas ONU Huawei HG8145V5 / ZTE Fxxx, sedangkan field root **`_registered`** (Date) SELALU terisi + bisa di-`sort`/`query` server-side (`{"_registered":{"$gte":ISO}}`). Sebelumnya `lib/psb-genieacs-filter.js getRegisteredInfo` + `lib/genieacs.js extractRegisteredDate` hanya baca `Events.Registered` → `registeredTimestamp` selalu null → filter `new` senyap mengembalikan 0. Fix: kedua fungsi prioritaskan `device._registered` (fallback Events.Registered → _lastInform), plus `_registered` DITAMBAHKAN ke projection `listPsbDevices` (`lib/psb-genieacs-service.js`) & `getPsbDevice` (`lib/genieacs.js`). `by-pppoe`/`serialNumber`/`default(tes@hw)` filter TAK terpengaruh (baca `currentPPPUsername` yang memang terisi — makanya "aman"). Solusi robust deteksi modem baru = query `_registered` recency server-side. Test: `lib/__tests__/psb-genieacs-filter.test.js` (+kasus prioritas `_registered`).

<a id="b96"></a>
### Semantik pesan selamat datang (JANGAN keliru): `${username}`/`${password}` = login PORTAL PELANGGAN, BUKAN PPPoE.

- **Semantik pesan selamat datang (JANGAN keliru): `${username}`/`${password}` = login PORTAL PELANGGAN, BUKAN PPPoE.** Portal pelanggan = project TERPISAH (dorman). Di `services/api-users/create-user-persist.js` welcome mode-aware: mode `new`+wifi → `psb_welcome` (tampil **WiFi SSID/sandi**, TANPA login), mode `import` → `import_welcome` (password-less), else default → `customer_welcome` (tampil `${username}`=`newUser.username` + `${password}`=`plainTextPassword` = **kredensial portal**). "Sembunyikan username/password portal" = admin edit template `customer_welcome` via `/api/templates` (semua teks template-driven). PPPoE (MikroTik `WANPPPConnection`) TAK PERNAH muncul di welcome. `services/api-users/send-welcome.js` (resend) pakai `import_welcome` — sengaja password-less.

<a id="b97"></a>
### Fitur 2026-07-04 (PSB Fase 2 — wizard PSB via DM teknisi + auto-provision modem, gate konfirmasi)

- **Fitur 2026-07-04 (PSB Fase 2 — wizard PSB via DM teknisi + auto-provision modem, gate konfirmasi)** — pivot dari intake-grup (Fase 1) ke **DM japri bot area** (1 grup PSB tapi 2 bot/area → input via DM ke bot area = nol ambiguitas; grup dipakai untuk RINGKASAN hasil). Teknisi DM `#PSB` + foto KTP → wizard berstate: kumpulkan **foto rumah + share lokasi (WAJIB)** → bot BACA modem terbaru dari GenieACS (recency `_registered`, lihat [[genieacs-registered-field]]) → tampilkan **SN** untuk dicocokkan teknisi (**YA**/**TIDAK**→daftar bernomor pilih angka/**REFRESH**/**BATAL**) — teknisi CUMA cek, tak ketik SN. **HANYA setelah YA**: buat pelanggan + secret PPPoE + **push PPPoE+WiFi ke `device_id` modem** via `upsertUserFromAdminPanel({registration_mode:"new", device_id, ssid_indices})` (mesin `create-user-mikrotik-sync` yg push WAN PPPoE + WiFi ke modem) + welcome + ringkasan ke grup. BATAL/timeout 15mnt = NOL efek (tak ada write sampai YA). Owner: **`message/handlers/state-domains/psb.state.js`** (`startPsbSession` trigger + `handlePsbConversationState` steps `PSB_COLLECT_DOCS`/`PSB_CONFIRM_MODEM`/`PSB_PICK_MODEM`), didaftarkan via prefix `PSB_` di `conversation-state-owner-map.js` + dispatch `conversation-state-router.js` (owner "psb"). Deteksi modem: **`lib/psb-genieacs-service.findRecentPsbCandidates({windowMinutes})`** (query server-side `{_registered:{$gte:cutoff}}` → urut terbaru). Trigger di `message/raf.js` jalur DM (setelah routeConversationState): teknisi (via `resolveAuthorizedStaff`) + `config.psbIntake.enabled` + `#PSB` image → `startPsbSession`; SCOPED + NON-THROWING. Config `psbIntake`: `enabled`, `groupId` (kini = grup RINGKASAN, bukan input), `recencyWindowMinutes` (default 120), `allowedRoles`; UI pane "Intake PSB" (`config.php`). Ringkasan grup lewat delivery boundary `reply-runtime.sendReply` (patuh invariant, BUKAN socket mentah). Fase 1 group-intake (`psb-group-intake.js`) tetap ada tapi tak dipakai (jalur DM menggantikan). Test: `psb.state.test.js` (5: happy YA→provision+summary, dokumen-wajib gate, TIDAK→pilih-nomor, BATAL, caption ngawur) + `psb-genieacs-filter` (registrasi). Deploy GELAP (`enabled:false`).

<a id="b98"></a>
### Fix 2026-07-09 (PSB risk hardening — welcome jujur + anti-orphan Fase 3)

- **Fix 2026-07-09 (PSB risk hardening — welcome jujur + anti-orphan Fase 3)** — (1) **Welcome jujur (jalur create-user, mode `new`: form tambah-pelanggan web + wizard/grup WA)**: `services/api-users/create-user-mikrotik-sync.js` kini mengembalikan `{ mikrotikSync, deviceConfig }` — `deviceConfig {attempted,ok,message}` = hasil push WiFi/PPPoE ke modem via GenieACS (dulu kegagalan ditelan jadi log `[USER_CREATE_MODE_NEW_ERROR]` & respons tetap `sync_status:"applied"` = status PPPoE, bukan WiFi). `create-user.js` menyalurkan `deviceConfig` ke `create-user-persist.js` yang **MENAHAN** welcome `psb_welcome` bila `attempted && !ok` (jangan janjikan WiFi aktif saat modem belum terkonfigurasi) + menaruh `device_config` + `warning:"device_config_failed"` di respons agar teknisi/admin set modem manual. (2) **Anti-orphan Fase 3 (`services/api-psb.service.js submitPhase3`)**: `movePSBToUsers` kini dipanggil **SEBELUM** record ditandai `completed` (dulu sebaliknya → kegagalan mengunci record `completed` tanpa baris `users` & tak bisa retry lewat UI); catch mengeksekusi rollback `removePPPoESecret` (di-import dari `lib/mikrotik`, di-wire di `routes/api-psb-routes.js`) **HANYA** saat user belum terbuat (bila user sudah terbuat, secret dipertahankan); welcome pelanggan (`sendPSBPhase2Notification`) DITAHAN bila push GenieACS gagal. Keputusan produk: **"Tahan + beri tahu teknisi"** (tanpa read-back modem). Invariant WA/template terjaga (welcome tetap via template + delivery boundary). Test: `services/api-users/__tests__/create-user-persist.test.js` (+3), `services/__tests__/api-psb.service.test.js` (+3: urutan anti-orphan, welcome ditahan, rollback secret).

<a id="b99"></a>
### Fitur 2026-07-05 (Notif grup perbaikan + perintah `tutorial teknisi`)

- **Fitur 2026-07-05 (Notif grup perbaikan + perintah `tutorial teknisi`)** — (1) **Papan pengumuman perbaikan**: `lib/repair-group-notifier.js` (`notifyRepairGroupNewTicket`/`notifyRepairGroupCompleted`, guarded + never-throw, kirim lewat delivery boundary `whatsapp-delivery-service`) auto-posting ke GRUP PERBAIKAN TERPISAH saat **tiket baru** (hook di `lib/report-notification-service.notifyNewReport` — sentral, cover semua jalur create) & **tiket selesai** (hook di `message/handlers/teknisi-workflow-handler.handleSelesaiTicket` setelah `completeTicketWorkflow`, bawa durationMinutes+teknisi+catatan). Fire-and-forget (`.catch(()=>{})`) — additive, tak ubah notif teknisi/admin/pelanggan eksisting. Config `repairNotif {enabled, groupId, notifyNewTicket, notifyCompleted}` (default OFF, grup TERPISAH dari `psbIntake.groupId`). (2) **Perintah `tutorial`**: keyword `panduan teknisi`/`tutorial teknisi`/`cara psb`/`cara perbaikan` (`database/wifi_templates.json` → intent `TUTORIAL_TEKNISI`) → `handleTutorialTeknisiIntent` (`message/handlers/raf-intent-dispatch/menu-intents.js`, teknisi/owner-only) balas ringkasan langkah PSB+perbaikan (template `dispatch_tutorial_teknisi` + fallback) + link `config.teknisiTutorialUrl`. UI: pane "Intake PSB" (`views/sb-admin/config.php`) tambah subseksi Notif Perbaikan (toggle+grup, tombol Muat Grup ke-2 isi kedua dropdown) + field Link Tutorial; save/get `repairNotif`+`teknisiTutorialUrl` di `routes/admin-config-routes.js`. Tutorial visual = Artifact web (bukan di repo). Test: `lib/__tests__/repair-group-notifier.test.js` (5). Deploy GELAP (`repairNotif.enabled=false`).

<a id="b100"></a>
### Fix 2026-07-06 (cek-wifi pelanggan gagal beruntun — refresh berat men-trip circuit breaker GLOBAL)

- **Fix 2026-07-06 (cek-wifi pelanggan gagal beruntun — refresh berat men-trip circuit breaker GLOBAL)** — akar "cek device terhubung WiFi Dani bisa sekali lalu gagal terus" (device HG8145V5 `00259E-…D0D7B2`, RAF-DANDER). `message/handlers/wifi-check-handler.js` → `lib/wifi.getSSIDInfo(device_id, false)` → `lib/genieacs.getWifiInfo({skipRefresh:false})` DULU me-refresh **6 objek connection-request serentak**: seluruh subtree `InternetGatewayDevice.LANDevice.1` (**~13 dtk**, menyapu tabel Hosts+statistik) + `WLANConfiguration` (~9 dtk) + `VirtualParameters` + 3 path TR-181 `Device.WiFi.*` (sia-sia di ONU Huawei TR-098). Diproses SERIAL oleh 1 sesi CWMP ONU → total kerja tembus timeout **15 dtk** → `TIMEOUT_ERROR` beruntun → **circuit breaker global** (`lib/genieacs.js`, threshold 5 / open 30 dtk, proses-wide) OPEN → SEMUA pelanggan ditolak "circuit breaker open"; siklus open↔half-open = "bisa sekali lalu gagal terus". Server/ACS/modem terbukti SEHAT (NBI baca 6 ms, modem inform tiap 15 mnt, hop ACS→CPE 5 ms). **Bukti fleet: 158/158 device sudah membawa `TotalAssociations`/`AssociatedDevice` SEGAR tiap periodic inform (lag ~0 vs `_lastInform`)** → refresh berat tak perlu untuk data live. **Fix (tetap LIVE, tanpa trip breaker):** `getWifiInfo` refresh HANYA container WLAN (`resolvePathTemplates('wifiContainer')`) + `VirtualParameters` — **buang subtree `LANDevice.1`** — plus timeout refresh dilonggarkan **25 dtk** (override `config.genieacsWifiRefreshTimeoutMs`); `refreshDeviceObjects` kini meneruskan `timeoutMs` ke `submitTask`. Baca (`getDeviceRecord`) tetap murah/timeout default. Sisa hardening (breaker per-device, drop path TR-181 utk device TR-098) = kandidat lanjutan. Test `genieacs`+`network-ops`+`wifi-state-hardening` hijau; eslint bersih. Deploy prod 2 bot.

<a id="b101"></a>
### Fix 2026-07-06 (root domain `/` auto-forward per peran — bukan 403 "khusus Administrator")

- **Fix 2026-07-06 (root domain `/` auto-forward per peran — bukan 403 "khusus Administrator")** — akar "ketik domain saja kadang error akses hanya untuk admin": `routes/pages.js` `GET /` dulu di-hard-gate `checkRole(['admin','owner','superadmin'])`, dan `checkRole` TIDAK pernah redirect — selalu balas 403 teks. Efek tergantung cookie sesi saat itu: tamu tanpa token → sudah di-redirect `/login` oleh `lib/http-auth-bootstrap.js` (benar); admin → dashboard (benar); **teknisi → 403 "Halaman ini khusus Administrator"**; **pelanggan (`req.customer`, tanpa `req.user`) → 403 "token tidak valid" yang menyesatkan**. Fix: root `/` kini ROLE-AWARE meniru redirect pasca-login `routes/public.js` — admin/owner/superadmin render `index.php`; teknisi → `/pembayaran/teknisi`; agen → `/agen-pembayaran`; pelanggan/tak dikenal → `/login`. Middleware auth tetap penjaga tamu-tanpa-token (tak pernah sampai handler). Load+eslint bersih; tak ada test lama yang meng-assert 403 root. Commit ef2334e.

<a id="b102"></a>
### Fitur 2026-07-07 (Asisten pelanggan Gelombang 1 — matcher longgar korpus + fallback anti-diam + jalur wizard sandi)

- **Fitur 2026-07-07 (Asisten pelanggan Gelombang 1 — matcher longgar korpus + fallback anti-diam + jalur wizard sandi)** — akar terukur dari korpus `database/message_logs.sqlite` prod (review 02-07 & 07-07): matcher start-anchored hanya menjawab 19–22% teks pelanggan, sisanya bot DIAM (66% kontak di luar jam layanan). (1) **Matcher 2 lapis**: `resolveKeywordIntent` (`message/handlers/raf-state-routing.js`) menerima lapis longgar **`lib/loose-intent-matcher.js`** (murni, kamus sinonim/typo hasil tambang korpus: "wifine", "lemot", "gak konek", "knta sandi", dst) yang HANYA jalan saat matcher ketat gagal + gate caller di `message/raf.js` (`customerAssist.looseMatcher.enabled!==false` DAN bukan owner/teknisi/agen; pesan grup tak pernah mencapai dispatch DM). Loose memetakan HANYA ke intent aman: `CEK_KONEKSI` (read-only), `GANTI_SANDI_WIFI`, `HISTORY_WIFI` (kasus lupa sandi). **Kunci keselamatan: match longgar mematok `matchedKeywordLength = args.length` + `qAfterKeyword = ''`** sehingga handler TIDAK pernah membaca parameter dari kalimat bebas — `GANTI_SANDI_WIFI` selalu mendarat di wizard tanya-sandi existing (`services/wifi-management.service.js`: newPassword kosong → state `ASK_NEW_PASSWORD` → konfirmasi), bukan menganggap sisa kalimat sebagai password (jebakan lama `matchedKeywordLength || 1` + `args.slice`). (2) **Fallback anti-diam** owner **`message/handlers/customer-fallback-handler.js`** (hook di `message/raf.js` setelah `resolveKeywordIntent`, sebelum `dispatchIntent`; gate `customerAssist.fallback.enabled`, DEFAULT OFF): pelanggan terdaftar (`canonicalContext.user`) yang chat bebas → teks bergejala gangguan (`hasConnectivityComplaintSignal`) dialihkan ke intent `CEK_KONEKSI`, selain itu dibalas menu bantuan (template `assist_fallback_menu` di `database/response_templates.json`, kirim `reply {skipDuplicateCheck:true}`) dengan cooldown per chat; tetap DIAM untuk ack pendek ("iya mas"/"oke makasih"), staf/agen, nomor non-pelanggan, dan chat yang sedang ditangani admin manual — jejak balasan manual admin dicatat **`lib/chat-activity-tracker.js`** (in-memory bounded, single-instance) via hook `noteAdminOutbound` di cabang skip `fromMe` `message/raf.js`. Contoh config `config.example.json` blok `customerAssist{looseMatcher,fallback}`. Test: `lib/__tests__/loose-intent-matcher.test.js` (kalimat korpus asli) + `chat-activity-tracker.test.js` + `message/__tests__/customer-fallback-handler.test.js` + `raf-state-routing-loose.test.js`; regresi `message/__tests__` 62 suite hijau.

<a id="b103"></a>
### Fitur 2026-07-07 (Monitor kualitas jalur upstream per-paket — collector Fase 1)

- **Fitur 2026-07-07 (Monitor kualitas jalur upstream per-paket — collector Fase 1)** — jawab "pelanggan komplain lemot itu dari sisi mana": poller **`lib/upstream-quality-poller.js`** (start di `lib/app-runtime.js`, gate `config.upstreamMonitor.enabled` + kredensial API router; interval default 60 dtk, in-flight guard, timer unref) men-spawn bridge **`views/upstream_quality_probe.php`** (kelas `routeros_api.class.php` + konvensi `mikrotik_helper.php`; kredensial via env `MTIN_UPQ_*`, spec non-rahasia via argv JSON) → `/ping` **policy-routed per routing-table** dari router gateway: `gmdp`=main table, `ih`=table `FREE`, `mni`=table `MNI` (primary Tunnel-MNI L2TP, backup SF), `sf`=table `SF-PROBE` (route khusus probe, ditambah 2026-07-07 atas izin user) — TIDAK pernah `interface=` (terbukti vonis palsu, lihat memori traffic-monitor-per-isp); probe router-originated bebas mangle prerouting (paksaan ICMP freedns→IH tak berlaku). Hasil per siklus (loss/RTT min-avg-max/jitter per jalur×target; target default `8.8.4.4`+`1.0.0.1` — hindari 1.1.1.1/8.8.8.8 yg dipin recursive-check router) + snapshot route default per mark → **`repositories/upstream-quality.repository.js`** (`database/upstream_quality.sqlite`, retensi prune harian). Vonis per jalur (UNKNOWN/NORMAL/DEGRADASI/GANGGUAN/PUTUS; threshold loss 5/20% + faktor RTT 1.7×/2.5× vs baseline sehat 24 jam) + **deteksi failover MNI→SF** dari route aktif — `buildStatusReport()`. Endpoint admin owner **`routes/admin-upstream-quality-routes.js`** (registrar di `admin-router.js`): `GET /api/upstream-quality/status`, `GET /api/upstream-quality/history`, `POST /api/upstream-quality/poll-now`. Pemetaan pelanggan→jalur (utk verdict cek-koneksi, fase berikut): paket 110k/125k = pool PPPoE 192.168.61.0/24+192.168.62.0/24 (list `freedns`) → jalur `mni`; pelanggan reguler pool 192.168.70.0/24 (`lokaldns`) → `gmdp`; pengecualian mangle: WA/GAME/ICMP freedns → `ih`, speedtest → `gmdp`. Test: `repositories/__tests__/upstream-quality.repository.test.js` + `lib/__tests__/upstream-quality-poller.test.js` (23 tes). Aktivasi per bot via blok config `upstreamMonitor` (contoh di `config.example.json`); LIVE di RAF-DANDER, Tanjung OFF (router berbeda). **Gelombang 2 (hari yang sama):** (a) **alert transisi jalur** `lib/upstream-quality-alerter.js` — gate `upstreamMonitor.alerts.enabled===true` (default MATI), N siklus sakit beruntun (loss-based, anti-flap) → alert WA via `whatsapp-critical-delivery.sendCritical` ke `admin-recipients.getAdminJids` + grup opsional, M siklus sehat → notif pulih, cooldown per jalur; menyertakan **saran steer MNI→SF** karena router TIDAK failover otomatis saat degradasi kualitas (route interface-gateway tetap `active` selama tunnel hidup walau lossy — backup dist-2 hanya kick saat interface down); dipanggil dari `pollOnce` (never-throw). (b) **Integrasi verdict cek koneksi**: `lib/upstream-path-resolver.js` (IP PPPoE pelanggan → jalur via CIDR `upstreamMonitor.pathPools`, default = peta pool recon) + seksi upstream di `message/handlers/connection-check-handler.js` (cache addr PPP active + laporan status TTL 30 dtk; HANYA tampil saat jalur paket pelanggan DEGRADASI/GANGGUAN/PUTUS; pelanggan MNI dapat catatan "WA/game lewat jalur lain yang normal" bila IH sehat — menjelaskan pola komplain korpus "chat lancar tapi browsing lemot"). (c) **Halaman admin `/upstream-quality`** (`views/sb-admin/upstream-quality.php` + `static/js/upstream-quality.js`, Chart.js vendor lokal, kartu status + grafik loss/RTT 6 jam + banner failover + tombol Probe Sekarang; route `routes/pages.js`, link sidebar grup Jaringan `_navbar.php`). Template baru: `upq_alert_down`, `upq_alert_recovered`, `conncheck_upstream_issue`, `conncheck_upstream_note_mni`. Test tambahan: `lib/__tests__/upstream-quality-alerter.test.js` + `upstream-path-resolver.test.js`. **Fase 3 (hari yang sama — WAN multi-ISP mendalam):** bridge `views/upstream_quality_probe.php` diperluas dalam SATU koneksi/siklus: ping GATEWAY per WAN (tanpa routing-table, next-hop connected — pemisah segmen last-mile; path tunnel pakai remote-address hasil monitor), counter interface WAN (`/interface print stats`: byte/error/drop/link-downs), monitor tunnel l2tp/sstp (uptime → deteksi flap), plus mode `trace` (traceroute policy-routed satu putaran). Kalkulasi murni di **`lib/upstream-wan-metrics.js`**: delta counter→bps (guard counter-reset), `detectFlap` (uptime mundur / link-downs naik), `computeUtilization` vs `paths[].capacity` (Mbps, 0=belum diisi→util null), dan **`classifySegment`** → SEHAT/JENUH(util≥`thresholds.saturationPct` 85)/LINK_ISP(loss di gateway)/UPSTREAM_ISP(gateway bersih). Repo: tabel baru `wan_link_samples` + `upstream_incidents` (alert/flap/trace), `getSummary` far-only (`target_key != 'gateway'`) + `getGatewaySummary` + `getWanWindowStats` + `getIspReport` (rapor availability/loss/RTT/sick%/flap per ISP) + `addIncident`/`getIncidents`; prune ikut. `buildStatusReport` per path kini membawa `segment`/`segment_label`/`gateway`/`wan`. `runTraceProbe(path)` menyimpan insiden trace (first_bad_hop); alerter otomatis men-trace + memperkaya slot `${saran}` (segmen+loss gateway+utilisasi) via `deps.getPathStatus`, dan mencatat insiden alert. Endpoint tambah: `GET wan-history`/`report`/`incidents`, `POST trace/:path`. Halaman `/upstream-quality` tambah grafik throughput per WAN, tabel Rapor ISP 7 hari, daftar insiden, tombol Trace per jalur. Test: `lib/__tests__/upstream-wan-metrics.test.js` + perluasan repo test.

<a id="b104"></a>
### Fitur 2026-07-07 (Switch koneksi WAN instan — dari deteksi ke kendali)

- **Fitur 2026-07-07 (Switch koneksi WAN instan — dari deteksi ke kendali)** — owner/admin mengalihkan trafik antar-ISP (mis. MNI→SF saat MNI degradasi) dari WhatsApp ATAU panel, reversible + ber-audit. Service **`lib/wan-switch-service.js`** owner: `getStatus()` (baca kondisi tiap switch: applied/normal), `applySwitch(id, direction, actor)` / `restoreSwitch(...)`. Definisi switch di **`config.wanSwitch.switches`** (allowlist; tiap switch = daftar `operations[{match,apply:'disable'|'enable'}]`, match by `table('main')/routingMark/gateway/comment/distance`); default 2 switch hasil recon router DANDER: `mni-to-sf` (disable route mark MNI gw Tunnel-MNI → backup SF dist-2 auto-aktif) & `main-to-ih` (disable GMDP `1.1.1.1/MAIN` + enable IH `192.168.102.1` di tabel utama). Bridge **`views/mikrotik_route_switch.php`** (mode status/plan read-only + apply write `/ip/route/set disabled=`; NEVER hapus route). **Guard berlapis:** allowlist id, gate `wanSwitch.enabled` (default OFF), plan-verify (abort bila 0 route cocok — anti topology-drift), audit incident (`repo.addIncident` kind 'switch') + notif admin WA (`sendCritical {text}` + `getAdminJids`/`wanSwitch.recipients`), role admin/owner/superadmin PRESISI (dari accounts.json, BUKAN isOwner placeholder/isTeknisi longgar). **WhatsApp:** intent `SWITCH_KONEKSI` (`owner-admin-intents.handleSwitchKoneksiIntent` → `startWanSwitch`) → state domain **`message/handlers/state-domains/wan-switch.state.js`** (prefix `WANSW_` di conversation-state-owner-map + router; alur SELECT→CONFIRM, eksekusi HANYA setelah *ya*, hormati *batal*). **Panel:** kartu switch di `/upstream-quality` (`static/js/upstream-quality.js` → `GET /api/wan-switch/status`, `POST apply|restore/:id` di `routes/admin-wan-switch-routes.js`, gate `requireAdminOwner`). Alert MNI kini menautkan "balas *switch koneksi*" saat `wanSwitch.enabled`. Template `wanswitch_*` di response_templates. Test: `lib/__tests__/wan-switch-service.test.js` (allowlist/gate/plan-abort/apply+notify) + `message/handlers/state-domains/__tests__/wan-switch.state.test.js` (gate peran/wizard/konfirmasi). Aktivasi per bot: `wanSwitch.enabled=true` + isi kredензial + recipients. LIVE Dander, Tanjung OFF (router beda).

<a id="b105"></a>
### Fitur 2026-07-07 (Monitoring reachability per-LAYANAN per-JALUR — TCP+TLS via source-IP steering)

- **Fitur 2026-07-07 (Monitoring reachability per-LAYANAN per-JALUR — TCP+TLS via source-IP steering)** — jawab "Instagram/FB/WA/Google via MNI/SF/GMDP/IH bermasalah atau tidak" dengan pengukuran NYATA (Meta & Google-web memblokir ICMP → ping tak bisa; TCP-443 satu-satunya cara akurat). Prober **`lib/service-reachability-prober.js`** (start `app-runtime`, gate `config.serviceMonitor.enabled` + minimal 1 jalur ber-`srcIp`): tiap ~60 dtk resolve host layanan → `tls.connect` (atau `net.connect`) ke IP:443 **MENGIKAT `localAddress` = IP alias server per jalur** → catat connect_ms + tls_ms + ok/error per layanan×jalur ke `service_probes`. **Akurasi: server di BELAKANG router → trafik di-forward+NAT seperti pelanggan (bukan router-originated), menghindari jebakan probe-dari-router yang dulu vonis Indihome palsu; tervalidasi live (ping via alias .201→SF = 56ms vs default→GMDP 17ms; Instagram TCP connect 21ms/TLS 55ms).** INFRA (permanen, terpisah dari kode): 3 IP alias di server `ens18` (`.202→MNI`, `.203→SF`, `.204→IH`; `.2`=GMDP default) + 3 mangle prerouting router `src=<alias> action=mark-routing new-routing-mark=<tabel>` (comment `RAF-SVCPROBE`). Config `serviceMonitor` memetakan `paths[].srcIp` ke alias itu; `services[]` = host layanan (default IG/FB/WA/Google/YouTube/Cloudflare — IG/FB/WA satu jaringan Meta 157.240.208.x, YouTube+Google=Google). `buildServiceReport` = matriks verdict OK/LAMBAT/TERGANGGU/DOWN + connect/TLS ms per sel. Alert: sebuah JALUR gagal ≥`minServicesDown` layanan `consecutiveCycles` beruntun → WA admin (`svc_alert_down` template). Endpoint `GET /api/service-quality/status|history`, `POST poll-now` (`routes/admin-upstream-quality-routes.js`). Panel: matriks Layanan×Jalur di `/upstream-quality`. Test: `lib/__tests__/service-reachability-prober.test.js` (probeConnect/cycle/verdict/alert/report; net/tls/dns di-inject). Aktivasi: pasang alias+mangle → isi `serviceMonitor.paths[].srcIp` + `enabled=true`. LIVE Dander; Tanjung OFF (router beda).

<a id="b106"></a>
### Fitur 2026-07-07 (Auto-pilot jaringan — failover WAN otomatis + agregasi komplain pelanggan)

- **Fitur 2026-07-07 (Auto-pilot jaringan — failover WAN otomatis + agregasi komplain pelanggan)** — melengkapi rantai deteksi→kendali menjadi deteksi→KEPUTUSAN OTOMATIS, untuk operasi tanpa admin standby. (1) **Engine failover `lib/wan-failover-service.js`** (owner keputusan): dipanggil dari `pollOnce` `lib/upstream-quality-poller.js` setelah alerter (never-throw), gate `config.wanFailover.enabled` (default OFF) + `wanSwitch.enabled`. Rule per `switchId` allowlist (`wanFailover.rules[]`: `sickPath`/`targetPath`/`minLevel`/`sickCycles`/`targetHealthyCycles`/`mode`/`autoRevert`/`revertHealthyCycles`): deret siklus dibangun dari `repo.getRecentProbes` DIPISAH far-target vs gateway (`buildSeries`) — far utk pemicu, **GATEWAY utk bukti kepulihan/auto-revert** (pasca-switch probe jauh ikut route backup sehingga tak valid menilai jalur sakit). Mode `propose` = kirim saran WA saja (cooldown `proposeCooldownMinutes`); mode `auto` = eksekusi `wan-switch-service.applySwitch(id,'apply',{label:'bot:auto-failover'})` (tetap lewat plan-verify+audit+notif service). **Guard berlapis:** verifikasi `getStatus()` router sebelum aksi (status tak terbaca → JANGAN menulis), cooldown per rule, kuota `maxAutoActionsPerDay` (apply+restore dihitung), kunci anti-flapping (`flapLockCount` pasang bolak-balik dalam `flapLockHours` → lock + alert sekali), auto-revert hanya bila rule `autoRevert` + switch diterapkan oleh auto-pilot sendiri (manual apply → hanya saran kembalikan), rehidrasi state dari insiden (`kind: switch/failover`) pasca-restart. Insiden `kind:'failover'` (`action: propose/auto_apply/auto_restore/revert_propose/lock/error`) di `upstream_incidents`. WA via `sendCritical {text}` + `getAdminJids`/`wanFailover.recipients`; template `wanfail_propose|auto_start|auto_fail|revert_propose|auto_revert_start|lock`. (2) **Agregator komplain `lib/complaint-signal-service.js`** (owner sinyal komplain): `recordComplaint({user,source,addr,lineStatus})` dari **dua pintu** — `message/handlers/connection-check-handler.js` (source `cek_koneksi`, addr dari cache PPP active, skip `lineStatus==='offline'`) dan `lib/report-orchestration-service.js` `createCustomerReportTicket` (source `tiket_lemot`, regex `lemot|lambat` pada laporanText — choke point semua varian smart-report). Resolusi jalur via `lib/upstream-path-resolver` (fallback lookup PPP active `router_id default`, cache 30 dtk; gagal → `unknown`). Dedup per pelanggan (`perCustomerCooldownMinutes`), jendela `windowMinutes` (default 15), ambang `minDistinctCustomers` (default 3) per jalur → SATU alert WA admin (kondisi jalur dari `buildStatusReport` + saran switch bila rule failover tersedia; cooldown `alertCooldownMinutes` per jalur) + insiden `kind:'complaint_cluster'`; state in-memory (single-instance), gate `config.complaintSignals.enabled` (default OFF). Template `complaint_cluster_alert`. Test: `lib/__tests__/wan-failover-service.test.js` (21) + `complaint-signal-service.test.js` (9). Aktivasi per bot: `complaintSignals.enabled=true` (aman, alert-only) lalu `wanFailover.enabled=true` mode `propose` → naikkan rule `mni-to-sf` ke `mode:"auto"` setelah yakin. Contoh blok di `config.example.json`. Deploy GELAP.

<a id="b107"></a>
### Fitur 2026-07-07 (Perintah WA `data <isp>` — rangkuman ISP on-demand utk owner/admin)

- **Fitur 2026-07-07 (Perintah WA `data <isp>` — rangkuman ISP on-demand utk owner/admin)** — keyword `data isp`/`rapor isp`/`data gmdp|ih|mni|sf|indihome`/`data jalur|wan`/`/data` (entri intent `DATA_ISP` di `database/wifi_templates.json`) → `handleDataIspIntent` (`message/handlers/raf-intent-dispatch/owner-admin-intents.js`; gate peran PRESISI admin/owner/superadmin via `resolveStaffRole` dari `wan-switch.state`, non-admin dibalas template `ispdata_not_authorized`) → **`lib/isp-data-summary.js`** (READ-ONLY): `resolvePathArg(chats)` (key/kata label, stoplist kata umum) memilih per-jalur vs overview; `buildIspDataSummary(pathKey)` = vonis KESELURUHAN (OK/perlu perhatian/sedang bermasalah dari status kini + sick%/avail 24 jam) + terdampak + status alert (SICK sejak) + rincian arah & kesimpulan segmen & matriks layanan (REUSE formatter alerter `_internal.buildDirectionText/buildConclusionText/buildServiceSectionText` — konsisten dgn isi alert) + rapor 24 jam & 7 hari (`repo.getIspReport`) + 3 insiden terakhir; `buildIspOverview()` = semua jalur ringkas + vonis global "KESELURUHAN OK / ADA YANG PERLU PERHATIAN". Balasan dibungkus template `ispdata_result` (slot `${body}`). Monitor nonaktif (Tanjung) → pesan informatif. **v2 DETAIL (2026-07-08):** per-jalur kini laporan diagnostik penuh 3 seksi — KONDISI SEKARANG (route aktif per-mark + deteksi BACKUP AKTIF/route dinonaktifkan dari `route_snapshot`+`failover` report; trafik 15 mnt + puncak throughput/error/drop/flap 24 jam + tunnel uptime dari `getWanHistory`; arah/layanan/segmen reuse formatter alerter; status engine auto-failover dari `getRuleStates`; komplain 60 mnt dari `complaint-signal-service.getComplaintStats`), 24 JAM TERAKHIR (**sparkline pola loss per jam** `▁▂▃…█`/`·` skala absolut + jam terburuk; agregat per-arah rata/puncak/jitter dari `getRecentProbes`; **downtime nyata** `computeDowntime` dari pasangan insiden alert→alert_recovered incl. clip jendela + gangguan BERJALAN saat state SICK; layanan bermasalah 24 jam dari `getServiceSummary`), 7 HARI (rapor + downtime) + 5 insiden terakhir. Overview ikut membawa trafik kini + BACKUP AKTIF + downtime 24 jam per jalur. Semua sumber di-guard `typeof` (repo lama/bot tanpa fitur → seksi dilewati). Test: `lib/__tests__/isp-data-summary.test.js` (13).

<a id="b108"></a>
### Fitur 2026-07-08 (Panel web kustomisasi ARAH monitor — target/layanan/jalur/thresholds)

- **Fitur 2026-07-08 (Panel web kustomisasi ARAH monitor — target/layanan/jalur/thresholds)** — owner leluasa menambah/mengubah arah pemantauan TANPA edit config.json manual & TANPA restart. Kartu "⚙️ Pengaturan Arah & Jalur" di `/upstream-quality` (`views/sb-admin/upstream-quality.php` + `static/js/upstream-quality.js` — collapsed, lazy-load saat dibuka) → `GET/PUT /api/upstream-quality/config` (`routes/admin-upstream-quality-routes.js`; GET staff + flag `canEdit`, PUT gate `requireAdminOwner`) → owner logika **`lib/upstream-config-service.js`**: `getEditableUpstreamConfig()` (view EFEKTIF ber-default dari `poller.getMonitorConfig` + `prober.getServiceConfig`, wiring routingTable/iface/tunnelType/srcIp ditandai read-only) + `applyUpstreamConfigPatch(patch, actor)` (validasi SEMUA-ATAU-BATAL: target key unik + IPv4/hostname + TOLAK 1.1.1.1/8.8.8.8 (dipin recursive gateway-check router); layanan host WAJIB hostname (IP CDN Meta/TikTok arbitrer mati); paths hanya field non-struktural label/affects/gatewayTarget/capacity per key existing — jalur baru tetap via config.json; thresholds rentang + crit>warn) → tulis `config.json` (backup `config.json.bak-webedit`) + mutasi `global.config` (LIVE: poller/prober baca config tiap siklus; `intervalSeconds` saja yang butuh restart) + insiden kind `config` (audit actor+changes). Test: `lib/__tests__/upstream-config-service.test.js` (8). Ini juga jalur resmi mengisi `capacity` Mbps per ISP (mengaktifkan deteksi JENUH) dari web.

<a id="b109"></a>
### Fitur 2026-07-08 (Steering Pelanggan per-ISP — visibilitas live + kendali web)

- **Fitur 2026-07-08 (Steering Pelanggan per-ISP — visibilitas live + kendali web)** — jawab "pelanggan X sekarang lewat ISP mana & arahkan leluasa". Halaman `/steering-pelanggan` (`views/sb-admin/steering-pelanggan.php` + `static/js/steering-pelanggan.js`, sidebar Jaringan; route `routes/pages.js` role admin+teknisi-lihat) → `routes/admin-customer-steering-routes.js` (registrar di `admin-router.js`; tulis = `requireAdminOwner`) → owner **`lib/customer-steering-service.js`**: (1) `buildSteeringOverview()` join LIVE: PPP active (`lib/mikrotik.getActivePPPoEUsers`) × `global.users` × address-list router (bridge) × route-state (`buildStatusReport().route_snapshot`) → per pelanggan `intended` (override RAF-STEER > pool freedns→mni > lokaldns→gmdp > default-main; SEMANTIK MANGLE DANDER recon 08-07: semua passthrough=no first-match, freedns: DNS/8.8.8.8/dst-lokal→GMDP-main, WA/GAME/ICMP→FREE(IH), sisanya→MNI (rule "ROUTING ALL FREE" idx40 SHADOWED oleh idx39), lokaldns→main dgn VOD/DLOAD→KE-TIKTOK) + `actual` (mark menumpang route aktif — mis. MNI riding SF; peta gateway→path `gatewayPathMap`); (2) `steerCustomer(userId, path|null)` — intent di `database/customer_steering.json`, entri `/32` comment `RAF-STEER uid=<id>` di list `RAF-STEER-GMDP|IH|MNI|SF`, **reconciler** interval 60 dtk (start `lib/app-runtime.js`, gate `config.customerSteering.enabled`) menjaga entri MENGIKUTI IP PPPoE baru saat reconnect + membersihkan entri yatim; offline → diterapkan otomatis saat online; (3) `poolEntryAction` toggle/add/remove entri freedns/lokaldns (allowlist `poolLists`, aksi pindah SATU POOL, konfirmasi UI); (4) `setupSteeringRules` — pasang idempoten 4 rule mangle `RAF-CUSTSTEER-*` (GMDP=accept→main; IH=mark FREE; MNI=mark MNI; SF=mark SF-PROBE; `passthrough=no`) di ANCHOR place-before rule mark-routing pertama (di atas speedtest/QoS — steering = override TOTAL, prioritas WA/game dilewati selama disteer; INERT selama list kosong). Bridge **`views/mikrotik_addrlist_steer.php`** (mode list/entry-add/entry-remove/entry-toggle/setup; guard: entri dynamic haram disentuh, rule mangle hanya ber-comment RAF-CUSTSTEER; kredensial env `MTIN_UPQ_*` waris `customerSteering`/`wanSwitch`/`upstreamMonitor`). Insiden audit kind `steer`/`steer_pool`/`steer_setup`. Config `customerSteering` (contoh di config.example.json; default OFF). Test: `lib/__tests__/customer-steering-service.test.js` (13).

<a id="b110"></a>
### Fitur 2026-07-08 (Diagnosa app-aware — "halo AI" buatan sendiri, jawaban spesifik per aplikasi)

- **Fitur 2026-07-08 (Diagnosa app-aware — "halo AI" buatan sendiri, jawaban spesifik per aplikasi)** — akar dari review korpus prod `message_logs.sqlite` (`inbound_messages`): pelanggan menyebut app SPESIFIK ("Digae tik tok kog muter terus", "video FB sama YouTube", "Shopee kak", "browsing semua lemot") tapi (a) sering di pesan SUSULAN, (b) dialek/gejala lokal ("muter terus"=buffering) yang matcher lewatkan. Rantai baru: **`lib/app-entity-extractor.js`** (murni; sebutan app→`serviceKey` matriks + `kind` video/social/chat/web/game/call + `symptom`; katalog TikTok/YouTube/IG/FB/WA/Shopee/browsing/game/streaming/meet; `extractAppEntities`/`topAppEntity`/`hasBareAppMention`) → **`lib/app-aware-diagnosis.js`** `buildAppDiagnosis({addr, appEntity})` (READ-ONLY, never-throw): resolve IP→jalur (`upstream-path-resolver`) lalu baca sel matriks reachability app×jalur (`service-reachability-prober.buildServiceReport`) → jawaban ber-ANGKA + pembanding jalur tersehat ("TikTok via MNI lambat ~640ms vs normal ±78ms — sisi kami, bukan perangkat"); app tanpa probe (game/streaming) → pakai loss/RTT jalur (`buildStatusReport`); app normal → arahkan ke perangkat + tawarkan *lapor*. Template `conncheck_app_issue|app_ok|app_path_issue|app_path_ok`. **Wiring:** (1) `connection-check-handler.handleCekKoneksi` kini terima `chats` (diteruskan dari `customer-service-intents`), ekstrak app, tambah seksi app-spesifik ke balasan + kirim `app` ke `complaint-signal-service` (alert owner kini bisa "mayoritas menyebut TikTok"); (2) **`loose-intent-matcher`** (A1, LIVE) — app+gejala jadi konteks jaringan (prioritas 2.5) → "tiktok muter"/"youtube lemot"/"ML ngelag" → CEK_KONEKSI tanpa perlu kata "wifi/internet" (menjawab contoh korpus yang dulu lolos); (3) `customer-fallback-handler` (gate A2) — app+gejala ATAU sebutan app polos ("Shopee kak") yang menyusul komplain koneksi baru-baru ini (`chat-activity-tracker.noteConnectivityComplaint`/`getConnectivityComplaintAgeMs`, jendela `complaintContextMinutes` default 10m) → CEK_KONEKSI (multi-turn). Nol LLM/halusinasi — tiap klaim = hasil ukur. Test: `lib/__tests__/app-entity-extractor.test.js` (13, korpus verbatim) + `app-aware-diagnosis.test.js` (7) + perluasan `loose-intent-matcher.test.js`. Seksi app tampil LIVE saat serviceMonitor/upstreamMonitor aktif (Dander); multi-turn susulan aktif saat A2 fallback dinyalakan.

<a id="b111"></a>
### Fitur 2026-07-09 (Notif PULIH LOS — menutup alarm yang tergantung)

- **Fitur 2026-07-09 (Notif PULIH LOS — menutup alarm yang tergantung)** — dulu grup/teknisi hanya dapat alarm LOS "mati" tanpa kabar saat ONU balik online (kurang akurat: tak tahu mana yang masih rusak). Sinyal pulih SUDAH mengalir (`lib/olt-event-classifier` parse syslog "Discovery" + `lib/olt-snmp-los-poller` transisi online → keduanya `handleOltEvent({event_type:'discovery'})`) tapi `cancelActiveIncident` di **`lib/olt-los-broadcaster.js`** hanya mencatat `recoveredAt`, tak mengirim. Ditambah: saat ONU yang SUDAH di-broadcast LOS pulih → `scheduleRecovery` (debounce anti-flap `recoveryConfirmMs` default 60s; kalau LOS lagi dalam window → dibatalkan via `recovering` map di `handleOltEvent`, kembali ke insiden aktif yang sama) → `recoveryFlush` (batch per-OLT `recoveryClusterFlushMs`; ≥clusterThreshold pulih serentak → 1 pesan "PEMULIHAN AREA") → `sendRecovery` (durasi putus `now−downAtMs` + rentang jam, ke grup+teknisi yang sama, label `los_recovery`, idempoten via `recoveredNotifiedAt`). **Restart-robust:** discovery tanpa active in-memory → `scheduleRecoveryFromFile` rekonstruksi dari `los-incidents.json` (status broadcasted tanpa recoveredNotifiedAt). Config `oltLosBroadcast.{notifyRecovery,recoveryConfirmMs,recoveryClusterFlushMs}` (default OFF) + toggle di `/los-broadcast` (route `admin-los-broadcast-routes.js` normalize+decorate; halaman `los-broadcast.php`). Test: `lib/__tests__/olt-los-broadcaster.test.js` (+5 = 25). Nol regresi (20 test lama hijau).

<a id="b112"></a>
### Fitur 2026-07-09 (Konfirmasi Bukti Pembayaran — foto bukti transfer pelanggan → 1-klik lunas)

- **Fitur 2026-07-09 (Konfirmasi Bukti Pembayaran — foto bukti transfer pelanggan → 1-klik lunas)** — dulu foto polos (tanpa caption, tanpa state) dari pelanggan DIBUANG diam-diam di guard `!chats` `message/raf.js` sehingga pelanggan kirim bukti transfer tanpa respons apa pun. Rantai baru: hook standalone-media di **`message/raf.js`** (sesudah managed state, sebelum drop `!chats`; SCOPED customer terdaftar + non-staf + tanpa `userState.step` + gate `config.paymentProof.enabled` default ON; non-throwing) → **`message/handlers/payment-proof-handler.js`** (tipis: unduh via `downloadMedia` adapter, balas pelanggan via helper `reply` injeksi; ditambahkan ke guard `message/__tests__/wa-forbidden-imports.test.js`) → owner **`services/payment-proof.service.js`** (`createPaymentProofService`/`getPaymentProofService`): `handleIncomingProof` simpan bukti + snapshot tagihan (`getCurrentBillingPeriod`+`getPaymentPositionForPeriod`+`getEffectivePrice`) + notif admin BERGAMBAR ke `getAdminJids()` via `sendMessageToMany({image|document,caption})` (best-effort, never-throw); `confirmProof` = catat lunas fail-closed + reaktivasi profile-state-gated lewat `lib/services/bill-payment-settlement.settleTagihanPayment` (method `TRANSFER_BANK`; idempoten — ledger `no_change` tak kirim struk ganda) + struk pelanggan `sendCritical {text}`; `rejectProof` = tandai rejected + notif pelanggan. Persistence **`repositories/payment-proof.repository.js`** (`database/payment_proofs.json` + `temp/payment_proofs/`, `withLock`). Sisi admin: **`routes/admin-konfirmasi-bayar-routes.js`** (registrar di `admin-router.js`; `ensureAdmin`; `GET /api/konfirmasi-bayar/list`, `GET :id/proof` = `res.sendFile` ber-auth — JANGAN via `/temp` static yang melewati auth untuk ekstensi gambar; `POST :id/konfirmasi|tolak`) + halaman **`views/sb-admin/konfirmasi-bayar.php`** (clean-URL `routes/pages.js` role admin/owner/superadmin; item navbar seksi Pembayaran) + **`static/js/konfirmasi-bayar.js`** (daftar + foto + tombol SweetAlert konfirmasi/tolak, poll 30 dtk, cookie-auth `credentials:'same-origin'`). Template `payment_proof_received|admin_notification|confirmed|rejected` di `response_templates.json`. Mark-lunas SELALU via `applyPaymentStatusChange` (tak menulis `paid`/ledger langsung). Test: `services/__tests__/payment-proof.service.test.js` (9) + `repositories/__tests__/payment-proof.repository.test.js` (4) + `message/handlers/__tests__/payment-proof-handler.test.js` (4). Aktif default; kill-switch `config.paymentProof.enabled=false`.

<a id="b113"></a>
### Fitur 2026-07-10 (Konfirmasi Bukti Pembayaran DARI WHATSAPP — menutup jalur admin yang buntu)

- **Fitur 2026-07-10 (Konfirmasi Bukti Pembayaran DARI WHATSAPP — menutup jalur admin yang buntu)** — Fase 1 (fitur 2026-07-09 di atas) sudah mengirim notif bergambar ke admin, tapi `confirmProof`/`rejectProof` HANYA terjangkau lewat `routes/admin-konfirmasi-bayar-routes.js` (HTTP + `ensureAdmin`): tak ada intent/keyword/handler apa pun di `message/`, sehingga admin **tidak bisa menerima pembayaran dari WhatsApp**. Lebih buruk, `adminUrl` di caption dirakit dari `config.site_url_bot` yang adalah URL **internal** bot (`http://127.0.0.1:3100`) → tautan mati saat diketuk dari HP. Rantai baru: hook teks-staf di **`message/raf.js`** (sesudah hook foto bukti, sebelum resolusi intent; syarat teks non-kosong + tanpa `userState.step` + gate `config.paymentProof.enabled`; non-throwing) → **`message/handlers/payment-proof-admin-handler.js`** (`handlePaymentProofAdminDecision`, `parseProofCommand`, `extractQuotedText`) → `services/payment-proof.service` (`listPending`/`confirmProof`/`rejectProof` yang SAMA dengan portal — nol logika duplikat). Dua bentuk perintah: (1) berkode `terima BP-YYMMDD-XXXX` / `tolak BP-… <alasan>` / `bukti` (daftar antrian); (2) **balasan ter-quote** pada notif bukti — `ok` / `tolak <alasan>`, kode diambil dari caption `quotedMessage`. **Gate peran PRESISI** `resolveStaffRole` (`state-domains/wan-switch.state`, accounts.json role admin/owner/superadmin) — BUKAN `isOwner` (hanya cocok `config.ownerNumber`, di prod sering beda dari nomor admin) dan BUKAN `isTeknisi` (truthy untuk SEMUA akun accounts.json, tanpa peduli role). Gate dijalankan SESUDAH parsing dan non-admin dikembalikan `handled:false` senyap → fitur tak bocor & pesan jatuh normal ke jalur intent. Balasan ter-quote hanya sah bila pesan yang di-quote memuat kode `BP-…`, jadi "ok" ke pesan bot lain tak pernah mengonfirmasi pembayaran; "terima kasih"/"tolak angin" dari pelanggan tak pernah cocok. Balasan admin lewat helper `reply` injeksi (`{skipDuplicateCheck:true}`), tak pernah `sendMessage` mentah. `adminUrl` kini `config.paymentProof.adminUrl` (URL portal publik) dengan fallback `site_url_bot`. Template baru `payment_proof_admin_{list,list_empty,confirm_ok,confirm_already_paid,reject_ok,not_found,already_processed,error}` + `payment_proof_admin_notification` diperbarui (mengajarkan perintah balas). Test: `message/handlers/__tests__/payment-proof-admin-handler.test.js` (18, termasuk gate peran nyata via accounts.json) + `payment-proof-admin-handler.js` ditambahkan ke guard `message/__tests__/wa-forbidden-imports.test.js`. Kill-switch sama: `config.paymentProof.enabled=false`.

<a id="b114"></a>
### Fitur 2026-07-10 (Konfirmasi bukti bayar WA — mode "nol ketik kode")

- **Fitur 2026-07-10 (Konfirmasi bukti bayar WA — mode "nol ketik kode")** — lanjutan fitur di atas: mengetik `terima BP-260710-Y6PZ` terlalu berat, dan `ok` TANPA reply-quote dulu ditelan diam-diam (admin mengira sudah konfirmasi). Sekarang **`message/handlers/payment-proof-admin-handler.js`** mengenali 4 bentuk: (1) balasan ter-quote (`ok` / `tolak <alasan>`, kode dari caption) → eksekusi langsung; (2) **nomor antrian** (`terima 1`, `tolak 2 <alasan>`) → eksekusi langsung; (3) kode eksplisit; (4) **polos** (`ok` / `tolak` persis) → 1 antrian: minta penegasan `ya`; banyak antrian: tampilkan daftar bernomor. **Antrian KOSONG + perintah polos → `handled:false`** (pesan dilepas; "ok" biasa admin tak dibajak). Langkah lanjutan dipegang state domain baru **`message/handlers/state-domains/payment-proof-admin.state.js`** (`PAYPROOF_SELECT` = menunggu angka; `PAYPROOF_CONFIRM` = menunggu `ya`, dan pada aksi tolak teks bebas = ALASAN), didaftarkan di `conversation-state-owner-map.js` (prefix `PAYPROOF_` → owner `payment-proof`) + `conversation-state-router.js`. Snapshot antrian disimpan di state supaya **penomoran stabil** walau ada bukti baru masuk di tengah jalan. Guard penting: `ctx.isGlobalCommand` → `handled:false` (admin tetap bisa `menu`/`lapor` keluar dari alur; raf.js baris ~526 yang menghapus state), teks kosong (foto) → `handled:false`, gate peran `resolveStaffRole` diulang tiap langkah, dan regex tolak-polos dicocokkan PERSIS supaya `tolak angin` tidak terbaca sebagai perintah. Hook di `raf.js` kini menyuntik `stateSender` + `setUserState`. Template baru `payment_proof_admin_{confirm_prompt,reject_prompt,need_yes,invalid_choice}`; `payment_proof_admin_list` jadi bernomor; `payment_proof_admin_notification` mengajarkan `ok` polos + `bukti`. Test: `__tests__/payment-proof-admin-handler.test.js` + `state-domains/__tests__/payment-proof-admin.state.test.js` (49 test gabungan) + guard forbidden-imports. Nol regresi (`message`: 80 suite / 339 test hijau).

<a id="b115"></a>
### Fitur 2026-07-10 (Reboot berbantu + tindak lanjut yang MEMBUKTIKAN)

- **Fitur 2026-07-10 (Reboot berbantu + tindak lanjut yang MEMBUKTIKAN)** — sebelumnya jalur reboot pelanggan praktis mati: keyword `REBOOT_MODEM` ("reboot modem"/"restart modem") tak pernah diketik siapa pun (0 reboot dipicu pelanggan dalam 6 hari log prod), gerbang konfirmasi `handleConfirmReboot` memakai exact-match `["ya","ok","lanjut","iya","y"]` (hanya 17% balasan afirmatif nyata lolos — pelanggan menulis "Ok mas"/"ya ka"/"Siap"), dan sesudah reboot bot mengirim `other_reboot_processing` ("tunggu 5-10 menit") lalu **diam selamanya**: satu-satunya tindak lanjut adalah `setTimeout(() => console.log("rebooted successfully"), 5000)` — klaim sukses tanpa verifikasi, padahal `rebootRouter().ok` hanya berarti GenieACS **menerima task** (`applied` selalu `null`). Rantai baru: pintu masuk bukan keyword melainkan hasil diagnosa **`message/handlers/connection-check-handler.js`** (`buildRebootOffer`, dipanggil sesudah `buildUpstreamSection`) → gate keamanan **`lib/reboot-followup-service.js:evaluateRebootGate`** (tolak bila `GANGGUAN_AREA` / `UPSTREAM_SAKIT` (DEGRADASI|GANGGUAN|PUTUS via `upstream-quality-poller.buildStatusReport`) / `SUDAH_RESTART_SENDIRI` (`lib/affirmative-parser.saysAlreadyRestarted`) / `PELANGGAN_VOUCHER` / `MODEM_TAK_TERJANGKAU` (`lib/device-status.isDeviceOnline`); mode **`strict`** saat `config.upstreamMonitor.enabled` mati (Tanjungharjo) menambah syarat `offlineCount === 0`) → bila lolos, set state `REBOOTFU_OFFER` (di-key **JID kanonik** dari `resolveCustomerBySender().canonicalJid`) dan tawarkan izin. **Bot tidak pernah mereboot tanpa izin.** Jawaban izin ditangani **`message/handlers/states/reboot-followup-state-handler.js`** (`handleRebootOffer`) → `executeRebootAndSchedule` (reboot + catat pekerjaan durabel; state dihapus SEBELUM eksekusi karena kelanjutan dipegang store, bukan state 15 menit) → tick `lib/reboot-followup-service.js` (start dari `lib/app-runtime.js`, interval 30 dtk, `unref`) pada `dueAt` (T+6 mnt) **memverifikasi dulu** lewat `isModemBack`: bukti boot ulang = `getDeviceLastInform` **lebih baru dari `rebootAt`** (⚠️ BUKAN "inform < N menit" — inform periodik ACS prod ~12 mnt, jadi modem sehat pun sering terakhir inform 11 mnt lalu), dengan sesi PPPoE MikroTik sebagai bukti sekunder; PPPoE masih down = belum kembali. Modem belum balik → `rebootfu_still_down` + jadwal ulang (`retryMs` 7 mnt agar cek kedua melewati satu siklus inform penuh), habis percobaan → `rebootfu_failed` + eskalasi `getAdminJids()`; modem terbukti balik → `rebootfu_ask` + state `REBOOTFU_ASK`. Gate memakai jendela terpisah `gateInformMaxMinutes` (default 30). Jawaban "masih bermasalah" **tidak langsung jadi tiket**: `runDeepCheck` (PPPoE → upstream → redaman optik) lalu jujurkan verdict. Parser `lib/affirmative-parser.js` menggantikan exact-match di seluruh jalur ini (`other-state-handler.js` ikut memakainya); ⚠️ token `"tak"` SENGAJA bukan negasi — di dialek pelanggan `"Sudah tak bayar"` = *sudah SAYA bayar*. Prefix state `REBOOTFU_` → owner `reboot-followup` di `conversation-state-owner-map.js`. Pengiriman proaktif lewat `sendCritical(jid, { text }, …)` (payload WAJIB objek). **Bug lama ikut diperbaiki:** template tersimpan `conncheck_online|offline_single|unknown` tak punya slot `${upstream}`/`${app}` sehingga seksi jalur upstream & diagnosa per-aplikasi dihitung tapi **tak pernah sampai ke pelanggan** (template menimpa fallback di kode) — slot ditambahkan bersama `${reboot_offer}`/`${langkah_manual}`. Template baru `rebootfu_*` (12 kunci). Test: `lib/__tests__/reboot-followup-service.test.js` (13) + `lib/__tests__/affirmative-parser.test.js` (41) + `message/handlers/states/__tests__/reboot-followup-state-handler.test.js` (12). Default **MATI** (deploy gelap): `config.rebootAssist.enabled=false`.

<a id="b116"></a>
### Fitur 2026-07-10 (Tindak lanjut saat PELANGGAN restart modemnya sendiri)

- **Fitur 2026-07-10 (Tindak lanjut saat PELANGGAN restart modemnya sendiri)** — lubang di fitur di atas: `evaluateRebootGate` mengembalikan `SUDAH_RESTART_SENDIRI` lalu `buildRebootOffer` keluar tanpa menjadwalkan apa pun, sehingga pelanggan yang sudah mencabut modemnya sendiri **tetap ditinggal diam** — persis kasus prod Tanjungharjo `6281244872662` (10 Jul: *"Ini baru dicoba cabut"* → intent `undefined` → nol balasan, padahal modem sudah inform ulang & PPPoE naik 1j51m). Tiga perbaikan: (1) **`message/handlers/connection-check-handler.js:buildRebootOffer`** kini memanggil `scheduleFollowupForReboot({reason:'restart_pelanggan'})` pada cabang `SUDAH_RESTART_SENDIRI` (TANPA reboot ulang; `rebootAt` job = waktu laporan, jadi acuan "inform sesudah restart") lalu membalas template baru `rebootfu_watch_self_restart`; (2) **`lib/loose-intent-matcher.js`** menambah aturan prioritas 2.7 — `saysAlreadyRestarted()` (dari `lib/affirmative-parser`) → `CEK_KONEKSI`, karena kalimat laporan restart tak punya `CONTEXT_TOKENS` jaringan sama sekali; (3) **kepemilikan keyword diperbaiki di `database/wifi_templates.json`**: `"cek koneksi"` & `"info koneksi"` dipindah dari intent `CEK_WIFI` → `CEK_KONEKSI` (matcher ketat mencocokkan keyword terpanjang di AWAL pesan, jadi pelanggan yang mengetik persis frasa yang bot ajarkan justru masuk handler cek-WiFi). Test: `message/handlers/__tests__/connection-check-self-restart.test.js` (4) + regresi di `lib/__tests__/loose-intent-matcher.test.js` (7).


<a id="b117"></a>
### Fitur 2026-07-11 (Digest anti-spam notifikasi pengajuan pembayaran)

- **Fitur 2026-07-11 (Digest anti-spam notifikasi pengajuan pembayaran)** — teknisi menagih dalam ronde (~7 pengajuan/menit) lalu admin bulk-approve sekaligus → spam DUA ARAH: owner dapat N notif detail "PENGAJUAN BARU", teknisi dapat N pesan "DISETUJUI". Pendekatan **ringkasan adaptif** (bukan jeda tetap): (1) **Sisi owner (submit)** — `routes/requests.js` (create) kini melewatkan notif ke **`lib/notification-digest.js`** (gate `config.paymentRequestDigest.enabled`): pengajuan PERTAMA ke tiap admin dikirim detail penuh SEGERA + membuka jendela `windowMinutes` (default 30); berikutnya dalam jendela DITAHAN lalu digabung jadi SATU ringkasan saat tick menutup jendela. Store durabel `database/notification-digest.json` + `startDigestScheduler` di `lib/app-runtime.js` (tick baca disk → selamat dari restart). Request tunggal tetap instan (jendela sunyi → tak kirim ringkasan). Kirim via `whatsapp-delivery-service` (never-throw, tolak `@lid`). (2) **Sisi teknisi (bulk)** — `services/payment-approval.service.js` mengumpulkan approved per-teknisi lalu `lib/approval-logic.sendTechnicianBulkSummary` kirim SATU ringkasan (bukan loop per-item); gate sama, digest OFF = perilaku lama per-item dipertahankan. **Perbaikan link mati:** `resolveAdminPanelUrl` (`routes/requests.js`) buang loopback `127.0.0.1` (mati dari HP) → pakai `paymentProof.adminUrl`/`adminPanelUrl` publik; kosong → instruksi singkat, bukan link mati. Template `payment_request_digest_summary` + `approval_teknisi_bulk_summary`. Test: `lib/__tests__/notification-digest.test.js` (8) + `services/__tests__/payment-approval-bulk-summary.test.js` (3). Default **OFF** (deploy gelap); LIVE 2 bot commit 210dbf7.

<a id="b118"></a>
### Fitur 2026-07-11 (CCTV sadar-modem — tahan broadcast saat mati listrik area)

- **Fitur 2026-07-11 (CCTV sadar-modem — tahan broadcast saat mati listrik area)** — akar keluhan: saat PLN padam, modem (ONU) & CCTV mati BERSAMA, tapi `lib/cctv-monitor.js` mengirim "CCTV Anda mati" ke tiap pelanggan seolah gangguan CCTV (bukti prod Dander 11-07 00:15:04 = **32 ONU dying-gasp serentak** → 00:15:40 netwatch lihat 3 CCTV turun → ~00:30 **3 pesan "CCTV mati" terpisah**; satu-satunya rem lama `massOutageThreshold` menghitung CCTV bukan modem & di prod `=0`). CCTV monitor & deteksi dying-gasp OLT dulu **dua subsistem saling buta**. Gerbang baru **`lib/cctv-power-outage-gate.js`** (`getContext` — baca-ONLY `database/olt_events.sqlite` via `repositories/olt-event.repository.listEvents`, hitung ONU `dying-gasp` BERBEDA di jendela ±sekitar waktu CCTV turun; cache TTL 30s; **never-throw → `{dgOnu:0}`**). Di `cctv-monitor.pollOnce` (sudah async) verdikt ditangkap per host PENDING via `ensureOutageVerdict` → disimpan di `pending.outageDgOnu` supaya **`onConfirm` tetap SINKRON** (nol perubahan alur tes lama). Bila `≥ powerOutageDgThreshold` (default **3**, samakan `oltLosBroadcast.clusterThreshold`) → broadcast pelanggan **DITAHAN** (status insiden `power_outage_suppressed`, TAK set `active` → tak ada notif "pulih" palsu) + 1 ringkasan admin `maybePowerOutageAlert` (penerima: `powerOutageAdminPhone`→`massOutageAdminPhone`→`getAdminJids()`; cooldown `powerOutageCooldownMs`, template `messagePowerOutage`). **FAIL-OPEN:** gate mati / OLT tak terbaca / error → `dgOnu`=0 → kirim spt biasa (gangguan CCTV asli tak pernah tertelan). Config **flat** di `cctv-monitor.DEFAULTS` `powerOutage*` (`powerOutageGateEnabled` default **true** → aktif begitu kode ter-deploy; prasyarat data: `oltEventLog.enabled` + syslog/scraper mengisi `olt_events.sqlite`). Test: `lib/__tests__/cctv-power-outage-gate.test.js` (5) + 7 kasus baru di `lib/__tests__/cctv-monitor.test.js` (total 55 hijau). Deploy 2 bot (pscp + restart).

<a id="b119"></a>
### Fitur 2026-07-12 (Broadcast Terarah — pilih template + pelanggan tertentu)

- **Fitur 2026-07-12 (Broadcast Terarah — generalisasi Broadcast Tagihan)** — halaman `/broadcast-tagihan` (URL & namespace endpoint DIPERTAHANKAN) diperluas jadi "Broadcast Terarah": admin memilih **template** + **filter penerima** lebih kaya, lalu kirim ke pelanggan terpilih. Owner `routes/admin-broadcast-tagihan-routes.js` kini punya registry **`DIRECTED_TEMPLATES`** (id→key+category): `tagihan`=`broadcast_tagihan` [responseTemplates], `tenggang`=`masa_tenggang_reminder`, `isolir`=`isolir_notification`, `welcome`=`import_welcome` [notificationTemplates]. Endpoint: **`GET /api/broadcast-tagihan/templates`** (baru, isi dropdown); **`GET .../default-template?template=<id>`** & **`POST .../preview`** (diperluas) me-resolve teks lintas-kategori via **`resolveDirectedTemplateText`** (responseTemplates→`renderResponseTemplate`; notificationTemplates→`lib/template-service.renderCategoryTemplate`; `!found`→fallback aman per-entri); **`GET .../customers?status=`** kini dukung `paid` / `isolir` (`u.status==='isolir'`, in-memory tanpa MikroTik) / `menunggak` (`services/arrears.service.getArrearsReadModel` berbasis ledger, dipetakan ke `global.users`, infra dibuang) selain `unpaid`(default)/`all`. **Pengiriman tetap REUSE `POST /api/broadcast`** (`mode:manual` + ids, `force_include_opt_out:true`) — tanpa jalur WA baru. Mesin `services/admin-broadcast.service.formatBroadcastMessage` **+2 slot lazy**: `${tanggal_isolir}` (`config.tanggal_isolir` default 16) & `${display_profile}` (`global.packages`) — additive, GAMAS/tagihan lama tak terpengaruh; resolver mesin (responseTemplates-only) TAK disentuh karena FE selalu mengirim `text` hasil prefill. FE `views/sb-admin/broadcast-tagihan.php` + `static/js/broadcast-tagihan.js` (dropdown template + filter status + prefill), `_navbar.php` relabel "Broadcast Terarah". Welcome pakai `import_welcome` (tanpa password); `customer_welcome` sengaja TIDAK diekspos. Invariant (`raf-invariants`): teks via template+fallback, delivery boundary (no raw Baileys), JID kanonik oleh mesin. Test: `services/__tests__/admin-broadcast.service.test.js` (+5 slot) & baru `routes/__tests__/admin-broadcast-tagihan-routes.test.js` (11).

<a id="b120"></a>
### Fitur 2026-07-13 (Resolver jalur baca PROFIL live freedns/lokaldns + monitor drift)

- **Fitur 2026-07-13 (Track A — presisi jalur tahan-banting)** — resolver jalur pelanggan `lib/customer-path-resolver.js` dulu 2 lapis (override RAF-STEER → peta pool hardcoded). Recon router Dander (read-only) menunjukkan **sumber kebenaran path normal = address-list `freedns` (base MNI) vs `lokaldns` (base GMDP)**, di-toggle per-subnet oleh owner (mis. 110k/125k `.61/.62` di-disable di lokaldns + enable di freedns → MNI). Resolver kini **3 lapis**: (1) OVERRIDE `RAF-STEER-<jalur>` per-individu menang; (2) **PROFIL live `freedns`→mni / `lokaldns`→gmdp** (entri `disabled` DIABAIKAN; menimpa peta pool) — sumber kebenaran, tetap presisi walau owner re-toggle; (3) peta pool `upstream-path-resolver.DEFAULT_PATH_POOLS` sbg **jaring pengaman** bila IP tak ada di profil; lalu gmdp; `null` HANYA saat router tak terbaca (fail-closed). Bridge `views/get_steering_lists.php` diperluas (RAF-STEER-* + freedns/lokaldns + flag `disabled`); `mikrotik.getSteeringAddressLists` tak berubah tanda tangan. Monitor baru **`lib/steering-drift-monitor.js`** (start di `app-runtime`, gate `config.steeringDriftMonitor.enabled` **default MATI**): tick harian + boot, `computeSteeringDrift` (peta pool vs profil live) → alert admin WA (via `whatsapp-critical-delivery.sendCritical` + `admin-recipients.getAdminJids`) DIREDAM tanda-tangan+24 jam di `database/steering-drift-state.json`. Template `steering_drift_alert` pakai fallback (belum ditambah ke JSON; monitor OFF). READ-ONLY router, nol tulis. Test: `customer-path-resolver.test.js` (15) + `steering-drift-monitor.test.js` (6), 28/28 hijau. LIVE-verified prod Dander: 70→gmdp, 61/62/71→mni. Deploy 2 bot (code-only, restart).

<a id="b121"></a>
### Fitur 2026-07-13 (Oper koneksi per-SEGMEN — Track B Slice B1: read/dry-run)

- **Fitur 2026-07-13 (Track B/B1 — kendali steering per-segmen, READ + PRATINJAU)** — lanjutan b109. Owner ingin "oper koneksi" **per-SEGMEN** (pool/subnet) sbg kendali utama (per-/32 ribet: recon membuktikan **semua IP PPPoE DINAMIS** — 66 secret nol static → /32 basi tiap reconnect, butuh reconciler; subnet stabil). `lib/customer-steering-service.js` (owner) +2 fungsi read-only: **`buildSegmentMap`** (tiap segmen pool→jalur base sekarang dari state list live `freedns`(mni)/`lokaldns`(gmdp), honor `disabled`, override `RAF-STEER` subnet menang, + hitung pelanggan aktif per subnet) & **`previewSegmentMove`** (DRY-RUN: operasi address-list PERSIS yg akan dijalankan B2 — enable target + disable/other, `add` bila entri belum ada, `noop` bila sudah; v1 target mni/gmdp; **TIDAK menulis router**). Segmen default (config `customerSteering.segments` override): reguler=.70→gmdp, 110k=.61/125k=.62/FREE=.71→mni. Endpoint READ-ONLY `GET /api/customer-steering/segments` + `GET /api/customer-steering/segments/preview?segment=&path=` (`routes/admin-customer-steering-routes.js`, `ensureAuthenticatedStaff`; jalan walau `customerSteering.enabled=false`/dorman krn cuma butuh `cfg.valid`). Test `lib/__tests__/customer-steering-segment.test.js` (8), total steering 21 hijau. **B2 (apply segmen, pilot) & B3 (override per-pelanggan /32 + reconciler) BELUM** — butuh tulis router, dikerjakan bertahap + konfirmasi. Deploy 2 bot (code-only, restart).

<a id="b122"></a>
### Fitur 2026-07-13 (Oper koneksi per-SEGMEN — Track B Slice B2: apply + verify + rollback)

- **Fitur 2026-07-13 (Track B/B2 — eksekusi pindah segmen, TULIS router aman)** — lanjut b121. `lib/customer-steering-service.js` +`applySegmentMove({segment,path,confirm,actor})`: alur AMAN preview → wajib `confirm:true` → jalankan ops address-list (`entry-add`/`entry-toggle` via bridge) → **VERIFY** (baca ulang `buildSegmentMap`, pastikan `currentPath===target`) → bila gagal **ROLLBACK otomatis** (`rollbackSegmentOps`, balik urutan mundur: toggle di-un-toggle, add di-remove) → audit insiden `steer_segment`. Butuh `cfg.valid` + confirm eksplisit; **TIDAK di-gate `customerSteering.enabled`** (itu utk reconciler /32; segmen = subnet stabil, tak perlu reconciler). Endpoint `POST /api/customer-steering/segments/apply` (admin/owner, `{segment,path,confirm:true}`). v1 target mni/gmdp (freedns↔lokaldns; presisi gratis krn RAF-STEER #21-24 jalan sebelum pool). Test +5 (confirm-gate, apply+verify+state, round-trip pulih, verify-gagal→rollback, noop) via router palsu STATEFUL; total steering 26 hijau. **Pilot FREE (6 pelanggan) round-trip live.** B3 (override per-pelanggan /32) menyusul. Deploy 2 bot (code-only, restart).

<a id="b123"></a>
### Fitur 2026-07-13 (Perintah WA `oper <segmen> ke <jalur>` — surface Track B)

- **Fitur 2026-07-13 (Track B — perintah WA "oper" per-segmen)** — surface admin utk `applySegmentMove` (b122). Admin chat `oper <segmen> ke <jalur>` (mis. "oper free ke gmdp") → **pratinjau** (dry-run + jumlah pelanggan aktif + langkah address-list) → balas **`ya`** → eksekusi + verify + rollback. Pola cermin `wan-switch.state` (switch koneksi). File baru `message/handlers/state-domains/oper-jalur.state.js` (`startOperJalur` set state `OPERJALUR_CONFIRM`; `handleOperJalurConversationState` konfirmasi via `lib/affirmative-parser.isAffirmative` → `applySegmentMove confirm:true`; gate PRESISI `resolveStaffRole` accounts.json, non-admin `handled:false` diam; NEVER-THROW; parse segmen alias 110/125/gratis + jalur mni/gmdp[/ih/sf→ditolak service v1]). Wiring: intent `OPER_JALUR` di `owner-admin-intents.js` (+ keyword `database/wifi_templates.json` "oper jalur/koneksi/segmen"+"oper", anchored `^kw`), prefix `OPERJALUR_→oper-jalur` di `conversation-state-owner-map.js`, branch di `conversation-state-router.js`; `customer-steering-service` export `getSegments`. Teks via `renderResponseTemplate` (8 key `oper_segment_*`/`oper_not_authorized` di `response_templates.json`, MERGE-KEY saat deploy). "batal" ditangani global (raf.js). Test `oper-jalur.state.test.js` (10); suite dispatch/state/steering 44 hijau. Deploy 2 bot (JS restart + merge JSON). B3 (override per-pelanggan /32) menyusul.

<a id="b124"></a>
### Fitur 2026-07-13 (Track B/B3 — `oper` diperluas ke per-PELANGGAN /32)

- **Fitur 2026-07-13 (Track B/B3 — oper per-pelanggan)** — perintah WA `oper` (b123) diperluas jadi SATU verb dua sasaran: `oper <sasaran> ke <jalur>` → jika `<sasaran>` cocok nama/alias SEGMEN ⇒ mode segmen (`applySegmentMove`, jalur mni/gmdp), else ⇒ **cari PELANGGAN** (`resolveCustomers`: id/pppoe eksak lalu contains nama/pppoe) ⇒ mode pelanggan (`steerCustomer`, jalur gmdp/mni/ih/sf). `message/handlers/state-domains/oper-jalur.state.js` ditulis ulang: `parseOperTarget` (jalur=token terakhir, sisanya target), `matchSegment`, `startSegment`/`startCustomer`, `handleConfirm` bercabang `userState.mode`. State `OPERJALUR_CONFIRM` + field `mode`. Pelanggan 0 cocok→notfound; >1→minta spesifik (pppoe/id). **Mode pelanggan DORMAN sampai `config.customerSteering.enabled=true`** (steerCustomer gate: IP dinamis butuh reconciler pengejar-IP `startCustomerSteeringReconciler`); mode segmen tetap jalan tanpa itu. Template +5 `oper_customer_*` + `oper_segment_invalid` di-update (sebut pelanggan) — MERGE-KEY UPSERT namespace oper_* (semua milik sendiri). Test `oper-jalur.state.test.js` jadi 13 (parse/resolve, 2 mode start, konfirmasi bercabang, notfound/multiple, gagal-nonaktif). **AKTIVASI + pilot 1 pelanggan: nyalakan customerSteering.enabled (mulai reconciler) — konfirmasi owner.** Deploy 2 bot (oper-jalur.state.js restart + merge RT).

<a id="b125"></a>
### Fitur 2026-07-13 (Web admin: kartu "Oper Segmen" — paritas dengan perintah WA)

- **Fitur 2026-07-13 (web Oper Segmen)** — halaman `/steering-pelanggan` (b109) + kartu **🔀 Oper Segmen (Pool → Jalur)** di atas tabel pelanggan, setara perintah WA `oper <segmen> ke <jalur>`. `views/sb-admin/steering-pelanggan.php` (tabel `#seg-rows`) + `static/js/steering-pelanggan.js` (`muatSegmen`/`renderSegments`/`operSegmen`): baca `GET /api/customer-steering/segments` (peta segmen→jalur+aktif), tombol per segmen → `GET .../segments/preview?segment=&path=` (dry-run di dialog `confirm`) → `POST .../segments/apply {confirm:true}` (verify+rollback di service). **Kartu ini JALAN tanpa `customerSteering.enabled`** (apply segmen gate `cfg.valid`; edit ikut `canEdit` peran dari endpoint) — beda dari kartu per-pelanggan/pool lama yang gate `enabled`. Refresh manual + interval 60s memuat ulang segmen juga. Aset .php/.js → TANPA pm2 restart (php-express render fresh + `rafAssetUrl` cache-bust). eslint bersih + `php -l` OK. Deploy 2 bot (pscp saja).

<a id="b126"></a>
### Fitur 2026-07-13 (Rapikan navbar: IA 5-seksi admin + CSS/JS sidebar ke statik bersama)

- **Fitur 2026-07-13 (navbar overhaul)** — sidebar admin dulu 9 grup RATA tanpa seksi induk, grup gemuk (Pembayaran 16 item campur bayar+keuangan+gaji-teknisi+komisi-agen, Jaringan 10), + CSS/JS inline terduplikasi 3 navbar (60KB `_navbar.php`). Rombak: **admin → 5 SEKSI induk** (`<div class="sidebar-heading">`) — *Operasional* (Pelanggan/Pembayaran/Layanan), *Keuangan & Bisnis* (Keuangan/Teknisi/Agen/Voucher), *Jaringan* (Infrastruktur/Monitoring), *Komunikasi* (Broadcast/Template), *Sistem* (Log & Audit/Pengaturan) = 13 grup, maks ~9 item/grup (dari 63 item, NOL link hilang — dibuktikan parity href). Menu di-generate deterministik dari katalog (preserve `isActive`/`isParentActive` + item tiket dinamis `$ticketPagePath` bersyarat). **CSS+JS sidebar diekstrak ke `static/css/sidebar.css` (630) + `static/js/sidebar.js` (153, search menu+filter heading+mobile; guard `if(searchInput)` → aman utk navbar tanpa search)** — kanonik = versi admin terlengkap; ke-3 navbar `<link>`/`<script src>` via `rafAssetUrl` (cache-bust). Teknisi (sudah bersekasi Operasional/Support/Jaringan) & agen (2 link) = block-surgery CSS/JS saja, menu utuh. Hasil: `_navbar.php` 1281→550, teknisi 556→179, agen 301→82; CSS/JS 3× inline → 1 file bersama. Aset .php/.css/.js → TANPA restart (php-express render fresh + cache-bust). `php -l` 3 file OK. Deploy 2 bot (pscp: 3 navbar + 2 statik).

<a id="b127"></a>
### Fitur 2026-07-13 (PSB terjadwal — Fase A/1: WA #jadwal → papan + notif grup)

- **Fitur 2026-07-13 (PSB #jadwal)** — jalur DAFTAR PSB "belum kepasang", terpisah dari pasang-langsung (`#PSB`). Teknisi/admin ketik `#jadwal` (teks; slot-filling Nama/HP/Dusun/Paket WAJIB + lokasi/catatan opsional, urutan bebas & one-shot bila caption lengkap) → buat record papan via `lib/psb-schedule-service.js` (tabel BARU `psb_schedule.sqlite`, lifecycle `menunggu→ditugaskan→terpasang→batal`, ref `PSB-<id>`, TIDAK menyentuh `psb_records` legacy web) status `menunggu` → notif grup announce "belum kepasang" (koordinasi manual dulu; AMBIL/TUGASKAN = Fase B). State domain `message/handlers/state-domains/psb-schedule.state.js` (owner `psb-schedule`, prefix step `PSBJADWAL_`) didaftarkan di `conversation-state-owner-map.js` + `conversation-state-router.js`; trigger `#jadwal`/`jadwal psb`/`psb baru` di `raf.js` (gated `psbIntake.enabled` + akun staf via `resolveAuthorizedStaff`). Parser `psb-caption-parser.js` +alias `catatan`. `getScheduleSummary` = sumber "belum kepasang" (menunggu+ditugaskan) & "terpasang bln ini". Test service(2)+state(5). **SISA:** Fase A/2 form+papan web; Fase B assign (AMBIL/TUGASKAN + DM); Fase C link `#PSB` pre-fill jadwal + tutup jadi terpasang + rangkuman dari papan. Deploy 2 bot (restart: raf.js/router/owner-map/state + service baru).

<a id="b128"></a>
### Fitur 2026-07-13 (P0 upstream: "Terdampak" alert + klaster komplain → data LIVE, buang peta statis)

- **Fitur 2026-07-13 (upstream affected LIVE)** — baris "Terdampak" di alert down/pulih (`upstream-quality-alerter`) + resolusi jalur klaster komplain (`complaint-signal-service`) dulu pakai peta STATIS (`upstream-path-resolver` CIDR / `paths[].affects`) yang **46% meleset** dari realita (prod 13-07: pool 62/125k sudah dipindah ke gmdp via `lokaldns`; static `{mni43,gmdp14}` vs live `{gmdp43,mni20}`, 29/63 divergen). **Owner baru `lib/upstream-affected-service.getAffectedSet(path)`** = himpunan pelanggan LIVE per jalur (PPPoE aktif × `customer-path-resolver.resolveCustomerPath` 3-lapis; **FAIL-CLOSED**: router tak terbaca → `confidence:'unknown'` count `null`, jangan karang angka). Alerter: `buildDirectionText`+`buildRecoveryRecap` panggil `buildAffectedLine(affected)` (live jumlah+nama / unknown jujur / **absen-dep → fallback `affects` statis** kompat-mundur). Komplain: `resolvePathForIp`→`resolveCustomerPath` (path `unknown` TETAP klaster — anti-diam). Poller `alerterDeps` TIDAK override `getAffectedSet` → `defaultDeps` hidup (prod live). Template TAK berubah (baris via slot `saran`/`rekap` yg sudah dirender — cek prod OK). Test: service baru(8) + alerter live(2) + komplain(mock live) — 124 hijau lintas domain. Verifikasi live prod: gmdp43/mni20/ih0/sf0 + invalid→unknown. Gate: `upstreamMonitor.alerts.enabled` (prod ON) + `complaintSignals.enabled`. Deploy 2 bot (restart: alerter+complaint + service baru). Bagian dari blueprint `docs/upstream-failover-blueprint.md` (P0 dari 4-lapis; L1 threshold-tuning & L3 failover-unification DITUNDA).

<a id="b170"></a>
### Fitur 2026-07-13 (PSB terjadwal — Fase A/2: form + papan WEB, paritas WA #jadwal)

- **Fitur 2026-07-13 (papan PSB web)** — paritas WEB untuk jalur DAFTAR PSB "belum kepasang" (b127). Halaman `/papan-psb` (`views/sb-admin/papan-psb.php` + `static/js/papan-psb.js`, route `routes/pages.js` staf teknisi+admin, link navbar admin grup Layanan) = form Nama/HP/Dusun/Paket + **3 BUKTI WAJIB** (foto KTP, foto rumah, lokasi) + kartu ringkasan (belum-kepasang/terpasang-bln-ini) + tabel papan. Foto reuse `POST /api/psb/upload-photo` (tempId+fieldname `ktp_photo`/`house_photo`); lokasi di-parse client-side (link Google Maps / `lat,lng` / geolocation). Backend `routes/psb-schedule-routes.js` (owner `/api/psb-schedule`, mount di `routes/api.js`): `POST` (guard `ensureStaff`; validasi identitas + `resolvePackage` + 3 bukti; `createRequest` status menunggu → notif grup), `GET /summary`, `GET ?status=`. **Notif grup = SATU builder `buildScheduleGroupNotif` di `lib/psb-schedule-service.js`** dipakai WA (#jadwal) & web → teks seragam (state `psb-schedule.state.js` di-refactor ikut builder). Kontrak web = kontrak WA (3 bukti wajib, tak bisa longgar diam-diam). Test route(7)+state(5)+service(2)=14 hijau. **SISA:** Fase B assign (AMBIL/TUGASKAN + DM); Fase C `#PSB` pre-fill jadwal + tutup terpasang + rangkuman dari papan; S3 pensiunkan web 3-fase legacy. Deploy 2 bot (route+service+api.js/pages.js RESTART; .php/.js/_navbar pscp saja).

<a id="b129"></a>
### Fitur 2026-07-14 (PSB terjadwal — Fase B/1: assignment WEB, admin TUGASKAN / teknisi AMBIL)

- **Fitur 2026-07-14 (assignment papan PSB — web)** — papan `/papan-psb` (b128) kini bisa **assign** (HYBRID): admin *TUGASKAN* ke teknisi tertentu, teknisi *AMBIL* (klaim) sendiri. Core di `lib/psb-schedule-service.js`: `assignSchedule({scheduleId,teknisiId,teknisiName,assignedBy*,mode})` — `mode:"assign"` (admin, boleh reassign dari `ditugaskan`) / `"claim"` (teknisi, HANYA dari `menunggu` = anti-serobot); guard `terpasang`/`batal` ditolak, idempoten bila teknisi sama, `reassignedFrom` terisi saat alih; +kolom audit `assigned_by_id/name` (ensureColumn idempoten). Notif = builder baru `buildAssignmentDm` (DM teknisi: detail + `mapsLink`) & `buildAssignmentGroupNotif` (grup, bedakan claim/assign) → seragam WA/web. Endpoint `routes/psb-schedule-routes.js`: `GET /api/psb-schedule/teknisi` (dropdown), `POST /api/psb-schedule/:id/assign` (admin-only, `resolveTeknisiById` dari `global.accounts`), `POST /api/psb-schedule/:id/claim` (self); DM+grup via `sendReply`+`normalizePhoneToJid` (best-effort, NEVER-THROW). UI `papan-psb.php`+`static/js/papan-psb.js`: kolom **Teknisi**+**Aksi**, peran dari `GET /api/me` (admin→select teknisi+Tugaskan/Alihkan, teknisi→Ambil saat menunggu). Diverifikasi LIVE via Preview (JWT admin+teknisi): assign & claim end-to-end (menunggu→ditugaskan, summary shift), light+dark OK. Test service(8)+route(7 assign)=+15 (total psb-schedule 26 hijau). **SISA:** Fase B/2 perintah WA `ambil`/`tugaskan`/`papan psb` (keyword MERGE-KEY); Fase C `#PSB` tutup→terpasang. Deploy 2 bot (service+route RESTART; .php/.js pscp).

<a id="b130"></a>
### Fitur 2026-07-14 (PSB terjadwal — Fase B/2: perintah WA ambil / tugaskan / papan)

- **Fitur 2026-07-14 (assignment papan PSB — WA)** — pelengkap web B/1 (#b129): teknisi/admin assign dari WA tanpa buka portal. Modul BARU `message/handlers/psb-assign-command.js` `handlePsbAssignCommand({text,staff,reply},_deps)` (deps-inject, NEVER-THROW) — 3 perintah one-shot (TANPA conversation state): `ambil PSB-<n>` (teknisi klaim → mode `claim`), `tugaskan PSB-<n> ke <teknisi>` (admin → mode `assign`, resolve teknisi dari `global.accounts`: username/id eksak lalu nama contains, tolak ambigu), `papan psb`/`daftar psb` (list menunggu+ditugaskan). Pakai core `assignSchedule` + builder DM/notif yang SAMA dgn web → seragam. Dipicu INLINE di `raf.js` (setelah blok #jadwal, sebelum #PSB) regex `^(ambil|tugaskan|papan|daftar)\s+psb\b` gated `psbIntake.enabled` + `resolveAuthorizedStaff` (non-staf diabaikan, tak bocor); DM+grup via `reply-runtime.sendReply`+`normalizePhoneToJid`. Balasan aktor via `renderResponseTemplate` — **+8 key `psb_*` di `database/response_templates.json`** (MERGE-KEY saat deploy prod, JANGAN overwrite): psb_cmd_format/assign_error/tugaskan_denied/tugaskan_ok/teknisi_notfound/teknisi_multiple/papan_empty/papan_list. Diverifikasi end-to-end (harness service+DB nyata, WA di-stub): ambil→ditugaskan+DM diri+grup, tugaskan→assign+DM teknisi+grup "oleh admin", papan→list. Test modul(11); wa-forbidden-imports tetap hijau (pakai reply-runtime, bukan Baileys). **SISA:** Fase C `#PSB` tutup→terpasang + rangkuman; S3 pensiun web legacy. Deploy 2 bot (raf.js+modul RESTART; response_templates.json MERGE-KEY).

<a id="b131"></a>
### Fitur 2026-07-14 (PSB terjadwal — Fase C/1: #PSB tutup jadwal → terpasang + rangkuman 1 sumber)

- **Fitur 2026-07-14 (tutup lingkaran papan PSB)** — install `#PSB` (wizard DM, `psb.state.js provision`) kini MENUTUP jadwal terkait & rangkuman grup 1 sumber. Service `lib/psb-schedule-service.js` +3: `markScheduleInstalled(id,userId)` (status→`terpasang`+`installed_user_id`/`installed_at`, idempoten), `findOpenScheduleForInstall({teknisiId,phone})` (auto-match jadwal terbuka by HP — **inti nomor dinormalisasi buang prefix 62/0** biar #jadwal 0812… cocok dgn #PSB 62812…; utamakan yg ditugaskan ke teknisi ini; ambigu→null), `recordWalkInInstall(...)` (install tanpa jadwal → catat record `terpasang` supaya SEMUA install terhitung). Di `provision()` (setelah upsert sukses, `newUserId=result.body.data.id`): jadwal eksplisit (`ctx.scheduleId`, utk C/2) → auto-match by HP → walk-in; lalu `getScheduleSummary` jadi angka notif grup ("Bulan ini: X terpasang · belum kepasang: Y") + reply teknisi sebut "Jadwal PSB-N ditutup". **`lib/psb-install-stats.js` DIHAPUS** (+testnya) — 1 caller saja, `recordInstall` diganti getScheduleSummary; dep `psb.state` swap recordInstall→scheduleService. Semua best-effort (tak menjatuhkan provisioning). Test service(+4 incl beda-prefix)+state(+1 auto-close)=63 hijau lintas 5 suite; wa-forbidden-imports hijau. Verifikasi harness DB nyata: jadwal tertutup lintas prefix + walk-in + summary. **SISA:** Fase C/2 pre-fill `#PSB PSB-<n>` (tarik data+foto jadwal, skip ketik ulang); S3 pensiun web legacy. Deploy 2 bot (service+psb.state RESTART; hapus psb-install-stats.js di prod).

<a id="b132"></a>
### Fitur 2026-07-14 (PSB terjadwal — Fase C/2: pre-fill #PSB PSB-<n> dari jadwal papan)

- **Fitur 2026-07-14 (pre-fill #PSB dari jadwal)** — teknisi ketik `#PSB PSB-<n>` (TEKS, tanpa foto) → wizard `psb.state.js` tarik data + REUSE 3 bukti dari papan (nol ketik ulang & nol upload ulang). `parsePsbScheduleRef(text)` (butuh `#psb` + `PSB-<n>` ber-HYPHEN — anti salah-picu paket "psb12"; diexport). `startLinkedSession(context, id)`: `getScheduleById` → seed `data.{nama←name,hp←phone_number,dusun,paket}` + `lokasi←lat/lng` + `ktpSaved/rumahSaved` dari `ktp_photo_path/house_photo_path` (via `reuseScheduleMedia`: copy best-effort ke folder sesi, flag=true krn jadwal WAJIB 3 bukti) + `ctx.scheduleId=id` → state `STEP_COLLECT`; bila `validatePsbData` sudah ok (jarang — jadwal TAK bawa WiFi) langsung `detectAndAskConfirm`, else checklist minta WiFi. Guard: jadwal not_found/terpasang/batal → tolak (tak buka sesi). `startPsbSession` cek ref di awal → `startLinkedSession`; sisanya alur foto normal. raf.js #PSB trigger: kondisi `type==='imageMessage'` → `(imageMessage || parsePsbScheduleRef)` supaya `#PSB PSB-<n>` boleh mulai via TEKS (isPsbTutorialTrigger `^…$` tak nyangkut). Saat YA → `provision` tutup jadwal EKSPLISIT via `ctx.scheduleId` (hook C/1), auto-match/walk-in TAK dipakai. Test +5 (parse/pre-fill/isi-WiFi→YA→tutup-eksplisit/notfound/terpasang) → 69 hijau 6 suite; wa-forbidden-imports hijau; verifikasi harness service+DB nyata (field mapping benar). **SISA:** S3 pensiun web 3-fase legacy. Deploy 2 bot (psb.state+raf.js RESTART).

<a id="b133"></a>
### Fitur 2026-07-14 (PSB terjadwal — S3: pensiun web PSB 3-fase legacy)

- **Fitur 2026-07-14 (pensiun web 3-fase)** — jalur web PSB lama (3-fase `psb_records`/`movePSBToUsers`, terpisah dari papan) DIPENSIUNKAN; jalur utama kini = Papan PSB (`/papan-psb`) + wizard WA `#PSB`. Prod data mati: Dander 0 record, Tanjung 1 record TEST "completed" Jan-2026 (nol in-flight → aman). **Halaman**: `/teknisi-psb`, `/teknisi-psb-installation`, `/teknisi-psb-setup`, `/psb-rekap` → REDIRECT 302 ke `/papan-psb` (`routes/pages.js`, bookmark lama tetap mendarat, bukan 404); `.php`/JS lama DIBIARKAN (dead, tak terjangkau — hapus fisik = follow-up). **Endpoint TULIS 3-fase** `POST /api/psb/{submit-phase1,submit-phase2,submit-phase3,update-status,delete-all}` → **410** via gate `router.use` di awal `api-psb-routes.js` (sebelum handler lama). **SHARED tetap**: `POST /api/psb/upload-photo` (dipakai papan), device/validate helpers (find-device/list-devices/test-connections/validate-pppoe). **Navbar**: teknisi grup "Pasang Baru (PSB)" 3 link legacy → 1 link "Papan PSB"; admin "Rekap PSB" dihapus (redundan dgn Papan PSB) + `/psb-rekap` dibuang dari `$layananPages`/isParentActive. Test retire(7: 5×410 + upload-photo bukan-410 + mount) hijau; diverifikasi server nyata (4 page→302 /papan-psb, 5 write→410, upload-photo→400, /papan-psb→200). GOTCHA: `navbar-shells.test.js` "teknisi navbar" MERAH tapi PRA-ADA (assert CSS/JS inline yg sudah diekstrak #b126, bukan regresi S3 — chip follow-up). **➡️ PSB terjadwal Fase A+B+C+S3 TUNTAS.** Deploy 2 bot (pages.js+api-psb-routes.js RESTART; 2 navbar pscp).

<a id="b134"></a>
### Fitur 2026-07-14 (Kustomisasi TAMPILAN report "data <isp>" — daftar terdampak lengkap + on/off seksi)

- **Fitur 2026-07-14 (report display config)** — report `data <isp>` (#b107) + baris "Terdampak" (#b128) kini bisa diatur leluasa dari panel `/upstream-quality` (#b108), **live tanpa restart**. Owner config `config.upstreamMonitor.report`, dinormalisasi di `upstream-quality-poller.getMonitorConfig` (`DEFAULT_REPORT`/`normalizeReport`, selalu balikkan report utuh): `affectedListMax` (0 = SEMUA, default — report on-demand yg ditarik admin) + `alertAffectedListMax` (default 5 — notif alert dorong) + `sections` (7 toggle: rincianArah/layananPopuler/polaLossPerJam/perArah24jam/gangguanTercatat/tujuhHari/insidenTerakhir, default ON). `buildAffectedLine` (`upstream-quality-alerter`) baca `cfg.report`: >5 nama → **daftar vertikal bernomor** (ganti "+40 lagi"), ≤5 → inline; `buildDirectionText` + jalur alert teruskan `{alert:true}` (cap terpisah report vs alert). `isp-data-summary` gate tiap seksi via `on(name)` (default true → **kompat mundur**; `rincianArah` off SKIP `getAffectedSet` = hemat probe MikroTik). `upstream-config-service`: `validateReport` (rentang + seksi dikenal, semua-atau-batal) + **deep-merge sections** di `applyUpstreamConfigPatch` + field `report` di `getEditableUpstreamConfig`; kartu "Tampilan Report" di `upstream-quality.php`/`.js` (input jumlah + checkbox seksi). Test +7 (alerter list/cap, isp-data toggle+kompat, validateReport+patch) → 48 hijau 3 suite; eslint bersih. Deploy 2 bot (poller+alerter+isp-data-summary+config-service RESTART; `.php`/`.js` pscp saja).

<a id="b135"></a>
### Fitur 2026-07-14 (Komisi marketing PSB — Fase 1: catat pemberi lead + nominal, deploy gelap)

- **Fitur 2026-07-14 (marketing PSB Fase 1)** — tiap pemasangan PSB kini bisa punya **pemberi lead / marketing** (peran KE-3, beda dari pendaftar `requested_by` & pemasang `assigned_teknisi`) + **nominal komisi manual**. Owner `lib/psb-schedule-service.js` +7 kolom `psb_schedule` (`marketing_type` teknisi|luar|none|NULL, `marketing_ref_id`/`ref_name`/`ref_phone`, `marketing_fee` INT, `marketing_status` NULL|pending|paid, `marketing_paid_at`) via `ensureColumn` idempoten. `setMarketing(id,{type,refId,refName,refPhone,fee})` = **sumber tulis tunggal** (normalizeMarketing; type=none bersihkan field+fee0; refId hanya teknisi; fee>0→status `pending`, else NULL; guard batal + kunci `paid`); `createRequest` terima `marketing` opsional (lewat setMarketing). `getScheduleSummary` +6 angka komisi (bulan ini total/teknisi/luar/count + pending total/count). **Capture**: WA `#jadwal` — parser `psb-caption-parser` alias `marketing` (`Marketing:`/`Pemberi lead:`/`Referral:`…) → `psb-schedule.state.finalizeSchedule` teruskan sbg `{refName}` (type NULL, admin klasifikasi/harga di web); web papan — `createRequest` terima `marketing_*` + **`POST /api/psb-schedule/:id/marketing` (ADMIN saja — uang)** set/koreksi (resolve nama+HP teknisi dari akun; validasi type/nama/fee≥0; not_found→404, already_paid→409). **GOTCHA init**: `CREATE INDEX ...(marketing_status)` WAJIB setelah `ensureColumn` selesai (Promise.all terpisah) — kalau digabung, index jalan sebelum ALTER → `no such column` (race). **Deploy gelap**: HANYA catat & rangkum, BELUM ada uang bergerak — teknisi→gaji & luar→kas menyusul **Fase 2**; #PSB-WA koreksi & Laporan Marketing PSB juga Fase 2/3. Test service +5, routes +7, parser +1 → 78 hijau 5 suite; eslint bersih. Deploy 2 bot (service+parser+psb-schedule.state+psb-schedule-routes RESTART). Lihat memori [[psb-marketing-commission]].

<a id="b136"></a>
### Fitur 2026-07-14 (Menu OWNER WA di-generate dari katalog — anti-basi + daftar ISP dinamis)

- **Fitur 2026-07-14 (menu owner generatif)** — menu owner WA (`menuowner`) dulu template STATIS tangan (basi ~1 thn: tak ada `data isp`/monitoring upstream/`oper`/PSB, malah ada hint MikroTik mentah `/ppp print`). Kini DI-GENERATE dari katalog perintah live. Owner baru `lib/owner-menu-builder.buildOwnerMenu` (NEVER-THROW, fallback pesan aman): filter katalog `database/wifi_templates.json` (via accessor baru `wifi_template_handler.getWifiTemplates`) ke kategori admin/owner, kelompokkan per DOMAIN (peta `INTENT_DOMAIN`/`KEYWORD_DOMAIN` di kode: Jaringan/ISP, Pelanggan, Keuangan, Voucher, MikroTik) — perintah tak-terpetakan → seksi **"Lainnya"** (ANTI-BASI: perintah owner baru otomatis muncul tanpa sentuh menu). **Daftar ISP DINAMIS**: di bawah `data isp` disisipkan `*data <key>*` per jalur dari `upstream-quality-poller.getMonitorConfig().paths` (gate `enabled` → kosong di bot tanpa upstream, mis. Tanjung). Link panel web per domain (Jaringan→`/upstream-quality`, Pelanggan→`/users`). Intro/outro via key editable `menuowner_intro`/`menuowner_outro` (fallback default di kode bila absen). `message/wifi.js exports.menuowner` panggil generator; **fallback ke template `menuowner` lama bila generator throw** (defensif). `cleanDesc` buang penanda peran ekor ("(Admin)"/"— Admin/Owner"). Test `owner-menu-builder`(3) + `raf-router`(9) hijau; eslint bersih; JSON valid. Deploy 2 bot (owner-menu-builder.js BARU + wifi_template_handler.js + wifi.js RESTART; `wifi_menu_templates.json` intro/outro OPSIONAL merge-key — generator jalan dgn default kode). Bagian **P0+P1** roadmap "serba lengkap owner"; P2 Owner Cockpit web menyusul.

<a id="b137"></a>
### Fitur 2026-07-14 (Komisi marketing PSB — Fase 2a: bayar pemberi lead LUAR via kas)

- **Fitur 2026-07-14 (marketing PSB Fase 2a)** — komisi pemberi lead **LUAR (makelar)** kini bisa DIBAYAR via KAS & tercatat sebagai pengeluaran. Lanjutan #b135 (Fase 1 = catat). Owner `lib/psb-schedule-service.js`: +2 kolom (`marketing_expense_id`, `marketing_payroll_id` utk Fase 2b) + `payMarketingExternal(id,{actor,paymentMethod,createExpense})`. `createExpense` **DI-INJECT** pemanggil (route pass `require('./expense-manager').createExpense`; test pass fake) → service tak hard-depend expense-manager (anti circular + testable tanpa global.db). **ANTI DOUBLE-EXPENSE**: optimistic lock `UPDATE ... SET marketing_status='paid' WHERE id=? AND marketing_status='pending'` (cek `changes`) — hanya pemenang bikin expense; `createExpense` gagal → **ROLLBACK** status ke pending (jangan tinggalkan paid tanpa bukti). Idempoten (paid → already_paid, tak dobel). Guard urut: not_found/cancelled/not_external(hanya luar; teknisi→payroll Fase 2b)/already_paid/no_fee(cek SEBELUM pending krn fee0→status NULL)/no_pending/no_expense_fn/race. Expense kategori `"marketing"` → `expense_entry` (debit=cash_out) di `financial_ledger` → **muncul di Rekap Keuangan otomatis**. `setMarketing` kunci diperluas: `paid` OR `settled`. Route `POST /api/psb-schedule/:id/marketing/pay` (ADMIN). UI papan-psb.js: tombol "Bayar kas" (hanya luar+pending+fee>0) + konfirmasi. Test service +5 (happy/idempoten/guards/rollback) + route +3 → 85 hijau 5 suite; eslint+php -l bersih. **SISA Fase 2b**: teknisi→payroll (komisi_marketing di technician_gaji, mirror lockCollectionEntriesToPayroll). Deploy 2 bot (psb-schedule-service+routes RESTART; papan-psb.js pscp).

<a id="b138"></a>
### Fitur 2026-07-14 (Owner Cockpit web — beranda ringkasan sekali-baca owner)

- **Fitur 2026-07-14 (Owner Cockpit)** — P2 dari roadmap "serba lengkap owner" (setelah menu owner WA generatif #b136): halaman `/owner` (checkRole admin/owner/superadmin) = beranda ringkasan sekali-baca. Owner baru `lib/owner-cockpit-service.buildCockpit()` = AGREGASI BEST-EFFORT per kartu (NEVER-THROW, tiap kartu try/catch TERISOLASI → satu sumber gagal ≠ halaman 500) dari service EXISTING, TANPA menulis: **Pemasukan** (`payment-finance-service.getPaymentReportForPeriod` → `summary.net_paid` periode + transaksi hari ini dari created_at), **Status ISP** (`upstream-quality-poller.buildStatusReport` → overall OK/WARN/DOWN/OFF + per-jalur; gate `getMonitorConfig().enabled`), **PSB belum-kepasang** (`psb-schedule-service.getScheduleSummary` → menunggu+ditugaskan), **Tiket aktif** (`global.reports` filter status≠selesai + belum-diambil), **Outage OLT** (`olt-los-broadcaster.getLosState` → activeIncidentCount/recovering/pending). Route `routes/admin-owner-cockpit-routes.js` `GET /api/owner/cockpit` (gate role) di-wire `admin-router.js`. View `owner-cockpit.php` + `static/js/owner-cockpit.js`: 5 kartu + STRETCHED-LINK drill-down (`/rekap-keuangan`, `/upstream-quality`, `/papan-psb`, `/admin-daftar-tiket`, `/olt-log`), auto-refresh 60s, degradasi anggun saat kartu ok:false. Nav link "Owner Cockpit" di `_navbar.php` bawah Dashboard. Test service(3: agregasi/ISP-off/ISOLASI-kegagalan) hijau; eslint+php -l bersih. **Diverifikasi LIVE prod** (mint JWT admin → HTTP 200; income Rp5.06jt/38tx, ISP 4 jalur OK, PSB/tiket/OLT 0, semua kartu ok:true). Deploy 2 bot (owner-cockpit-service + 2 route RESTART; owner-cockpit.php/.js + _navbar.php pscp). **➡️ Roadmap owner P0+P1+P2 TUNTAS.** Lihat [[owner-tooling-completeness]].

<a id="b139"></a>
### Fitur 2026-07-14 (Komisi marketing PSB — Fase 2b: komisi TEKNISI masuk payroll)

- **Fitur 2026-07-14 (marketing PSB Fase 2b)** — komisi pemberi lead **TEKNISI** kini masuk **slip gaji** (`technician_gaji`), melengkapi #b135 (catat) + #b137 (luar→kas). Owner sumber `lib/psb-schedule-service.js`: `getUnsettledMarketingForTeknisi(teknisiId)` (SEMUA pending teknisi ybs — **PERIODE-AGNOSTIK**, biar komisi yang di-harga belakangan tak nyangkut di payroll bulan tutup) + `settleMarketingToPayroll({teknisiId,payrollId})` (kunci pending→`settled` + tempel payroll_id; netTotal dari snapshot; idempoten). Integrasi `lib/technician-finance-service.js` (mirror rail `komisi_collection`): +kolom `komisi_marketing` di technician_gaji; **sumber marketing DI-INJECT** (`__setMarketingSourceForTest`, default `require('./psb-schedule-service')`) — anti coupling keras/circular + testable; baca/settle **DEFENSIF** (gagal → 0, TAK menjatuhkan payroll). `buildPayrollPayload` +`getMarketingPayableSummary` → gross += komisiMarketing; `createPayrollDraft`/`formatPayrollRow`/`updatePayrollDraft`/`getPayrollSummary` bawa `komisi_marketing`; `finalizePayroll` +`lockMarketingToPayroll` (settle, netTotal otoritatif) → gross/net. `financial-ledger.syncTechnicianPayrollPaid` metadata +komisi_marketing. Route `routes/gaji.js` `/kasbon-summary/:id` +`komisi_marketing` (preview modal konsisten dgn draft). UI `static/js/gaji-teknisi.js`: baris "Marketing PSB" (list + detail) + estimasi create/edit. Test +4 psb (getUnsettled/settle) & +2 finance (masuk draft+settle, defensif gagal-baca→0) → 26 hijau 2 suite; eslint bersih. **➡️ Komisi marketing PSB Fase 1+2a+2b TUNTAS** (SISA: Laporan Marketing PSB per pemberi lead = Fase 3 opsional). Deploy 2 bot (psb-schedule-service + technician-finance-service + financial-ledger + gaji.js RESTART; gaji-teknisi.js pscp). Lihat [[psb-marketing-commission]].

<a id="b140"></a>
### Fitur 2026-07-14 (Komisi marketing PSB — Fase 3: halaman Laporan Marketing PSB per pemberi lead)

- **Fitur 2026-07-14 (marketing PSB Fase 3)** — halaman **Laporan Marketing PSB** (`/laporan-marketing-psb`, checkRole admin/owner/superadmin) — melengkapi #b135/#b137/#b139. Owner `lib/psb-schedule-service.getMarketingReport({month,year,area})`: kelompokkan row `psb_schedule` (fee>0) per **PEMBERI LEAD** (teknisi by ref_id, luar/lainnya by nama) → `summary` (count/total_fee/pending/paid/settled per referrer) + `entries` (rincian per pemasangan) + `totals` (fee/count/pending/paid/settled/teknisi_fee/luar_fee); filter periode by tgl-aktivitas `COALESCE(installed_at,updated_at,created_at)` (month+year → per bulan; tanpa → all-time); `area` opsional. Route `GET /api/psb-schedule/marketing-report` (ADMIN, read-only). Halaman `views/sb-admin/laporan-marketing-psb.php` + `static/js/laporan-marketing-psb.js` (pola Laporan Komisi Agen: filter bulan/tahun/rentang + 4 kartu total + 2 tabel DataTables ringkasan/rincian, badge tipe & status). `routes/pages.js` +route render; navbar Layanan +link "Laporan Marketing PSB" setelah Papan PSB (+`/laporan-marketing-psb` di `$layananPages` active-detect, 4 tempat via replace-all). Test service +1 (group/totals/status, isolasi area) + route +2 (admin 200 + non-admin 403) → 49 hijau 2 suite; eslint + php -l bersih. **➡️ Komisi marketing PSB Fase 1+2a+2b+3 TUNTAS.** Deploy 2 bot (psb-schedule-service + psb-schedule-routes + pages.js RESTART; laporan-marketing-psb.php/.js BARU + _navbar.php pscp). Lihat [[psb-marketing-commission]].

<a id="b141"></a>
### Fitur 2026-07-14 (Owner Cockpit — pengayaan: tunggakan/pelunasan/MRR + kartu Pelanggan & Perlu Tindakan)

- **Fitur 2026-07-14 (cockpit diperkaya)** — Owner Cockpit (#b138) dari 5 → 7 kartu + metrik lebih dalam (tetap best-effort/terisolasi per kartu, `lib/owner-cockpit-service.buildCockpit`). **Pemasukan** +tunggakan (`services/arrears.service.getArrearsReadModel` → jumlah penunggak + Rp outstanding) + tingkat pelunasan (lunas/total) + **MRR/target** (`getEffectivePrice` pelanggan aktif non-infra). Kartu **Pelanggan** BARU (aktif/isolir/baru bln ini/**PPPoE online** dari `mikrotik.getPppStats` DI-CACHE 90s — mahal: spawn PHP+RouterOS). Kartu **Perlu Tindakan** BARU (bukti bayar pending `payment-proof.repository` + ganti paket `global.packageChangeRequests` + topup `saldo-manager.getPendingTopupRequests` + approval `global.requests`). Diperkaya: ISP +trafik agregat ↓/↑ Mbps (`paths[].wan.rx_mbps/tx_mbps`); PSB +terpasang bln ini menonjol +komisi (`komisi_bulan_ini`); Tiket +per-status +lama>24j; OLT +pelanggan offline (dari PPP, perkiraan). `getPppStats` diambil SEKALI (cache) dibagi kartu Pelanggan+OLT. Fix drill-down tiket `/admin-daftar-tiket`→`/admin/daftar-tiket`; label PPPoE online absolut (bukan `/total` yg bisa >100% krn akun infra). Test service 4 (metrik baru + isolasi PPP-fail & finance-fail) hijau; eslint bersih. **Diverifikasi LIVE prod** (HTTP 200: MRR Rp6,875jt, pelunasan 72% [38/53], trafik ↓112/↑10 Mbps, PPPoE online 62, 1 bukti bayar pending). Deploy 2 bot (`owner-cockpit-service.js` RESTART; `owner-cockpit.js` pscp statik). Lihat [[owner-tooling-completeness]].

<a id="b142"></a>
### Fitur 2026-07-14 (Owner Cockpit — tren MoM + offline presisi + redesign elegan dark-aware)

- **Fitur 2026-07-14 (cockpit P4)** — Owner Cockpit (#b138/#b141) dapat **tren vs bulan lalu** + **offline presisi** + **redesign**. TREN: `buildIncomeCard` ambil `net_paid` periode SEBELUMNYA → `prevNetPaid`+`trendPct` (badge ↑/↓% di kartu Pemasukan; best-effort). OFFLINE PRESISI: `buildCustomerCard` silang `getPppStats().data.inactive_users_list` × `global.users` → hitung pelanggan AKTIF (non-infra & non-isolir) yg sesi PPPoE-nya mati (BUKAN lagi total secret offline kasar yg ikut hitung isolir+infra); dipindah dari kartu OLT → kartu Pelanggan. REDESIGN: `static/css/owner-cockpit.css` BARU (grid responsif, icon chip ber-aksen via `color-mix`, angka besar, badge tren, progress bar pelunasan, stat rows) ganti look sb-admin border-left "jadul"; **DARK-AWARE** via design token `tokens.css` (`body.tk-dark` → `--dark-*`, konsisten [[design-tokens]]). `owner-cockpit.js` render ditulis ulang; `owner-cockpit.php` muat CSS + kontainer `.oc-grid`. Test service 4 (trend + offline presisi + isolasi kegagalan) hijau; eslint + php -l bersih. **Diverifikasi LIVE prod** (trendPct −21% [5,06jt vs 6,39jt], offline presisi 0, CSS terlayani `oc-card`×10). Deploy 2 bot (`owner-cockpit-service.js` RESTART; css/js/php pscp statik). Lihat [[owner-tooling-completeness]].

<a id="b143"></a>
### Fitur 2026-07-14 (Owner Cockpit fix — status tiket + route /admin/daftar-tiket 500 + timestamp)

- **Fitur 2026-07-14 (cockpit fix)** — dari feedback owner: (1) **Status tiket**: `owner-cockpit-service.DONE_TICKET` +`resolved`/`dibatalkan`/`ditutup` → tiket dibatalkan/resolved TAK lagi dihitung "aktif" (Tanjung dulu keliru "aktif 4" = dibatalkan 2 + resolved 1 + baru 1; BENAR = 1). (2) **Route `/admin/daftar-tiket` 500 DIPERBAIKI** (bug pra-ada): `routes/pages.js` render `sb-admin/admin-daftar-tiket.php` yg TAK ADA → "Failed to lookup view" → diganti `sb-admin/tiket.php` ("Ticket Management", include `_navbar` admin). Bug ini juga bikin link navbar "Tiket Support Admin" ($ticketPagePath=/admin/daftar-tiket) 500. **Deploy pakai SED bedah** (ganti string view saja) di prod agar tak mengusik route lain di pages.js (mis. /laporan-marketing-psb sesi paralel). (3) **Timestamp "Diperbarui"** dipindah dari bawah kartu ke header kanan-atas + ringkas (`jam · auto 60 dtk`), tombol "Segarkan". **Catatan (bukan bug cockpit):** `psb_schedule` KOSONG di 2 bot → "terpasang bln ini 0" benar dari tabel papan; install PSB di luar jalur papan/#jadwal tak terekam di situ (tercermin di kartu Pelanggan "baru bln ini"). Verifikasi LIVE: daftar-tiket HTTP 200 (2 bot), Tanjung tickets.active=1 {baru:1}, Dander 0. Test service 4 hijau. Deploy 2 bot (`owner-cockpit-service.js` + `pages.js`[sed] RESTART; `owner-cockpit.js`/`.php` pscp).

<a id="b144"></a>
### Fitur 2026-07-14 (Bukti bayar — aksi HAPUS untuk bukti PALSU, beda dari tolak)

- **Fitur 2026-07-14 (hapus bukti palsu)** — dari masalah nyata: foto KELUHAN pelanggan salah masuk antrian bukti bayar; "tolak" salah karena mengirim pesan "pembayaran ditolak" ke pelanggan yg tak pernah mengaku bayar. Aksi KE-3 **hapus** (bukan tolak): `services/payment-proof.service.deleteProof` → `repositories/payment-proof.repository.softDelete` (status `deleted`, BUANG file bukti, pertahankan metadata utk audit) — **NOL notif pelanggan & NOL sentuh ledger** (guard pending-only, idempoten). Web: tombol "Hapus (bukan bukti bayar)" di `static/js/konfirmasi-bayar.js` → `POST /api/konfirmasi-bayar/:id/hapus` (`routes/admin-konfirmasi-bayar-routes.js`). WhatsApp: `hapus`/`hapus BP-…`/`hapus <n>`/balas `hapus` di `message/handlers/payment-proof-admin-handler.js` (`executeDelete`/`dispatchAction`, `DELETE_WORDS=hapus|delete`, bare `^hapus$` empty-queue→handled:false) + peralihan aksi di `state-domains/payment-proof-admin.state.js`. Template admin baru `payment_proof_admin_delete_prompt`/`_ok` (tanpa template pelanggan). Test +19 (repo/service/handler/state) hijau; lint bersih. Terkait [[payment-proof-confirmation]]. **DEPLOY LIVE 2 bot 2026-07-14** (commit `b41fa0d`): 8 file inti prod=basis→timpa LF (pscp, verify sha+node --check), template merge-key aditif 2 key (851→853). pm2 restart, WA open, err bersih. Backup `/root/bot/raf-*/backups/20260714-freehapus`.

<a id="b145"></a>
### Fitur 2026-07-14 (Gratis bulan pemasangan — auto saat PSB + halaman /gratis-bulan-ini)

- **Fitur 2026-07-14 (free install month)** — pelanggan PSB baru: pemasangan + bulan pertama gratis, mulai bayar bulan DEPAN. (2a) AUTO saat PSB: gate `config.psbIntake.freeInstallMonth` (default OFF) → inject `free_first_month:true` ke `userData` di titik konvergen PSB — wizard `message/handlers/state-domains/psb.state.js` + grup `message/handlers/psb-group-intake.js` (fallback `global.config`), **memakai ulang** blok waiver eksisting `services/api-users/create-user-persist.js` (`applyFreeMonth`, best-effort, hanya paket bertagihan) → periode lunas TANPA masuk pemasukan, kebal isolir. (2b) UNTUK YG TERLANJUR: halaman baru `/gratis-bulan-ini` (`routes/pages.js` + `views/sb-admin/gratis-bulan-ini.php` + `static/js/gratis-bulan-ini.js` + entri navbar Pembayaran) — pakai `GET /api/payment-status/read-model` + `POST /api/payment-status/free` (keduanya SUDAH ada) untuk membebaskan per-baris & bulk, filter periode + belum-lunas. Tidak ada logika finance baru. Test +8 (psb.state/psb-group-intake) hijau; lint/php-l bersih; halaman diverifikasi render via preview (read-model 200, tabel terisi). Terkait [[free-month-waiver]] [[billing-lifecycle-stages]]. **DEPLOY LIVE 2 bot 2026-07-14** (commit `b41fa0d`, barengan #b144): `pages.js`+`_navbar.php` prod DRIFT NYATA (≠basis) → **patch bedah Python idempoten** (anchor route/nav byte-identik, sisip di situ; drift lain tak disentuh), BUKAN timpa; halaman `/gratis-bulan-ini` route 302 di kedua bot. **Toggle UI (commit `7541303`):** `psbIntake.freeInstallMonth` kini bisa di-on/off dari **`/config` tab Intake PSB** (select Nonaktif/Aktif; `config.php`+`config.js`+`admin-config-routes.js` GET/POST map + merge-key + `setConfig` hot-reload). Round-trip GET/POST/GET + render tab terverifikasi. **DEPLOY + AKTIFKAN LIVE 2 bot 2026-07-14** (3 file config prod=basis→timpa LF; `freeInstallMonth:true` disisip-bedah ke config.json sebelum `pppoeRealm`; pm2 restart): `RUNNING_freeInstallMonth=true` terverifikasi via GET /api/config kedua bot, WA open, err bersih. Backup `backups/20260714-freetoggle`. **Fitur 2a kini AKTIF** — tiap PSB baru otomatis gratis bulan pemasangan.

<a id="b146"></a>
### Bugfix 2026-07-14 (KRITIS: INSERT pelanggan membuang kolom senyap — created_at/lokasi/kredensial hilang)

- **Bugfix 2026-07-14 (users insert silent data loss)** — **AKAR:** `repositories/api-users.repository.js insertUserRecord` memakai daftar kolom+VALUES **manual berisi 20 kolom** → `created_at`, `updated_at`, `address`, `latitude`, `longitude`, `maps_url`, `username`, `password`, `registration_date` **TERBUANG tanpa error**. Objek LENGKAP tetap masuk snapshot in-memory (`create-user-persist.js:13 replaceUsersSnapshot`), jadi data **TAMPAK utuh** sampai bot restart — lalu **RAIB senyap** saat `global.users` di-reload dari SQLite (prod restart 7–13×/hari). Kena SEMUA pelanggan via admin panel / `#PSB` / import Excel (semua lewat `upsertUserFromAdminPanel`). **Lokasi GPS yang WAJIB dikumpulkan wizard #PSB pun hilang.** Bukti prod: `agus supriono` (Tanjung id 96, PSB 13-07) → 21 kolom terisi = PERSIS daftar INSERT lama + default DB; 27 kosong = PERSIS yang dibuang. **Lapis DB juga gagal:** tabel prod ber-`created_at TEXT DEFAULT NULL` (dibuat `lib/migration-helper.js:85` yang komentarnya klaim "Will use CURRENT_TIMESTAMP" padahal `default: null`) → tak ada yang menambal. **FIX:** (P1) `insertUserRecord` membangun INSERT **DINAMIS** dari whitelist `USER_INSERT_COLUMNS` ∩ kolom tabel (PRAGMA, cache per-instance) ∩ field yang ada; semantik lama dipertahankan (alias `phone`/`package`/`odp_id`, bool→0/1, `bulk` default `["1"]`, `notify_outage`, normalisasi `account_type`); `created_at`/`updated_at` dijamin di lapis app. (P2) `lib/database.js` trigger idempoten **`trg_users_stamp_timestamps`** mengisi `created_at`/`updated_at` bila INSERT meninggalkannya kosong — SQLite TAK punya `ALTER COLUMN` untuk mengubah default kolom lama; **WAJIB dipasang di DUA jalur** (tabel-baru DAN tabel-sudah-ada, karena blok CREATE TABLE di-`return` saat tabel ada → di prod tak pernah jalan). (P3) test kontrak **ROUND-TRIP ke SQLite `:memory:` nyata** (insert → SELECT → assert) — test LAMA justru MENGUNCI bug (assert array 20 nilai dari INSERT cacat, tak pernah menyentuh DB) → itu sebabnya lolos bertahun. 26 test hijau 3 suite; eslint bersih. **Diverifikasi LIVE:** trigger `ADA` di 2 bot. **Timestamp asli bisa dipulihkan dari `activity_logs`** (`CREATE|res=96|agus supriono|2026-07-13 09:42:02`); backfill = langkah terpisah (Dander 57 & Tanjung 12 tanpa `created_at`; mayoritas Dander hasil migrasi v2 tanpa log CREATE). Deploy 2 bot (RESTART). **PELAJARAN:** kode men-stamp, objek membawa, memori menampilkan — **tapi SQL membuangnya**. Jaga invariant di DB + uji ROUND-TRIP, jangan percaya "kan sudah di-set di kode".

<a id="b147"></a>
### Bugfix 2026-07-14 (Share lokasi PSB tak tersimpan — ctx.lokasi tak pernah dikirim ke create API)

- **Bugfix 2026-07-14 (PSB share-location silent drop)** — **AKAR:** wizard `#PSB` **MEWAJIBKAN** share lokasi (gate `psb.state.js:585` `v.ok && ktpSaved && rumahSaved && ctx.lokasi`), menangkapnya dgn benar (`:569-572` `locationMessage`/`liveLocationMessage` → `ctx.lokasi={lat,lng}`), bahkan **MENULISNYA ke file** (`:465` `uploads/psb/<sesi>/lokasi.json`) — **TAPI `provision()` tak pernah menyertakan `ctx.lokasi` pada `userData`** yg dikirim ke `upsertUserFromAdminPanel` → koordinat TAK PERNAH sampai ke tabel `users` (`latitude`/`longitude`/`maps_url` kosong). Dikumpulkan susah payah teknisi, lalu dibuang senyap. **FIX:** `provision()` kirim `latitude`/`longitude` (+ `maps_url` turunan) dari `ctx.lokasi`; terisi dari share WA (STEP_COLLECT) MAUPUN pre-fill jadwal papan (`startLinkedSession` seed `rec.latitude/longitude`) → kedua jalur tertutup. `create-user-validate` men-**SPREAD** `userData` (`:99 ...userData`) shg field terbawa ke `newUser`, lalu `insertUserRecord` (diperbaiki #b146) menulisnya ke DB — **rantai lengkap: tangkap → kirim → spread → INSERT**. Jalur WEB (`users.php`/`users.js`) memang sudah mengirim lat/long → ikut sembuh berkat #b146. **KENAPA LOLOS:** happy-path test hanya assert **SEBAGIAN** `userData` (name/phone/subscription/device_id/registration_mode) — lokasi TIDAK pernah dicek padahal test itu sendiri men-share lokasi. Kini dikunci (assert `latitude`/`longitude`/`maps_url`); 20 test `psb.state` hijau, eslint bersih. **PEMULIHAN:** lokasi PSB lama SELAMAT di `uploads/psb/**/lokasi.json` (agus supriono, Tanjung id 96: `{"lat":-7.1985798,"lng":111.8869848}`, mtime 13-07 16:42 WIB = activity_log 09:42 UTC) → backfill mungkin. Deploy 2 bot (RESTART). **PELAJARAN:** test yang meng-assert SEBAGIAN payload = lubang; yang tak di-assert bebas hilang.

<a id="b148"></a>
### Bugfix 2026-07-14 (Owner Cockpit: kartu isolir SELALU 0 — dihitung dari users.status yang tak pernah ditulis)

- **Bugfix 2026-07-14 (cockpit isolir card)** — **AKAR:** `lib/owner-cockpit-service.js isIsolir()` membaca `users.status === 'isolir'`, padahal **TAK ADA kode non-test yang pernah menulis nilai itu** (`status` selamanya `'active'`). Isolir sesungguhnya = **profil PPPoE MikroTik** (`config.isolir_profile` = `"ISOLIR"`) yang di-push cron `lib/cron/jobs/isolir.js`; `isolir-notification.js:51` pun menentukan "siapa terisolir" dari **profil LIVE**. Akibatnya kartu "Terisolir" **SELALU 0** (paling kentara sesudah tgl-16, saat isolir massal jalan), dan filter `!isIsolir` pada MRR **tak pernah memfilter apa pun**. **KENAPA LOLOS:** fixture test **mengarang** `status:"isolir"` — nilai yang tak pernah ada di produksi → suite hijau untuk perilaku yang tak pernah terjadi. **FIX:** `buildCustomerCard` jadi **async** & membaca peta profil LIVE lewat `IsolirService.buildProfileMap()` (**pakai-ulang 1 sumber kebenaran**, bukan query baru), cache 90s seperti `getPppStats` (RouterOS mahal). **FAIL-CLOSED:** MikroTik tak terbaca / `isolir_profile` kosong → `isolir` **dan** `offline` = **`null` ("—")**, BUKAN 0 palsu — "cannot observe" ≠ "observed good"; `offline` kini menuntut peta isolir juga, sebab tanpanya pelanggan yang SENGAJA dimatikan ikut terhitung gangguan (alarm merah palsu). MRR = SELURUH pelanggan tagih (terisolir tetap ditagih — justru diisolir karena belum bayar) → **angka MRR prod TIDAK berubah**, filter lama memang tak pernah aktif. `buildCockpit`: kartu async WAJIB lewat `card()` (di-await), **bukan** `safe()` yang sinkron — Promise yang lolos akan jadi `{}` di JSON (kartu kosong senyap, tanpa error). Frontend `static/js/owner-cockpit.js`: `isolir==null` → metrik "N pelanggan", bukan klaim "N aktif". **Test:** fixture **DIBALIK** — `users.status` sengaja BERTENTANGAN dgn profil (status "aktif" + profil ISOLIR, dan sebaliknya) → regresi ke `users.status` langsung merah; + null-on-failure, case-insensitive, `isolir_profile` kosong. 11 test hijau, eslint bersih. **DEPLOY LIVE 2 bot** (commit `8a2f549`; drift 0 — file prod persis basis; backup `/root/backups/20260714-isolir`; `node --check` + checksum sebelum restart): diverifikasi via `GET /api/owner/cockpit` — **Dander `isolir:1`** (total 53, aktif 52), **Tanjung `isolir:1`** (total 96, aktif 95, baru 1); sebelum fix **0 di kedua bot**. Terkait [[users-data-integrity-gotchas]] [[billing-lifecycle-stages]] [[income-audit-recipe]]. **PELAJARAN:** fixture yang mengarang nilai yang tak pernah ditulis produksi bukan menguji kode — ia **mengunci fiksi**. Kalau sebuah kolom jadi dasar keputusan, buktikan dulu ADA yang menulisnya.

<a id="b149"></a>
### Fitur 2026-07-14 (Aset jaringan: teknisi memetakan ODC/ODP dari WhatsApp + fondasi port/kapasitas/validasi dibereskan)

- **Fitur 2026-07-14 (network assets — WA intake + fondasi)** — **MASALAH:** peta jaringan **KOSONG TOTAL** di 2 bot prod (0 ODC, 0 ODP, 0 dari 154 pelanggan tersambung) — bukan karena teknisi malas: satu-satunya jalur input adalah halaman admin desktop dan di sana **teknisi di-403** (`admin-network-assets-routes.js:84`), sedangkan jalur WhatsApp untuk aset **NOL**. Orang yang paling tahu posisi ODP justru terkunci. **FONDASI (dibereskan SEBELUM data masuk — mengisi model rusak = membakukan angka salah):** (a) **`ports_used` punya 3 makna di 3 tempat** (boot=jumlah ODP anak, route POST=+1, `port-usage-updater.updateOdcPortUsage`=**SUM pelanggan anak**) → ODC 8-port bisa terbaca "64/8" dan maknanya **berubah sampai restart**; kini **DITURUNKAN dari data** (`recomputePortUsage`: ODP=jumlah pelanggan, ODC=jumlah ODP anak) di boot + tiap mutasi → tak ada penghitung yang bisa meleset karena tak ada penghitung. (b) **Kapasitas ODP tak pernah ditegakkan untuk PELANGGAN** (hanya untuk relasi ODC→ODP) → ODP 8 port bisa diisi tak terbatas, padahal "muat berapa lagi" itu alasan utama mencatat ODP. (c) **`connected_odp_id` tak pernah divalidasi** → typo diterima diam-diam (tanpa FK). (d) **Hapus pelanggan tak pernah mengurangi port**: penjaganya `if (user.odp_id && user.odp_port)` memakai **2 kolom yang tak pernah ada** (kolom aslinya `connected_odp_id`) — dan **test lama justru MENGUNCI panggilan cacat itu** (`toHaveBeenCalledWith("odp-1", 3, false)`, argumen masuk ke parameter yang keliru). (e) **Lubang auth:** `/network-assets` & `/map-viewer` tanpa `checkRole` (jatuh ke handler generik `pages.js:390`) → teknisi, agen, bahkan **PELANGGAN yang login** bisa merender halaman aset admin. (f) `generateAssetId` diduplikasi di `routes/admin-shared.js` → kini SATU implementasi di service. **PEMILIK BARU:** `lib/network-assets-service.js` (recompute/validasi/cari-terdekat/**createAsset SATU jalur** dipakai web MAUPUN WA). **LAPANGAN (WA):** state domain baru `message/handlers/state-domains/network-asset.state.js` (owner `network-asset`, prefix step `ASSET_`) — trigger `#ODC <nama>` / `#ODP <nama>` (gate akun staf, sama spt `#jadwal`; config `networkAssets.waIntake.enabled`, default **ON**). **Buang keputusan:** wajibnya cuma **nama + share lokasi**; induk ODC **dipilihkan otomatis** (terdekat, tinggal diiyakan — teknisi tak pernah mengetik ID aset), kapasitas ada default (8), foto box opsional (field `photo_path` BARU — aset dulu tak punya foto sama sekali); bot **MEMBUKTIKAN** hasil (ID, kapasitas, induk+jarak, link peta). Tak ada ODC dekat → **lanjut TANPA induk, jangan menebak**. **SAMBUNG PELANGGAN:** `#PSB` mengusulkan ODP terdekat (yang masih bersisa port) **menumpang layar konfirmasi SN modem yang sudah ada** → nol langkah tambahan tapi tetap lewat mata manusia; halaman **`/rapikan-odp`** (+ `GET /api/map/odp-tidy`, baca-saja) menyambungkan pelanggan lama ber-GPS — bot mengusulkan, **admin memutuskan** (borongan dibatasi <50 m), penetapan lewat `POST /api/users/:id` yang SUDAH ADA (validasi + hitung-ulang port ikut jalan; **tak lahir jalur tulis kedua**). Jarak garis lurus = **TEBAKAN** (kabel drop bisa ditarik ke ODP lain) → tak ada penetapan senyap. **BONUS:** begitu ODP terisi, 2 fitur yang sudah ada tapi **menganggur** ikut hidup — broadcast per-ODP (`admin-broadcast.service.js:134`) & auto-outage scope ODP (`auto-outage-rule.service.js:86`). **BUG TERTANGKAP SAAT VERIFIKASI LIVE:** `Number(null) === 0` membuat pelanggan tanpa GPS terbaca "punya GPS di (0,0)" → halaman melaporkan "0 tanpa GPS" padahal 18 (Dander) & 37 (Tanjung) belum dipetakan; fix `parseCoord` (null/""/0 = BELUM DISET) + 3 test anti-regresi. **DEPLOY LIVE 2 bot** (commit `3dfce39` + `e8b182d`; backup `/root/backups/20260714-odp`): 14 file timpa-aman (drift 0) + **`pages.js`/`_navbar.php` PATCH BEDAH** (prod TERTINGGAL — belum punya route `/laporan-marketing-psb` sesi lain; menimpa = ikut men-deploy fitur orang yang belum siap). Verifikasi live: `#ODP` cocok & `"odp penuh?"` TIDAK memicu, step `ASSET_`→`network-asset`, `/api/map/odp-tidy` 200 (Dander 40 ber-GPS/18 tanpa, Tanjung 59/37 — **persis cocok dgn SQL**), `/rapikan-odp` render 200. Test: 36 service+wizard, 215 hijau lintas 19 suite. Terkait [[psb-papan-terjadwal]] [[newbie-sdm-design-principle]] [[users-data-integrity-gotchas]]. **PELAJARAN:** fitur tak dipakai bukan berarti tak dibutuhkan — periksa dulu **pintunya terkunci atau tidak**. Dan jangan isi data ke model yang penghitungnya bermakna ganda.

<a id="b150"></a>
### Fitur 2026-07-14 (Menata ODP dari lapangan: sambungkan pelanggan lewat WA — nomor ATAU nama, tanpa butuh GPS)

- **Fitur 2026-07-14 (ODP tooling — fleksibilitas teknisi & admin)** — **AKAR MASALAH:** memetakan ODP (#b149) baru **SETENGAH** pekerjaan; setengah lagi = menyambungkan 154 pelanggan, dan di situ ada **dua tembok**: (1) **55 pelanggan TAK punya GPS sama sekali** (18 Dander + 37 Tanjung — angka live) → usulan-berdasarkan-jarak tak akan pernah menampilkan mereka; (2) **jarak itu TEBAKAN** — yang benar-benar tahu kabel drop ini masuk ke ODP mana adalah orang yang berdiri di depan boks-nya dan bisa MENELUSURI kabelnya. **KOREKSI RANCANGAN:** alurnya bukan per-pelanggan ("ODP-nya mana?") melainkan **PER-ODP** ("ODP ini isinya siapa saja?") — itu yang cocok dgn kenyataan lapangan. Dan **menyambungkan LEWAT NAMA tak butuh GPS sama sekali** → 55 pelanggan tanpa titik pun tertata. Jarak hanya dibutuhkan untuk MENGUSULKAN, bukan untuk MENYAMBUNGKAN. **BARU (WA, khusus staf, `message/handlers/state-domains/network-asset.state.js`, 8 step `ASSET_*`):** (a) sesudah `#ODP` tersimpan bot **LANGSUNG menawarkan** pelanggan terdekat yang belum ber-ODP (bernomor) → balas **nomor** / **SEMUA** / ketik **NAMA atau HP** → *satu kunjungan = petakan + sambungkan*; (b) **`#ISI <nama ODP>`** → mode sambungkan kapan saja; (c) **`#ODP`/`#ODC` dgn nama yang SUDAH ADA = EDIT** (perbaiki titik/kapasitas via `updateAsset`), **BUKAN duplikat** — salah share-lokasi bisa dikoreksi dari lapangan tanpa minta admin (duplikat ODP = peta yang berbohong); (d) **`odp <nama>` / `cek odp <nama>`** (tanpa `#`) → hunian `3/8` + siapa saja + link peta, tanpa buka laptop; (e) **`#LOKASI <nama/HP>`** → simpan titik rumah pelanggan, lalu **langsung ditawari ODP terdekat**. **PAGAR YANG SENGAJA DIPASANG:** nomor usulan **TIDAK PERNAH digeser** sesudah ada yang tersambung (yang sudah jadi ditandai ✅) — kalau dinomori ulang, teknisi yang mengetik "2" bisa menyambungkan **ORANG YANG SALAH**; nomor urut dibatasi **1–2 digit** supaya `08211112222` terbaca sebagai **NOMOR HP**, bukan nomor urut (bug nyata, tertangkap test); pemindahan antar-ODP **SAH** (koreksi lapangan) tapi **DIKATAKAN**, tak diam-diam; seluruh tulisan pelanggan lewat **`usersService.updateUserById` yang SUDAH ADA** → validasi kapasitas/typo + hitung-ulang port ikut jalan otomatis → **tak ada jalur tulis kedua**. **BUGFIX menumpang:** `repositories/api-users.repository.js updateUserRecord` punya whitelist kolom manual yang **kekurangan `maps_url`** → link peta **TERBUANG SENYAP** saat update (objek in-memory tetap membawanya → tampak tersimpan sampai restart). **Pola bug #b146 lagi** — kalau menambah kolom users yang ingin bisa diedit, DAFTARKAN di whitelist itu. **WEB:** `/rapikan-odp` dapat tab **"Tanpa GPS"** (cari nama/HP → pilih ODP manual; hunian tampil, yang PENUH tak bisa dipilih) — pelanggan ini sebelumnya **tak terlihat sama sekali** di halaman mana pun; `GET /api/map/odp-tidy` kini mengembalikan `tanpaGpsRows` + `odps`; Manajemen Aset menampilkan hunian `5/8` berwarna. **DEPLOY LIVE 2 bot** (commit `af50b70`; drift 0 di 8 file; backup `/root/backups/20260714-odp2`): trigger `#ODP`/`#ISI`/`#LOKASI`/`odp <nama>` semua cocok; **pencarian nama diuji pada DATA PRODUKSI NYATA** (Dander "Muji"→3 cocok dari 58; Tanjung "Pak"→2 dari 96); `/api/map/odp-tidy` 200 dgn `tanpaGpsRows` 18 (Dander) & 37 (Tanjung). Test: 47 (service 32 + wizard 26) hijau, 226 hijau lintas suite terdampak. Terkait [[network-assets-odp-mapping]] [[newbie-sdm-design-principle]] [[users-data-integrity-gotchas]]. **PELAJARAN:** kalau sebuah alur terasa berat, periksa apakah **arah alurnya** yang terbalik — bukan tambah fitur di arah yang salah.

<a id="b151"></a>
### Bugfix 2026-07-15 (Intake bukti bayar: foto keluhan/koordinasi salah-tangkap jadi "bukti pembayaran" — gerbang uang + jejak admin durabel)

- **Owner:** `lib/payment-proof-intake-policy.js` (BARU, murni — SATU tempat keputusan "foto ini bukti bayar atau bukan") + `lib/chat-activity-tracker.js` (kini DURABEL) + gerbang di `services/payment-proof.service.js` (`evaluateIntake`/`confirmProof`). **AKAR (2 bug, insiden prod Dander 14-07 Lapak RT 15):** hook foto-yatim `message/raf.js` memutuskan dari **KETIADAAN bukti-lawan** ("tak ada `userState.step`", "tak ada jejak admin di Map RAM") — dan (1) jejak admin cuma **Map in-memory** → prod restart 7–13×/hari, foto koordinasi CCTV masuk **19 detik** sesudah restart saat Map kosong → gerbang admin-aktif buta; (2) snapshot tagihan **sudah dihitung** (`outstanding:0`, `isFullyPaid:true`) bahkan dicetak ke admin sebagai label, **tapi tak pernah dipakai sebagai gerbang** → pelanggan nol-tunggakan tetap dibalas "kalau ini bukti pembayaran…". **FIX:** klasifikasi pindah ke fungsi murni `classifyIncomingPhoto` → `capture|complaint|neutral|silent`, diputuskan dari **bukti DURABEL** (tagihan terbuka / isolir = ada yang bisa dilunasi; jejak admin & komplain kini di tabel `chat_activity` di `message_logs.sqlite`, di-`hydrate` saat boot SEBELUM WA connect, fail-closed selama warm-up). Media **hanya diunduh** pada `capture`; `neutral`/`complaint` balas ack **TANPA kata "pembayaran"**. `confirmProof` menolak `no_outstanding` (dulu tetap ditandai `confirmed` walau ledger `no_change` → antrian berbohong). Notif admin: saran aksi **ikut bukti** (`payment_proof_admin_hint_due` vs `_nodue`) — kini iklankan **ok/tolak/HAPUS** (aksi `hapus` #b144 sudah ada tapi tak pernah disebut di WA; web punya 3 tombol, WA cuma 2). **Diskriminator echo-bot TAK disentuh:** `sendMessage` pakai `generateMessageIDV2` (ID 22 char) → tersaring guard `<32` raf.js:195 SEBELUM `noteAdminOutbound`, jadi balasan bot sendiri tak mencemari jejak (dibuktikan: foto ke-3 chat yg sama tertahan). **Gate:** `config.paymentProof.enabled` (default ON) + `adminQuietMinutes`/`complaintQuietMinutes` (default 15). Persistensi jejak **SENGAJA tak digate `messageLogging`** (itu flag korpus AI, bukan sinyal pengaman). **Test:** korpus prod 13 record diputar-ulang (11 bukti asli WAJIB tetap capture — 11/11 lolos; 2 salah-tangkap ditolak; insiden 14-07 direproduksi persis) + handler/service/tracker/repo = 9 suite hijau, lint 0 error. Template: 6 key baru + `payment_proof_admin_notification` dapat slot `${saran}` (merge-key aditif). Terkait [[payment-proof-confirmation]] [[customer-chat-corpus]]. **PELAJARAN:** gerbang yang memutuskan dari ketiadaan bukti-lawan diam-diam merosot jadi "izinkan semua" tiap kali sinyalnya hilang — putuskan dari **bukti-dukung yang durabel**, dan "tak bisa mengamati" ≠ "aman mengklaim".

<a id="b152"></a>
### Bugfix 2026-07-15 (Gerbang bukti bayar #b151: pengecualian ISOLIR belum berlaku di cabang caption → foto "internet mati" dari pelanggan terisolir tertelan jadi keluhan)

- **Owner:** `lib/payment-proof-intake-policy.js` (`classifyIncomingPhoto`, step 4). **AKAR:** pengecualian-isolir cuma terpasang di cabang keluhan-DURABEL (step 5, guard `!isolir`), TAPI cabang caption (step 4) tak punya guard itu → pelanggan **isolir** yang kirim foto ber-caption "internet mati" (tanpa kata bayar) dibalas `complaint` → **bukti transfer asli ditelan** (false-negative jalur uang) — persis yang diperingatkan komentar step 5: buat pelanggan terisolir, "mati" ITU akibat menunggak, foto susulan justru sangat mungkin bukti bayar. **FIX:** tambah `!isolir` di kondisi step 4 → pengecualian isolir kini berlaku di KEDUA cabang; HANYA kasus itu yang berubah (non-isolir & yang menyebut bayar tak tersentuh — hand-trace + suite). **Test:** +1 (isolir+caption "mati" → CAPTURE); intake-policy 30 + handler/service 29 hijau, lint 0. Ditemukan saat audit sesi-paralel (smoke gerbang uang di kode ter-deploy: 9/10 → celah ini). Terkait [[payment-proof-confirmation]]. **PELAJARAN:** kalau memberi pengecualian di satu cabang keputusan, periksa SEMUA cabang yang berbagi alasan sama — pengecualian setengah-jadi = kebijakan tak konsisten yang diam-diam menelan kasus yang mestinya lolos.

<a id="b153"></a>
### Fitur 2026-07-18 (Survei kepuasan pelanggan bulanan / CSAT — kirim rating 1-5, tangkap balasan DURABEL, rekap owner; deploy GELAP)

- **Owner:** `lib/cron/jobs/rating-survey.js` (kirim + digest) + `repositories/csat.repository.js` (`database/csat.sqlite`, 1 tabel `csat_surveys`) + `lib/csat/csat-survey-service.js` (state-machine tangkap balasan) + `lib/csat/rating-parser.js` (parser murni) + `lib/csat/csat-time.js` (util tanggal SQL). **TUJUAN:** owner ingin dengar keluh-kesah SEMUA pelanggan → survei proaktif bulanan (sebelumnya NOL CSAT/rating — greenfield; [[smart-cs-audit-2026-07]]). **ALUR KIRIM:** cron bulanan (`schedule_rating_survey` default tgl 22, `schedule_rating_digest` tgl 27 di `cron.json`; hindari tanggal tagihan 1/11/12 & jam ramai 19–21) kirim template `rating_survey` (nilai 1-5) ke pelanggan LAYAK — tiru filter `lib/cron/jobs/isolir.js:89`: kecuali `isInfrastructure`/paket-whitelist-gratis/belum-bayar(`onlyPaid`)/tanpa-HP/tanpa-PPPoE/sudah-disurvei-periode-ini — lewat `sendQueueWithRetry` (blip-safe, 1 item=1 nomor). **TANGKAP BALASAN DURABEL (kunci):** hook tipis di `message/raf.js` (sesudah `originalReply`, sebelum interceptor; gate `config.csatSurvey.enabled`, non-throwing, balas via `originalReply`/sendReply — BUKAN sendMessage mentah) → `csatService.handleInboundReply` cek survei aktif **by `user.id` di csat.sqlite**, BUKAN conversation-state in-memory (tahan restart 7–13×/hari — [[reboot-followup-gap]]). Tahap-1 rating (`rating_parser` toleran: "4 mas"/emoji/dialek Jawa; `unclear` → balik `false`, TIDAK membajak pesan sungguhan) → tanya komentar; tahap-2 komentar dalam `commentWindowMinutes` → simpan, di luar jendela → finalisasi & lepaskan pesan ke pipeline normal; `stop` → optout. **REKAP:** digest bulanan ke `getAdminJids` (avg/response-rate/detractor≤2/komentar/non-responder) — teks INTERNAL owner dibangun inline (bukan pesan pelanggan; 5 template pelanggan tetap ditemplatekan di `message_templates.json`, merge-key). **GATE:** master `config.csatSurvey.enabled` (DEFAULT OFF → deploy GELAP; aktivasi = flip 1 flag) + knob `responseWindowDays`/`commentWindowMinutes`/`onlyPaid`; cron keys di-whitelist `routes/admin-config-routes.js`. **Test:** parser 45 + repo 6 + service 6 + cron 6 = 64 hijau; `wa-forbidden-imports` + `cron-whatsapp` tetap hijau; lint 0. Terkait [[smart-cs-audit-2026-07]] [[customer-journey-audit]] [[customer-facing-ai-assistant]] [[customer-chat-corpus]]. **PELAJARAN:** balasan survei WAJIB durabel (state in-memory mati tiap restart) & parser WAJIB conservative (`unclear` lebih baik daripada membajak keluhan/perintah sungguhan).

<a id="b154"></a>
### Fitur 2026-07-18 (CSAT #b153 GO-LIVE 2 bot: alert detractor real-time + nomor HP di rekap + mode uji testPhone)

- **Owner:** `lib/csat/csat-survey-service.js` (+`notifyOwnerDetractor`/`isDetractorAlert`), `lib/cron/jobs/rating-survey.js` (+testPhone), `message/raf.js` (hook +bypass testPhone). **P1 — alert detractor real-time:** saat pelanggan beri skor ≤`detractorMaxScore` (default 2), owner langsung dialert via `getAdminJids`+`sendCritical` (retry/dead-letter, never-throw) — heads-up saat rating + teruskan komentar saat masuk. NOTIFIKASI saja (bukan auto-tiket) sesuai pilihan owner "rekap ke owner", hanya real-time. Gate `config.csatSurvey.alertDetractor` (default true). **P2:** rekap digest detractor/komentar kini sertakan No HP (owner bisa langsung hubungi). **MODE UJI `csatSurvey.testPhone`:** cron kirim HANYA ke 1 nomor (dedup dilewati) + hook tangkap-balasan izinkan nomor itu menembus gerbang `!isOwner/!isTeknisi` — utk uji end-to-end tanpa blast; kosong=normal, di-log "MODE TES". **GOTCHA:** nomor owner=staf (isTeknisi) → hook sengaja lewati staf (benar); uji butuh testPhone + daftar pelanggan sementara. **GO-LIVE:** `config.csatSurvey.enabled=true` + cron `status_rating_survey/digest=true` (`0 9 22`/`0 9 27`) di **2 bot**; kirim perdana tgl 22. **TERUJI LIVE end-to-end** (pilot Dander: rating 1→komentar "lemot youtube"→done→rekap→alert; data tes dibersihkan). Prod 3 file == HEAD (md5), backup `/root/bot/backups/csat-golive-*`. Test: parser 45+repo 6+service 9(+3 P1)+cron 7 = 68 hijau; wa-forbidden-imports hijau; lint 0. Terkait [[csat-survey-feature]] [[smart-cs-audit-2026-07]]. **PELAJARAN:** detractor real-time itu payoff nyata — keluhan skor-1 yang mengendap sampai digest bulanan = risiko churn; alert cepat (bukan auto-aksi) menghormati pilihan owner sekaligus menutup jendela waktu.

<a id="b155"></a>
### Fitur 2026-07-18 (Halaman web admin Survei Kepuasan `/survei` — rangkuman CSAT on-demand)

- **Owner:** `routes/admin-csat-routes.js` (BARU, `GET /api/owner/csat?period=YYYY-MM` → report+tren+detractor+komentar+non-responder; gate `ensureAuthenticatedStaff`+role-whitelist admin/owner, pola `admin-owner-cockpit-routes`) + `repositories/csat.repository.getTrend` (agregat per periode) + view `views/sb-admin/survei.php` + `static/js/survei.js` + `static/css/survei.css` (tiles skor/response-rate + sebaran 1-5 + tabel detractor **+No HP** / komentar / non-responder + tren; dark-aware design token). Registrasi: `routes/admin-router.js` (registerAdminCsatRoutes) + `routes/pages.js` `/survei` `checkRole(admin/owner/superadmin)` + link "Survei Kepuasan" di `views/sb-admin/_navbar.php`. **KENAPA:** rangkuman CSAT selama ini hanya digest WA tgl 27 + alert detractor; admin perlu lihat on-demand + histori. Auth JS via cookie `token` same-origin (mount-render, tak ada token di JS). **Verified LIVE 2 bot:** `/api/owner/csat` 200 `{success,data}` (kosong s/d tgl 22) + `/survei` render bersih (no PHP error), 8 file == HEAD (md5), backup `/root/bot/backups/csat-page-*`. **GOTCHA verifikasi:** accounts di `database/accounts.json` (BUKAN root), admin id=2; mint JWT `{id,role}` tapi middleware `http-auth-bootstrap:132/152` LOOKUP akun by `decoded.id` & ambil role dari AKUN (payload role cuma pemicu cabang); port Dander 3010 / Tanjung 3200 (HTTP prod tak di-tunnel → verifikasi curl localhost). Test getTrend hijau; lint 0; PHP `-l` OK. Terkait [[csat-survey-feature]] [[preview-authenticated-pages]].

<a id="b156"></a>
### Fitur 2026-07-18 (Anti-ban broadcast: jitter + batch + cap + circuit-breaker + ledger di `sendQueueWithRetry`)

- **Owner:** `lib/cron/wa-send-queue.js` (extend `sendQueueWithRetry` + `resolveBroadcastGuard`) + `lib/broadcast-ledger.js` (BARU, `database/broadcast_ledger.sqlite`). **KENAPA:** semua broadcast (CSAT survei, reminder, masa-tenggang, isolir-notif) lewat SATU helper; owner tanya cara hindari ban selain delay 2 dtk — dan delay saja bukan lever utama. **5 fitur OPT-IN (`config.broadcastGuard.enabled`, default OFF → helper INERT byte-identik):** (1) **jitter** delay (`messageDelayMs + random×jitterMaxExtraMs`), (2) **jeda batch** tiap `batchSize` pesan (`batchPauseMs`), (3) **cap** `maxPerRun` → sisa `deferred` (default 0=off; caller yg pakai wajib handle deferred), (4) **circuit-breaker** `breakerThreshold` gagal-beruntun → STOP + sisa dilaporkan gagal (bukan hilang) + **alert admin** via getAdminJids/sendCritical, (5) **ledger** `broadcast_ledger.sqlite` (skip nomor terblokir/opt-out + opsional min-gap anti over-messaging lintas kampanye; **FAIL-OPEN** — error ledger TAK PERNAH menahan kirim). Guard dibaca dari `o.guard` ATAU fallback `global.config.broadcastGuard` → semua cron auto-dapat TANPA edit caller. **INVARIANT DIJAGA:** guard OFF (tanpa config) → return `{sent,failed,rounds}` PERSIS (extras deferred/skipped/breakerTripped hanya saat guard ON) → 9 test existing utuh. **AKTIVASI:** `broadcastGuard.enabled=true` di config prod. **CATATAN JUJUR:** ini menurunkan risiko signifikan TAPI Baileys (klien tak resmi) tak pernah 100% aman — jaminan hanya WA Business Cloud API resmi. Test: wa-send-queue 16 (9 existing + 7 guard) + broadcast-ledger 6 hijau; cron caller (rating-survey/isolir-notif/reminder/grace) 17 hijau; lint 0. Terkait [[wa-cron-send-retry]] [[csat-survey-feature]]. **PELAJARAN:** anti-ban = "terlihat manusiawi + diinginkan" (jitter/batch/variasi/dua-arah/opt-out/volume-cap), bukan sekadar delay lebih lama; dan pengerasan jalur kritis WAJIB opt-in inert supaya kontrak lama tak berubah.

<a id="b157"></a>
### Fitur 2026-07-18 (Spintax: variasi isi pesan `{a|b|c}` anti-ban di render template)

- **Owner:** `lib/spintax.js` (BARU — `expandSpintax`/`countVariants`) + hook post-render di `lib/templating.renderTemplate` & `lib/response-template-helper.renderResponseTemplate`. **KENAPA:** isi identik massal = tanda tangan spam + terasa basi; owner mau tiap pelanggan tiap bulan dapat variasi efektif. **CARA:** sintaks `{a|b|c}` → pilih ACAK satu opsi tiap render (dukung nesting `{A {x|y}|B}`). **KUNCI KESELAMATAN:** hanya grup ber-PIPE diekspansi (regex `\{[^{}]*\|[^{}]*\}`) → `${slot}` (tanpa pipe) TIDAK tersentuh; ekspansi SETELAH slot terisi (tak ada `${}` tersisa). **INERT** bila template tak pakai sintaks → SEMUA template lama byte-identik (diverifikasi: 0 template existing memakai `{a|b}`). 5 template CSAT ditulis ulang spintax (rating_survey/followup_low/high/thanks/optout — banyak kombinasi greeting/ajakan/penutup). Test spintax 8 (termasuk `${slot}` tak tersentuh + nesting) + CSAT render tetap hijau; lint 0; render live 4× tervariasi benar. Terkait [[broadcast-anti-ban]] [[csat-survey-feature]]. **PELAJARAN:** variasi isi melengkapi jitter/batch utk anti-ban; ekspansi post-render + syarat-pipe menjaga `${slot}` aman tanpa menyentuh template lain.

<a id="b158"></a>
### Fitur 2026-07-18 (Opt-out survei balas-STOP survey-scoped + validasi onWhatsApp anti-ban)

- **Owner:** OPT-OUT → `repositories/csat.repository` (tabel `csat_optout` + setSurveyOptout/isSurveyOptedOut/listOptouts/clearOptout) + `lib/csat/csat-survey-service` (cabang optout catat permanen) + `lib/cron/jobs/rating-survey` (skip opted-out) + template `rating_survey` (baris ajakan STOP). onWhatsApp → `lib/whatsapp-gateway.isRegisteredOnWhatsApp` (FAIL-OPEN `null`) + `lib/broadcast-ledger` (cache `wa_registered`/`wa_checked_at`) + `lib/cron/wa-send-queue` (cek pra-kirim). **OPT-OUT (owner pilih SURVEY-SCOPED + trigger BALAS STOP):** pelanggan balas STOP → `setSurveyOptout` PERMANEN di csat.sqlite; cron survei berikutnya SKIP; **HANYA survei — tagihan/isolir TAK tersentuh** (opt-out di domain CSAT, dicek cron survei saja; tagihan tak lewat sini). Survei sertakan baris halus "Ketik *STOP* kalau tak mau survei lagi" — justru menekan orang menekan Blokir/Lapor-spam (sinyal ban TERKUAT). **onWhatsApp (opt-in `config.broadcastGuard.validateOnWhatsApp`, default OFF):** pra-kirim cek nomor terdaftar; tak terdaftar→SKIP (kurangi sinyal gagal-kirim); `null` (tak bisa cek)→FAIL-OPEN tetap kirim; hasil di-cache di ledger (re-check `waRecheckDays`=30 hari). **DUA level blokir:** opt-out survei (pelanggan, survey-scoped) vs hard-block ledger (admin/auto, SEMUA broadcast). Test: repo opt-out + ledger wa-status (batch 15) + service opt-out (10) + send-queue onWhatsApp (25) HIJAU; lint 0; node --check semua OK. (Cron opt-out-skip = guard 1-baris pakai `isSurveyOptedOut` yang teruji; verifikasi jest tertunda krn OOM mesin lokal — diverifikasi di prod pasca-deploy.) Terkait [[broadcast-anti-ban]] [[csat-survey-feature]]. **PELAJARAN:** opt-out yang mudah = anti-ban (kurangi lapor-spam) — TAPI WAJIB scoped: tagihan tak boleh bisa di-opt-out.

<a id="b159"></a>
### Fitur 2026-07-19 (CSAT web-managed: jadwal survei di halaman Cron + tombol "Kirim Survei Sekarang")

- **Owner:** `views/sb-admin/cron.php` (+field `schedule_rating_survey`/`status_rating_survey` + digest — pola form existing /api/cron; API sudah di-whitelist `admin-config-routes`) + `routes/admin-csat-routes` (POST `/api/owner/csat/run` → trigger `runRatingSurveyCycle` FIRE-AND-FORGET, gate admin/owner + `csatSurvey.enabled`, idempoten via dedup periode) + `views/sb-admin/survei.php`+`static/js/survei.js` (tombol "Kirim Survei Sekarang" + confirm + tile opt-out + status fitur AKTIF/NONAKTIF). **KENAPA (akar):** setelan CSAT/cron TADINYA cuma bisa via SSH/edit-file — kemunduran; owner tegur "semua fitur harus editable via web". Kini jadwal survei & digest bisa diubah di halaman **Cron**, dan survei bisa dipicu MANUAL ke semua pelanggan layak dari **/survei** tanpa perlu tahu sintaks cron. GET `/api/owner/csat` kini sertakan `enabled` + `optouts`. **SISA (belum web, follow-up):** toggle `csatSurvey.enabled` + knob `broadcastGuard.*` masih config.json/SSH. Verifikasi: route load (GET+POST) OK, php -l cron/survei OK, lint 0. Terkait [[csat-survey-feature]] [[owner-tooling-completeness]]. **PELAJARAN:** whitelist API TIDAK cukup — WAJIB tambah field FORM di halaman-nya; kalau tidak, fitur "ada" di backend tapi tak terjangkau owner (persis kemunduran yang ditegur).

<a id="b160"></a>
### Fix 2026-07-19 (CSAT: tutup celah balasan survei bocor → rating hilang + reboot salah picu; + pemulihan)

- **Owner:** `lib/cron/jobs/rating-survey` (markSent SEKETIKA di FASE 1, bukan akhir siklus) + `repositories/csat.repository` (markUndelivered kini revert `sent` belum-berskor; +`noteReminded`/`listLostForRecovery`) + `lib/csat/rating-parser` (+`isBareAck` SEMPIT) + `lib/csat/csat-survey-service` (ack-swallow saat survei aktif) + `lib/csat/csat-recovery.js` (BARU) + `routes/admin-csat-routes` (POST `/api/owner/csat/recover` +dryRun) + `views/sb-admin/survei.php`/`static/js/survei.js` (tombol "Pulihkan Terlewat") + template `rating_reprompt`. **AKAR (insiden survei perdana 19-07 09:00):** pengiriman ber-jeda anti-ban makan 6–10 mnt tapi baris survei baru ditandai `sent` di FASE 3 (akhir siklus); `getActiveByUserId` hanya lihat `sent`/`rated`, jadi ~130 pelanggan yang balas SELAMA jendela itu balasannya bocor ke pipeline normal → rating hilang (banyak balas bot diam), keluhan (parse `rating` neg) sebetulnya ketangkap TAPI ack singkat "siap"/"iya" (`unclear`) jatuh ke pipeline → dikira konfirmasi → **router ke-reboot**. **FIX:** (1) markSent di FASE 1 → survei aktif sejak detik pertama; FASE 3 cukup revert gagal-total→`undelivered`. (2) `isBareAck` (token afirmasi tunggal siap/iya/makasih/nggih — SEMPIT biar "Info mas"/"cek koneksi" lolos) → saat survei aktif di-reprompt sekali lalu ditelan, TAK bocor ke reboot. (3) **Pemulihan:** `csat-recovery` baca `inbound_messages` (message_logs.sqlite), cocok `canonical_jid`, PUTAR ULANG anchor rating/optout + komentar dlm burst 15mnt lewat `handleInboundReply` (rating + WA susulan + alert + opt-out); `dryRun` verifikasi tanpa kirim; in-process (butuh WA) via endpoint. **HASIL LIVE 2 bot:** dipulihkan Dander 5 + Tanjung 11 (incl. Mas Andik=1 & opt-out Fandi "STOP"); balasan susulan terkirim 0 error; test regresi isBareAck+ack-swallow (60 hijau). **GOTCHA TEMUAN:** alert detractor (`notifyOwnerDetractor`→`getAdminJids`→`sendCritical`) TAK menghasilkan log `[WA_CRITICAL]` saat Andik=1 dipulihkan → kemungkinan P1 detractor-alert mati (BUKAN akibat fix ini) — perlu cek terpisah. Terkait [[csat-survey-feature]] [[broadcast-anti-ban]] [[wa-cron-send-retry]]. **PELAJARAN:** kalau "aktif menangkap" bergantung status yang di-set BELAKANGAN sedangkan pengiriman jadi lambat (jeda anti-ban), ada JENDELA BUTA — set status penangkap SEBELUM aksi lambat, bukan sesudah.

<a id="b161"></a>
### Fix 2026-07-19 (CS: tutup miss deteksi cek-koneksi + keamanan reboot — data-driven korpus prod)

- **Owner:** `lib/loose-intent-matcher` (kosakata) + `lib/affirmative-parser` (`isCleanRebootConsent`) + `message/handlers/connection-check-handler` (M2 gate + export `resolveLineStatus`) + `message/handlers/reboot-modem-handler` (M3 gerbang area) + `message/handlers/states/{reboot-followup,other}-state-handler` (pakai konsen bersih) + `database/wifi_templates.json` (REBOOT_MODEM 5→30 kw) + `database/response_templates.json` (3 key). **AKAR (audit korpus `INTENT_DEBUG`+`inbound_messages`):** ~38–47% pesan bernuansa koneksi jatuh `undefined`, DAN reboot bisa salah picu. **DETEKSI:** SYMPTOM +loading/muser/mubeng/eroran/ngeror/bufring; CONTEXT +connect/conect/wifian; frasa +sinyal(e) lemah/jelek/kurang; `CONTEXT_GATED_PHRASES` "belum ada/konek/nyala" (HANYA bila ada kata jaringan → aman dari "uang belum ada"); REBOOT_MODEM +kosakata alami (tolong reboot/restart dong/di reboot…). **KEAMANAN REBOOT:** `isCleanRebootConsent` (BLOCKLIST muatan-lain + no `?` + ≤7 token) → "bisa cek tagihan?"/"siap kak makasih"/"mau lapor" TAK lagi mereboot (akar "ketik siap malah reboot"); "ya ka"/"iya boleh" tetap lolos; dipakai di REBOOTFU_OFFER + CONFIRM_REBOOT, afirmasi ambigu → lepas tawaran (tak reboot). M2: jalur SEHAT + pelanggan tak mengeluh → tak ditawari reboot (jalur "sudah restart sendiri" tetap jalan; gate DI DALAM buildRebootOffer, bukan rebootEligible). M3: permintaan reboot eksplisit pelanggan kini lewat gerbang GANGGUAN AREA. Regresi 150 hijau (4 suite) + verifikasi LIVE 2 bot (isCleanRebootConsent + loose + REBOOT_MODEM 30 kw). data-file di-MERGE (union kw + tambah key), bukan timpa. Terkait [[smart-cs-audit-2026-07]] [[customer-facing-cek-koneksi]] [[reboot-followup-gap]] [[customer-chat-corpus]]. **PELAJARAN:** konsen aksi berisiko pakai BLOCKLIST muatan-lain (bukan whitelist partikel — "ya ka" pasti ada); gate "sehat tak perlu reboot" taruh di titik-tawaran, bukan kelayakan (jangan matikan jalur lain spt self-restart).

<a id="b162"></a>
### Fix 2026-07-19 (UI: token semantik sadar-mode + guard — akar "halaman baru selalu gelap-di-gelap")

- **Owner:** `static/css/tokens.css` (lapisan token SEMANTIK baru) + `static/css/{admin,teknisi}-theme.css` (@import `?v=2`) + `static/css/survei.css` & `static/css/owner-cockpit.css` (migrasi) + `scripts/check-theme-tokens.js` (guard, `npm run check:theme`) + `package.json`. **AKAR (owner lapor /survei tak terbaca di dark mode + "kenapa halaman baru selalu begitu"):** tokens.css hanya punya primitif TETAP (`--white`/`--slate-900`, tak ikut mode gelap) + token dark-only (`--d-*`, hanya ada di body.tk-dark); TAK ADA token SEMANTIK sadar-mode. Jadi CSS halaman kustom terpaksa pakai primitif tetap → teks gelap di latar gelap, ATAU nulis `body.tk-dark .x{}` manual per kelas (rawan luput — survei.css luput → bug). **FIX:** tokens.css +`--surface/--surface-2/--canvas/--ink/--ink-soft/--muted/--line` (light di `:root`, di-flip di `body.tk-dark`) → halaman cukup `var(--ink)`/`var(--surface)` = benar 2 mode otomatis. @import tokens.css di-`?v=2` (cache-bust: @import tak ikut rafAssetUrl → tanpa versi BASI di CF → `var(--ink)` kosong). survei.css (gelap-di-gelap) + owner-cockpit.css (benar tapi pola manual rapuh) dimigrasi. **PENCEGAHAN:** `check-theme-tokens.js` flag CSS halaman yang pakai primitif tetap utk bg/teks/border → **langsung menangkap owner-cockpit sbg offender ke-2** (bukti pola berulang). Deploy 5 CSS 2 bot (static, tanpa restart; token semantik terverifikasi di disk prod). Terkait [[design-tokens]] [[admin-ui-overhaul]] [[theme-css-cache-bust]] [[owner-tooling-completeness]]. **PELAJARAN:** sediakan token SEMANTIK sadar-mode (bukan cuma primitif+dark-only) supaya "jalan benar" = jalan termudah; guard mengubah konvensi jadi cek otomatis, bukan andalan ingatan.

<a id="b163"></a>
### Feat 2026-07-20 (Request ganti paket: keputusan admin langsung dari WhatsApp — ok / tolak / batalkan)

- **Owner:** `message/handlers/package-request-admin-handler` (BARU) + `message/handlers/state-domains/package-request-admin.state` (BARU; owner `package-request`, prefix `PKGREQ_` di `conversation-state-owner-map`+`conversation-state-router`) + `services/admin.service` (`approvePackageChange` +aksi `cancel`; +`listPendingPackageChangeRequests`; notif request kini ke `getAdminJids()` ∪ `config.ownerNumber` via `mergeStaffRecipients`) + `database/response_templates.json` (14 key `package_request_admin_*` + petunjuk balas ditambahkan ke `admin_service_package_change_owner_request`) + hook di `message/raf.js` (DITARUH SEBELUM hook bukti-bayar). **AKAR:** teknisi ajukan ganti paket lewat web → approval HANYA bisa lewat panel; owner dinotif ke `config.ownerNumber` yang di prod sering placeholder → notif tak sampai & tak ada yang bisa proses. **FIX:** admin balas notif langsung di WA — `ok` (approve: update MikroTik+DB, pelanggan+teknisi dinotif) / `tolak <alasan>` (pelanggan+teknisi dinotif) / `batalkan` (batal DIAM-DIAM: pelanggan TAK disentuh, hanya teknisi) — via balasan-quote (id `req_pkg_…` dibaca dari teks notif), kode eksplisit `ok req_pkg_…`, atau `request paket` (antrian bernomor → state `PKGREQ_SELECT`/`PKGREQ_CONFIRM`). Handler HANYA klaim perintah bersasaran pasti (quote-req_pkg / kode / `request paket`) → tak bentrok antrian bukti-bayar; ditaruh SEBELUM bukti-bayar agar quote-reply "ok" ke notif paket tak dibajak fallback bare-confirm bukti-bayar. Gate peran accounts.json presisi (non-admin → handled:false senyap); never-throw; `skipDuplicateCheck`; gate `config.packageRequestWa.enabled` (default ON). Reuse `approvePackageChange` yang sama dengan web (`POST /api/approve-package-change`). **GOTCHA:** guard batal-universal (`raf.js:453` `isCancellationKeyword`) menyerap bare `batal`/`cancel` sebelum hook → verb batal-diam-diam dipakai `batalkan`/`hapus` (lolos guard), `batal <n>`/`batal req_pkg_…` (multi-kata) juga lolos; regex `CANCEL_WORDS` "batalkan" HARUS sebelum "batal" (di regex ber-`(.*)` "batal" lebih pendek cocok dulu → sisa "kan" salah terbaca jadi alasan — ditangkap test). Tes: handler 21 + state 12 + service (cancel diam-diam / listPending FIFO / recipient merge). Terkait [[payment-proof-confirmation]] [[conversation-state-canonical-key]] [[admin-alert-recipients]]. **PELAJARAN:** saat menambah jalur perintah WA baru yang berbagi ruang teks dengan handler lain, klaim HANYA yang bersasaran pasti + urutkan sebelum handler ber-fallback-bare; dan jangan pakai kata yang sudah jadi kata-kunci global (batal) sebagai verb — ia sudah dicegat lebih dulu.

<a id="b164"></a>
### Feat 2026-07-20 (LOS: gerbang MATI-LISTRIK-AREA anti salah-vonis "fiber putus" + notif pelanggan ramah + notif pulih pelanggan)

- **Owner:** `lib/olt-los-verifier` (baru: properti `result.areaDgClusterByMac` — hitung jumlah ONU tetangga se-OLT yang `dying-gasp` di ±`areaOutage.windowSeconds` dari baris Lost target, korelasi INTRA-log = kebal drift jam OLT) + `lib/olt-los-broadcaster` (GERBANG di `broadcastGroup`: LOS dengan klaster DG tetangga ≥`dgClusterThreshold` = kolateral mati listrik area → status `suppressed_area_dg`, TAK broadcast/tiket/notif; + notif PULIH ke PELANGGAN di `sendRecovery` HANYA yang sempat dikabari saat gangguan; template pelanggan default kini RINGAN — tanpa klaim "fiber putus"/"menuju lokasi") + `routes/admin-los-broadcast-routes` (`normalizeLosConfig` +param `current` fallback anti-wipe + `verifyViaScrape.areaOutage` + `notifyCustomer.{notifyOnRecovery,recoveryMessageTemplate}`; `decorateForView` ekspos). **AKAR (investigasi prod Tanjung 20-07, MAC 3820282539F0 Pak Achmad):** saat mati-listrik massal ~94 ONU tumbang serentak (OLT-time 18:24:40–41); 92 kirim `dying-gasp` (=DG, benar diabaikan) tapi **2 GAGAL gasp** → hanya "Lost" → salah-vonis LOS "fiber putus" + broadcast ke teknisi. verify-scrape lama tak menolong (DG memang tak pernah ada utk MAC itu) & cluster-gate lama cuma hitung event LOS (buta thd 92 DG di hulu). **FIX:** deteksi klaster DG dari WEB SCRAPE (bukan `olt_events` — syslog DROP 88/92 paket DG saat burst → durable log buta) → suppress LOS yang sebenarnya mati listrik. Krn mati-listrik massal tak lagi menembus, **notif pelanggan aman dinyalakan** (Tanjung: `enabled`, `delayMs`=10mnt, teks santai, `notifyOnRecovery`; kode deploy 2 bot, config-enable Tanjung dulu). Config `oltLosBroadcast.verifyViaScrape.areaOutage{enabled:true,dgClusterThreshold:5,windowSeconds:10}` + `notifyCustomer{enabled,delayMs,messageTemplate,notifyOnRecovery,recoveryMessageTemplate}`. Fail-open: tanpa sinyal → tak menyaring (fiber putus asli tak bikin tetangga gasp → klaster ~0 → aman). Tes: verifier +3, broadcaster +4 (`suppressed_area_dg`+recovery pelanggan), route +5; 67 hijau area LOS + 126 adjacent hijau. Terkait [[olt-dying-gasp-detection]] [[olt-event-log-los-group]] [[cctv-power-outage-gate]]. **PELAJARAN:** vonis per-ONU terisolasi buta thd konteks — "Lost tanpa DG" di tengah tetangga yang gasp massal = mati listrik, BUKAN fiber putus; gunakan sumber yang PUNYA event (web scrape), bukan yang lossy (syslog); pesan ke pelanggan jangan men-diagnosa yang bisa meleset.

<a id="b165"></a>
### Feat 2026-07-21 (PSB: alamat & dusun jadi field sendiri + gerbang modem copotan)

- **Owner:** `message/handlers/psb-caption-parser` (+`rt_rw`/`alamat` alias, `normalizeRtRw` → `{rt,rw}` 3-digit, `composeAddress`, `titleCaseDusun`, `validatePsbData(...,{requireRtRw})`) + `message/handlers/state-domains/psb.state` (slot RT/RW, dusun PILIHAN BERNOMOR dari `psbIntake.dusunList` via balasan angka, alamat DIRAKIT & tampil di layar konfirmasi, kirim `address`+`dusun` ke create, `annotateCandidates`/`assignmentBlockReason`, perintah `cari <SN/nama>`) + **`lib/psb-modem-provenance` (BARU)** + `lib/psb-genieacs-service.findPsbCandidatesByHint` + `repositories/api-users.repository` (`USER_INSERT_COLUMNS` +`dusun`) + `lib/database-migration-manager` (migrasi v7 kolom `users.dusun`, expected version 6→7) + `routes/admin-config-routes` & `views/sb-admin/config.php` & `static/js/config.js` (`psbIntake.{desa,kecamatan,dusunList}`) + `views/sb-admin/users.php` & `static/js/users.js` (field dusun).
- **AKAR (audit PSB nyata 21-07, Imam Ghozali pakai modem copotan Wimpi Sayekti):** (1) wizard tak pernah menanyakan alamat → `users.address` SELALU null utk pelanggan hasil wizard, padahal catatan insiden OLT menyalin alamat → laporan gangguan beralamat kosong; dusun cuma "hidup" di dalam string username PPPoE sehingga tak bisa dipakai mengelompokkan. (2) Tak ada validasi apa pun yang mencegah modem pelanggan hidup dipakai ulang — di prod sudah ada 1 `device_id` dipakai 2 baris pelanggan. (3) Deteksi modem hanya `_registered` (recency): terukur 158/159 device ber-`_registered` ±347 hari → modem copotan MUSTAHIL muncul di daftar "baru".
- **FIX:** teknisi hanya mengetik RT/RW (`14/2` → `RT 014 RW 002`), sisanya dirakit; dusun dipilih bernomor supaya ejaannya konsisten (ikut jadi username PPPoE permanen). Kandidat modem diberi label otomatis; nama pemilik lama diambil dari `olt_modem_state` (satu-satunya jejak yang SELAMAT dari penghapusan pelanggan — baris users + secret MikroTik + kadang catatan ACS ikut hilang). Gerbang gagal-tertutup di JALUR YA **dan** pilih-nomor. Modem copotan dicari via `cari <4 digit SN>` / `cari <nama pemilik lama>` (cari-lalu-pilih: typo hanya "tak ketemu", tak pernah menulis).
- **GOTCHA:** sumber kebenaran "modem sedang dipakai" = **daftar sesi PPPoE aktif**, BUKAN `last-logged-out`/`caller-id` di secret — secret milik pelanggan yang justru sedang online berbunyi `jan/01/1970` + caller-id kosong (nyaris salah vonis). Router tak terbaca ⇒ `activeUsernames=null` = "TIDAK TAHU" (bukan "tak ada yang aktif"), gerbang tautan pelanggan tetap jalan. Deteksi band yang GAGAL tak lagi disimpan sebagai fakta: dibaca ULANG setelah push (modem yang baru semenit terdaftar di ACS belum selesai ditelusuri → `bulk` terlanjur `["1"]` pada 2/2 PSB wizard, akibatnya ganti sandi WiFi tak menyentuh 5GHz).
- **Data prod (dibereskan terpisah):** `bulk` 2 pelanggan wizard dikoreksi ke `["1","5"]` + alamat parsial diisi; 92 pelanggan (52 Tanjung + 40 Dander) yang koordinatnya dipatok ke SATU titik basecamp dikosongkan (koordinat palsu lebih berbahaya daripada kosong — usul ODP & peta jadi salah dengan percaya diri). Tanjung punya **0 aset ODP** → usul ODP otomatis mati sampai dipetakan.
- **Tes:** parser +12, wizard +12 (alamat/dusun/gerbang/cari/koreksi band), `lib/__tests__/psb-modem-provenance` +14. Terkait [[psb-simplification-plan]] [[psb-pppoe-username-convention]] [[genieacs-registered-field]] [[bulk-ssid-device-drift]] [[olt-modem-state-engine]].
- **PELAJARAN:** tanyakan ke manusia HANYA yang cuma dia tahu (angka di stiker, RT rumah); yang bisa diturunkan (alamat, "bekas siapa", indeks band) diturunkan — dan kalau tak bisa diturunkan, jangan simpan tebakannya sebagai fakta.

<a id="b166"></a>
### Feat 2026-07-21 (Peta berhenti berbohong: JALUR kabel yang mengikuti jalan — endpoint waypoint yang hilang + rekam jalur dari WhatsApp)

- **Owner:** **`lib/network-route-service` (BARU)** — pemilik tunggal aturan jalur (validasi titik, buang titik kembar berurutan, `routeLengthMeters` = panjang GARIS bukan jarak lurus, `routeKey`) + `routes/admin-network-assets-routes` (`GET/POST/DELETE /api/map/waypoints` **+ `GET /api/map/waypoints/all`**; baca=staf, tulis=admin) + `message/handlers/state-domains/network-asset.state` (trigger `#JALUR <ODP>` → step `ASSET_ROUTE_WAIT`/`ASSET_ROUTE_REPLACE`, share lokasi tiap BELOKAN, `SELESAI`/`HAPUS`/`ULANG`) + `message/raf.js` (`ROUTE_RE` di gerbang aset) + `static/js/map-viewer.js` (`preloadManualRoutes`/`resolveConnectionRoute`/`routeStyleHint`).
- **AKAR:** garis peta SELALU lurus bukan karena fiturnya belum dibuat — editor waypoint di `map-viewer.js` sudah lengkap (mode edit, klik-tambah, drag, hapus, simpan), tabel `connection_waypoints` + `lib/waypoints-repository` sudah ada dan sudah di-init saat boot (`lib/database.js`), **hanya lapisan HTTP-nya yang tak pernah ada** (grep + riwayat git: `/api/map/waypoints` nol di `routes/`). 404-nya ditelan `if (response.ok)` tanpa cabang else → jatuh diam-diam ke routing API, yang juga mati (`openRouteService.enabled=false`, apiKey kosong) → garis lurus, tanpa satu pun keluhan.
- **FIX:** urutan kepercayaan eksplisit — jalur manual (manusia) → routing API (tebakan mesin) → garis lurus yang kini **digambar putus-putus** (`routeStyleHint`) supaya "belum dipetakan" tak menyamar jadi jalur nyata. Jalur direkam dari LAPANGAN: teknisi kirim share lokasi tiap belokan; titik ODC & ODP diambil dari aset (ujung tak perlu dipin ulang); jalur lama tak pernah ditimpa tanpa `YA`. Klien memuat SEMUA jalur sekali (`/all`) — dulu 1 permintaan per koneksi di dalam loop ber-`await` (100 ODP = 100 request tiap peta dibuka). **Kabel drop pelanggan→ODP TIDAK di-route** (memotong pekarangan; memutar lewat jalan lebih salah daripada lurus) kecuali `openRouteService.routeDropCable=true`.
- **GOTCHA:** `DONE_RE` lama (`/^(selesai|sudah|…)\b/`) membuat **"sudah sampai mana ya" = SELESAI** → jalur setengah jadi tersimpan diam-diam; mode rekam kini pakai `ROUTE_DONE_RE` satu-pesan-utuh (dialek: "sudah tak bayar" = *saya sudah bayar*). Koordinat `0` ditolak sebagai titik (`Number(null)===0` = Teluk Guinea). Baris jalur lama yang rusak → `getRoute` mengembalikan `null`, bukan melempar (satu baris cacat tak boleh menjatuhkan peta).
- **UKURAN (Overpass live, bukan intuisi):** OSM di wilayah layanan ternyata MEMADAI untuk snap-to-road — Dander 13,6 km jalan/246 bangunan per 1,4 km², median rumah→jalan **20 m** (93% <50 m); Tanjungharjo 15,9 km/683 bangunan, median **16 m** (99% <50 m). ORS gratis 2000 rute/hari, dan dipakai sebagai PENABUR sekali-jalan (hasilnya disimpan jadi waypoint) → kuota & ketergantungan internet praktis nol.
- **Tes:** `lib/__tests__/network-route-service` +13, route waypoints +6, `#JALUR` +8 (34 hijau di suite aset). Terkait [[network-assets-odp-mapping]] [[newbie-sdm-design-principle]].
- **PELAJARAN:** fitur yang "tak dipakai" cek dulu **jembatannya putus atau tidak** — dua kali berturut-turut peta kosong bukan karena orangnya malas (dulu: teknisi 403 di halaman aset; kini: endpoint tak ada + 404 ditelan senyap). Klien yang menelan status gagal membuat kerusakan bertahan berbulan-bulan.

<a id="b167"></a>
### Feat 2026-07-21 (Kartu bantuan aset + gagal-senyap gerbang staf ditutup)

- **Owner:** `message/handlers/state-domains/network-asset.state` (`HELP_RE` = `bantuan|panduan|menu|perintah` + `aset|odp|odc|jaringan|peta`, atau `#ASET` → `showAssetHelp`; `ringkasProgres`/`langkahBerikut`; dep baru `getAssets`; konstanta `HINT_HELP` ditempel di 6 pesan "salah ketik"/"tak ketemu") + `message/raf.js` (dispatch `HELP_RE` + balasan **BUKAN-PETUGAS** untuk perintah ber-`#`) + key template baru `aset_bukan_petugas` di `database/response_templates.json`.
- **AKAR:** peta 2 bot **0 ODC / 0 ODP** padahal wizard WA-nya hidup sejak #b149. Grep membuktikan seluruh perintah aset (`#ODC/#ODP/#ISI/#LOKASI/#JALUR`) **nol kemunculan** di menu WA, template, maupun tutorial teknisi — hanya ada di kode. Dan nomor yang belum terdaftar staf **didiamkan total** (`resolveAuthorizedStaff` null → jatuh ke alur pelanggan): teknisi menyimpulkan "botnya rusak" lalu berhenti, tanpa jejak apa pun.
- **FIX:** kartu menyebut semua perintah **plus papan progres hidup** (`N ODC · N ODP · tersambung X/Y · jalur Z`) dan **satu langkah berikutnya yang ditunjuk** (0 ODC → `#ODC`; ada ODP + pelanggan menggantung → `#ISI` + jumlah sisa; dst) — pekerjaan tanpa ujung yang terlihat tak akan dikerjakan. Balasan "belum terdaftar" HANYA untuk perintah ber-`#`/kartu; `odp <nama>` tanpa `#` tetap senyap supaya kalimat pelanggan ("odp saya mati") tak dijawab pesan petugas.
- **Tes:** kartu +6 (isi, angka progres, nudge per-kondisi, `getAllRoutes` gagal → jalur 0, jejak dari pesan "tak ketemu"), router end-to-end +3 (non-staf `#ODP` dijawab · "odp saya mati" TIDAK dijawab · staf dapat kartu). 87 suite / 550 test hijau di area `message/`.
- **PELAJARAN:** tiga kali berturut-turut peta kosong karena hal yang sama — **pintunya tak terjangkau, bukan orangnya malas** (403 halaman aset #b149 → endpoint jalur hilang #b166 → perintah tak pernah terlihat + penolakan senyap #b167). Setiap perintah baru wajib punya tempat ia MUNCUL, dan setiap penolakan wajib berbunyi.

<a id="b171"></a>
### Feat 2026-07-21 (Titik lokasi pelanggan: satu gerbang untuk WA teknisi, web admin, dan pin pelanggan)

- **Owner:** **`lib/customer-location-service` (BARU — gerbang MURNI bersama)** + **`message/handlers/state-domains/customer-location.state` (BARU; owner `customer-location`, prefix `CUSTLOC_`)** + `routes/api-users-routes` (`POST /api/users/:id/location`, pratinjau tanpa `confirm`) + `views/sb-admin/users.php` & `static/js/users.js` (kolom TITIK jadi tombol + modal tempel koordinat/link Maps) + `message/raf.js` (2 trigger) + `lib/database-migration-manager` (migrasi v8: `users.location_source` + `location_updated_at`, expected 7→8) + `repositories/api-users.repository` (whitelist) + `config.example.json` (`customerLocationSelfService.enabled`, default OFF).
- **AKAR:** 148 dari 156 pelanggan tak punya titik setelah 92 koordinat palsu dibersihkan ([#b165] + [#b172]), padahal sebagian pelanggan SUDAH pernah mengirim share-lokasi lewat WA. Belum ada cara memasukkan titik selain modal edit penuh yang justru sumber masalahnya.
- **ALUR WA (teknisi/admin):** `lokasi <nama>` (cari) atau `lokasi` polos (**mode borongan**: daftar yang belum bertitik, dan sesudah menyimpan langsung menyodorkan sisanya) → pilih angka → **bot menampilkan TITIK LAMA + sumber + peringatan akan menimpa** → kirim pin / **teruskan pin pelanggan** / tempel link Maps → layar konfirmasi (titik lama vs baru + jarak geser + tetangga & ODP terdekat + peringatan) → *YA* baru disimpan.
- **GERBANG PRESISI (dipakai ketiga jalur, satu sumber):** titik default basecamp `-7.24139,111.83833` **ditolak keras**; titik yang sama (≤30 m) dengan pelanggan lain yang **baru saja** disimpan (≤30 mnt, dibaca dari `location_updated_at` sehingga tahan restart & lintas petugas) → peringatan "kemungkinan ini lokasimu sendiri"; titik >3 km dari acuan terdekat → peringatan nyasar; `(0,0)` & koordinat di luar rentang ditolak.
- **GOTCHA:** pelanggan dipilih **lebih dulu**, pin belakangan — kalau dibalik, bot harus mencegat SEMUA pesan lokasi dan itu membajak `otw [ID]`. Trigger `lokasi` hanya pada TEKS; jalur pin-pelanggan ditaruh SESUDAH `handleActiveTicketLocationUpdate` + hanya non-staf tanpa state aktif. Konfirmasi pelanggan pakai `lib/affirmative-parser` (bukan daftar cocok-persis). Link pendek `maps.app.goo.gl` **dijelaskan**, bukan gagal senyap.
- **Tes:** service 17, wizard WA 13, penjaga peta panel 10 (lihat [#b172]). Terkait [[psb-simplification-plan]] [[network-assets-odp-mapping]] [[users-data-integrity-gotchas]].
- **PELAJARAN:** saat menambah jalur input baru untuk data yang pernah rusak, taruh aturannya di SATU modul murni lalu panggil dari semua permukaan — kalau tiap permukaan punya salinan aturannya sendiri, satu di antaranya pasti tertinggal (itulah yang terjadi pada panel admin vs teknisi).

<a id="b168"></a>
### Fix+Feat 2026-07-21 (Peta jaringan: tiga penyaring yang saling menimpa disatukan + ODP melapor sendiri)

- **Owner:** **`static/js/map-filter-core.js` (BARU, murni & teruji)** — `buatFilterState`/`terapkanQuickFilter`/`setelKategori`/`bolehTampilAset`/`bolehTampilPelanggan`/`hitungKesehatanOdp`/`ringkasKesehatan`/`ringkasHunian`; dipakai `static/js/map-viewer.js` (admin) & `static/js/teknisi-map-viewer.js` supaya dua peta tak berbeda perilaku. + `static/js/broadcast.js` (prefill `?mode=&target=`) + CSS lencana/curiga di `map-viewer.css` & `teknisi-map-viewer.css`.
- **AKAR (penyaring):** ADA TIGA PENULIS VISIBILITAS yang tak saling tahu — kotak centang legenda (`toggleLayerVisibility`) menambah/menghapus marker LANGSUNG **tanpa mencatat state**, tombol Quick Filter menimpa `selected*Ids`, dan modal Filter Kustom. Akibatnya centang legenda hilang senyap tiap auto-refresh 30 dtk / tiap quick filter ditekan: kotak tetap tercentang, peta berkata lain. Garis pun diuji dengan daftar pilihan (bukan yang benar-benar tampil) sehingga bisa tergambar menuju ODP yang sedang disembunyikan.
- **FIX:** satu `filterState` (kategori + prasetel), semua kontrol menulis ke sana lalu peta digambar ulang; `sinkronkanKontrolFilter()` membuat legenda & tombol SELALU menggambarkan state. **Quick "Online"/"Offline" tak lagi menyembunyikan ODC/ODP** — dulu memilih Offline membuang justru konteks yang dicari (pola "3 dari 4 di ODP ini mati"). Menyentuh satu kotak legenda menjadikan prasetel `custom` supaya tombol tak mengaku aktif; menyusun ulang kategori yang sama persis dgn prasetel menyalakannya lagi.
- **FEAT:** ikon ODP kini melapor sendiri — lencana hunian `3/8` (kuning tinggal 1 port, merah penuh) + **cincin merah berdenyut bila mayoritas penghuni offline** (≥50% dari yang TERBACA, minimal 2 pelanggan). Popup menaruh **vonis di atas daftar nama**, menampilkan **panjang jalur kabel terekam** (dari #b166; kalau belum ada → arahan `#JALUR <nama>`), dan tombol **"Beri tahu N pelanggan ODP ini"** → `/broadcast?mode=odp&target=<id>` (mode `odp` sudah lama ada di service tapi tak punya pintu dari peta; sengaja BERHENTI di layar tulis pesan).
- **GOTCHA:** "tak terbaca" ≠ "mati" **dan** ≠ "sehat" — daftar sesi PPPoE gagal dibaca ⇒ status `unknown`, tak dihitung offline (tak menuduh) DAN tak boleh berbunyi "Semua 0 pelanggan online" (versi pertama begitu; ketahuan di uji visual). 1 dari 1 mati bukan pola. Kapasitas `0` = tak dibatasi, jangan dilaporkan penuh.
- **Tes:** `static/js/__tests__/map-filter-core` +15. Verifikasi visual lewat harness lokal memakai CSS/JS asli: lencana `3/8`→abu, `7/8`→`rgb(253,126,20)`, `8/8`→`rgb(220,53,69)`, cincin `odpDenyut` aktif pada kasus curiga.
- **PELAJARAN:** dua kontrol yang menulis hal sama tanpa state bersama BUKAN "dua fitur", melainkan satu bug yang menunggu — dan bug seperti ini tak pernah dilaporkan sebagai bug, cuma dirasakan sebagai "petanya berantakan".

<a id="b169"></a>
### Fix+Feat 2026-07-21 (Template pesan: guard integritas key + Speed on Demand pindah ke state domain)

- **Owner:** **`message/handlers/state-domains/speed-boost.state.js` (BARU)** — pemilik tunggal alur Speed on Demand pelanggan (step `SODB_SELECT_PACKAGE`/`_DURATION`/`_PAYMENT`/`_CONFIRM`, terdaftar di `conversation-state-owner-map` prefix `SODB_` + `conversation-state-router`), dipicu `startSpeedBoost` dari `raf-intent-dispatch/customer-service-intents.js`. + **`lib/template-usage-scanner.js` (BARU)** — pemilik tunggal pemindaian pemakaian template (key yatim/hilang/slot tak dikenal).
- **Status path lama:** `handleRequestSpeedBoost` di `package-management-handler.js` **DIHAPUS** (beserta ekspor & pemasangan di `message/raf.js`); step `SELECT_SOD_CHOICE`/`CONFIRM_SOD_CHOICE` di `states/other-state-handler.js` + case-nya di `conversation-state-handler.js` **DIHAPUS**; template `other_sod_payment_success` **DIHAPUS**.
- **AKAR (rekening dummy):** template SOD legacy memuat rekening **karangan** hardcode (`BCA 1234567890`, `Mandiri 9876543210`, `DANA 08123456789`) sebagai instruksi transfer ke pelanggan, padahal `lib/templating.formatBankAccounts()` sudah menyediakan `${rekening}` dari `config.bankAccounts` — dan dari 910 template hanya SATU yang memakainya. Alur baru mengambil rekening dari config; ada tes yang menolak digit dummy itu muncul lagi.
- **KENAPA BUKAN "hidupkan `speed-boost-handler.js`":** handler mati itu fiturnya lebih lengkap TAPI melanggar 3 invariant — store state sendiri `global.tempStates` ber-key `sender` MENTAH (pelanggan `@lid` jadi tuli di tengah alur), kirim ke `msg.key.remoteJid` (target bisa `@lid`), dan di luar `conversation-handler` sehingga kata batal universal/expiry tak berlaku. Fiturnya di-PORT, filenya tidak disambungkan. `speed-boost-handler.js` + `speed-payment-handler.js` (861 baris, 40 template) tetap TAK TERJANGKAU dari entrypoint manapun.
- **Guard integritas:** `message/__tests__/response-template-key-integrity.test.js` memindai SELURUH repo dan menolak key `renderResponseTemplate('x')` / `renderTpl(ctx,'x')` yang tak ada di `response_templates.json`. Sebelumnya guard hanya berupa daftar key MANUAL → 11 key bisa hilang tanpa gejala (fallback runtime tetap terkirim, tapi admin kehilangan kemampuan mengeditnya di `/api/templates`) — termasuk **seluruh alur konfirmasi transfer saldo**. Ke-11 key ditambahkan dengan teks & slot persis fallback-nya.
- **Simpan template aman:** `template-service.saveCategory` kini **backup ke `backups/templates/`** sebelum menimpa dan **menolak penghapusan massal** (>20% key kategori, min. 10 key) dengan `error.code = TEMPLATE_MASS_DELETION` → HTTP `409`; lolos hanya lewat `allowMassDeletion`. Alasannya: endpoint itu menimpa SATU KATEGORI PENUH, jadi satu POST parsial bisa menghapus ~900 template. `GET /api/templates/diagnostics?health=1` kini melaporkan yatim/hilang/slot tak dikenal (dulu cuma menghitung jumlah key).
- **GOTCHA:** ada **4 signature `renderResponseTemplate` berbeda** di repo (`(key,fallback,data)`, `(key,data)`, `(key,data,fallback)`, satu polimorfik) + wrapper `renderTpl(context,key,…)`. Deteksi signature per file rapuh; scanner memakai "argumen pertama yang berbentuk object literal" — berlaku di semua varian karena `fallback` selalu string. Salah menanganinya membuat laporan slot tak dikenal melonjak palsu dari 11 → 98. Karena itu peringatan slot bersifat **advisory**, bukan penolakan.
- **Afirmatif:** `lib/affirmative-parser` dapat `isCleanConsent(text, {onTopic})` — generalisasi `isCleanRebootConsent` (yang kini tipis di atasnya, perilaku identik). `CONSENT_BLOCKERS` disusun dari sudut pandang reboot sehingga memuat kata domain lain ("sandi", "saldo", "transfer"); tanpa `onTopic`, konfirmasi sah "ok transfer aja" justru tertolak. Dipasang KETAT di alur mahal (transfer saldo, buat tiket, ganti paket, pesan SOD) dan LONGGAR (`isAffirmative`) di alur yang bisa diulang (ganti nama/sandi WiFi).
- **Kebocoran ditutup:** `speed_boost_validation_missing_pppoe` tak lagi menyebut "PPPoE" ke pelanggan; `wifi-management.service.js` jalur gagal ganti nama tak lagi hardcode + membocorkan `error.message` mentah (kini lewat `wifi_name_change_error`); 3 template WiFi berhenti menampilkan `Error: ${error_message}` ke pelanggan.
- **Tes:** integritas key 3, state domain SOD 9, pengaman simpan + scanner 11, `isCleanConsent` 23. Suite penuh 386/3068 hijau. Terkait [[customer-facing-no-internal-ids]] [[conversation-state-canonical-key]] [[wifi-change-message-paths]].
- **PELAJARAN:** guard yang isinya daftar manual bukan guard — ia hanya melindungi yang sudah diingat orang. Dan pola pemanggilan yang bercabang empat membuat analisis statis (termasuk guard-nya sendiri) diam-diam salah: guard pertama versi ini lolos 100% justru karena regexnya tak mengenal wrapper `renderTpl`, yang menutupi 22 call site.

<a id="b172"></a>
### Fix 2026-07-21 (Panel pelanggan: gagal-GPS tak lagi menstempel koordinat default)

- **Owner:** `static/js/users.js` + `static/js/teknisi-pelanggan.js` — `handleGeolocationErrorUserModal` kini hanya menggeser TAMPILAN peta (`moveMapViewOnly`); input lat/lng hanya ditulis oleh tindakan sengaja (klik peta / geser marker / tombol GPS yang berhasil).
- **AKAR:** kegagalan/inisialisasi GPS ikut menstempel koordinat → 92 pelanggan menumpuk di titik default basecamp `-7.24139,111.83833`; data prod dibersihkan di [#b165].
- **Tes:** penjaga peta panel 10 (dirujuk juga dari [#b171]).
- Catatan: se-batch dengan [#b165]/[#b171]; entri ini ditulis saat perapian indeks karena semula hanya tercatat di indeks SYSTEM_MAP tanpa badan entri.

<a id="b173"></a>
### Fix 2026-07-22 (Penataan rules: indeks boundary diregenerasi + guard integritas + sapu rule-sprawl)

- **Owner:** `scripts/check-boundary-index.js` (BARU) + `scripts/__tests__/boundary-index.test.js` — guard anchor unik / link indeks valid DUA ARAH / baris indeks ringkas (≤450 char); jalan di `npm test`, indeks rusak = suite merah.
- **Indeks SYSTEM_MAP diregenerasi** dari heading log (172 baris satu-baris seragam; b141+ sempat membusuk jadi paragraf ratusan kata). Anchor dobel dibetulkan: b128 kedua→[#b170], b166 kedua→[#b171]; entri GPS panel yang tak pernah ditulis→[#b172].
- **Status path lama:** `.kiro/` (steering Kiro basi — mendeskripsikan `message-templates.json` `{placeholder}` yang tak pernah jadi sistem nyata) + `.sisyphus/` (state agent Feb 2026, hardening @lid yang sudah lama landed) **DIHAPUS**; skill `system-map-sync` ditulis ulang ke arsitektur pasca-split (versi lama masih menyuruh menulis entri panjang DI DALAM peta = akar pembusukan indeks).
- **CLAUDE.md:** + tabel "where a rule lives" (rules→map→changelog→skill→hook→guard-test, satu rule satu lapis) + playbook "standard path" tambah fitur (7 langkah) + seksi Git dibetulkan (branch lokal kini `main` tracking `raf-bot-v3/main`; `clean-main` tiada) + konvensi indent dibetulkan 2→4 space (ikut `.prettierrc.json`).
- **Konvensi (kini tertulis di 3 tempat yang saling menunjuk):** nomor entri baru = tertinggi + 1, badan ≤8 baris di log, indeks SATU baris — SYSTEM_MAP intro + skill `system-map-sync` + guard.
- **PELAJARAN:** aturan pemisahan peta/changelog membusuk justru lewat jalur patuh-prosedur — skill yang tak ikut diupdate saat split terus mengajarkan pola lama. Saat memindah kepemilikan dokumen, audit juga SEMUA instruksi yang menunjuk ke sana.

<a id="b174"></a>
### Feat 2026-07-22 (Otomasi anti-basi dokumen: guard docs↔kode + pre-push gate + hook diperluas + guard tema masuk npm test)

- **Owner:** `scripts/check-docs-sync.js` (BARU) + `scripts/__tests__/docs-sync.test.js` — 3 cek: path yang disebut CLAUDE.md / SYSTEM_MAP (non-indeks) / skills harus ADA; tiap `getDatabasePath('X')` literal terdokumentasi di SYSTEM_MAP; tiap gate langsung `config.<key>.enabled` ter-contoh di config.example.json. + `scripts/__tests__/theme-tokens.test.js` (guard tema kini otomatis di `npm test`; `check-theme-tokens.js` di-refactor export `scan()`).
- **Pre-push gate:** `.githooks/pre-push` (boundary-index + docs-sync + theme, ~2 dtk) di-wire OTOMATIS oleh `scripts/setup-githooks.js` via postinstall `package.json` (idempoten, hormati hooksPath lain, no-op senyap di salinan non-git seperti prod).
- **Hook `.claude/hooks/invariant-lint.js` diperluas:** warning Header Doc hilang (layer backend) + pengingat entri boundary saat file BARU muncul di layer owner (routes/services/repositories/message-handlers/lib-services/cron-jobs; rate-limit 30 mnt/file via marker os.tmpdir).
- **Temuan guard yang langsung dibetulkan:** SYSTEM_MAP menyebut `legacy-wifi-state-handler.js` yang sudah tiada; 8 DB tanpa dokumentasi (saldo/broadcast/voucher/olt_events/olt_state/psb_schedule/monitoring_metrics/isolir_audit → seksi DB); gate `steeringDriftMonitor` absen dari config.example.json (+default OFF).
- **CLAUDE.md:** + seksi "UI & theme conventions" (token semantik / `body.tk-dark` / `_head.php` / `rafAssetUrl` / CSS eksternal / gotcha php-express — sebelumnya hanya hidup di memory agent, tak ada di panduan repo) + baris "Pre-push gate" di tabel enforcement.
- **Tes:** docs-sync 1, theme-tokens 1; smoke hook manual (run-1 warning+reminder, run-2 reminder tersuppress rate-limit).

<a id="b175"></a>
### Feat 2026-07-23 (Keuangan pribadi owner menumpang nomor bot — domain terisolasi penuh)

- **Owner:** `repositories/personal-finance.repository.js` (BARU, `database/personal_finance.sqlite`, tabel `pf_entries`) + `lib/personal-finance-service.js` (parser nominal `50rb`/`2jt`/`50.000` + perintah + perakit laporan, fungsi murni) + `message/handlers/personal-finance-wa.js` (perintah `#U`) + `routes/admin-personal-finance-routes.js` (`/api/keuangan-pribadi/*`) + halaman `/keuangan-pribadi`.
- **Isolasi dari saldo pelanggan (alasan utama desain):** DB terpisah, kosakata terpisah (`entry`, bukan `saldo`), tak menyentuh `lib/saldo`, tak menulis `activity_logs`. `personal_finance.sqlite` SENGAJA di luar allowlist `lib/telegram-backup.js` — tujuan backup itu GRUP multi-anggota.
- **Kepemilikan TIDAK memakai `ownerNumber` maupun role:** daftar terpisah `config.personalFinance.ownerJids` (WA) + `webUsers` (halaman). Sebabnya konkret: di `accounts.json` role `owner` tidak ada dan 2 akun admin tak terbedakan → gate berbasis role akan membuka dompet pribadi untuk admin lain.
- **Gate:** `config.personalFinance.enabled` default **OFF**; allowlist kosong ⇒ 403/404 untuk semua (deploy gelap). Non-pemilik yang mengetik `#U` **didiamkan** (jatuh ke alur normal) — membalas "khusus pemilik" membocorkan keberadaan dompet ke pelanggan. Halaman balas **404, bukan 403**, dengan alasan yang sama.
- **Status path lama:** tidak ada — domain baru, tak menggantikan apa pun.
- **Tes:** 47 (parser nominal/kategori/perintah, gate kepemilikan termasuk penolakan `@lid` tak terdaftar + penolakan `ownerNumber`, repository CRUD/rekap/batas periode/tolak nominal ≤0).
- **PELAJARAN:** `routes/pages.js` punya handler generik `/:type` yang merender `views/sb-admin/<type>.php` **tanpa cek role sama sekali** — halaman baru yang sensitif WAJIB terdaftar eksplisit di atasnya DAN tak menyisipkan data dari server.

<a id="b176"></a>
### Feat 2026-07-23 (Keuangan pribadi: perintah tanpa prefix + login halaman TERPISAH dari akun admin)

- **Owner baru:** `lib/personal-finance-auth.js` (kredensial + rahasia sesi SENDIRI di `database/personal_finance_auth.json`, bcrypt 10, cookie `pf_session`) + `scripts/set-keuangan-pribadi-password.js` (bootstrap CLI) + `views/sb-admin/keuangan-pribadi-login.php`.
- **Status path lama:** gate allowlist `config.personalFinance.webUsers` **DIHAPUS** (ikut dibuang dari config.example) — dulu menumpang sesi admin, jadi browser admin yang tertinggal terbuka = dompet ikut terbuka. Token admin kini DITOLAK dompet dan sebaliknya (rahasia tanda tangan beda + klaim `scope`).
- **Perintah WA:** prefix `#U` **DIBUANG** (terlalu ribet untuk pemakaian harian). Kini tanpa prefix: `keluar 50rb bensin` / `masuk 2jt gaji`, payung `uang` (`uang`, `uang bulan [YYYY-MM]`, `uang hapus <id>`, `uang bantuan`). Aman telanjang karena gerbangnya cek IDENTITAS lebih dulu; `TRIGGER_WORDS` sengaja sempit — `bayar`/`beli`/`lapor`/`k`/`m` TIDAK memicu supaya percakapan bisnis pemilik tak tertelan.
- **PUBLIC_PATHS:** `/keuangan-pribadi` + `/api/keuangan-pribadi` ditambahkan ke `lib/http-auth-bootstrap.js` — "publik" HANYA terhadap middleware sesi admin, persis pola `/bayar`; otorisasi asli di handler. Penjaga dipasang di level prefix (`router.use`) supaya endpoint baru tak lolos gara-gara lupa gate.
- **Tutorial:** panel `<details>` terbuka-default di `/keuangan-pribadi` (daftar perintah WA + FAQ), pilihan buka/tutup diingat localStorage; di WA lewat `uang bantuan`.
- **Tes:** 68 total (+21) — termasuk token admin ditolak dompet, token dompet tak sah dgn rahasia admin, scope palsu ditolak, cookie `token` admin tak terbaca sebagai `pf_session`, dan `TRIGGER_WORDS` tak memuat kata bisnis ambigu.
- **PELAJARAN:** `PUBLIC_PATHS` cocok-prefix (`req.path.startsWith(path + "/")`) — menambah entri induk membuka SEMUA anaknya sekaligus, jadi hanya boleh untuk subtree yang punya penjaga sendiri di level prefix.

<a id="b177"></a>
### Feat 2026-07-23 (Kas usaha via WhatsApp — menumpang expense-manager, BUKAN pembukuan kedua)

- **Owner:** `lib/business-expense-service.js` (BARU — parser `kas`, pemetaan teks bebas ke 9 kategori TETAP, perakit laporan) + `message/handlers/business-expense-wa.js` (BARU — permukaan WA). Keduanya TANPA penyimpanan sendiri.
- **Penyimpanan tetap milik `lib/expense-manager.js`:** `createExpense`/`listExpenses`/`cancelExpense` → tabel `expense_entries` + buku besar keuangan. Jadi angka WhatsApp, `/pengeluaran`, dan `/rekap-keuangan` SATU SUMBER. Ini pilihan sadar: membangun ledger kedua untuk uang yang sama = angka yang tak pernah cocok.
- **`listExpenses` diperluas ADITIF** dengan `dateFrom`/`dateTo` (jalur month/year yang dipakai halaman web tak berubah) — laporan harian/mingguan tak bisa dinyatakan sebagai "bulan penuh".
- **Kata `kas` DIPINDAH** dari payung dompet pribadi (`lib/personal-finance-service`) ke kas usaha; dompet pribadi kini `uang`/`duit`/`dompet`. Tanpa itu pengeluaran usaha tercatat di dompet pribadi — pencampuran yang justru dihindari #b175.
- **Gate:** `config.businessExpense.enabled` default **OFF** + `groupId` + `ownerJids` TERPISAH. Per-instance penuh: Dander & Tanjungharjo punya grup + ledger sendiri, tak saling bergantung. Dipasang di 2 titik `raf.js` (blok grup + jalur `fromMe`), pola sama dengan dompet.
- **Tes:** 22 — termasuk jaminan tiap hasil `inferKategoriKas` ADA di `EXPENSE_CATEGORIES` (kategori di luar daftar ditolak `createExpense`, jadi tebakan meleset = pencatatan GAGAL), baris dibatalkan tak ikut dihitung, dan `kas` bukan lagi pemicu dompet pribadi. Plus uji end-to-end DB sungguhan.
- **PELAJARAN:** sebelum membangun "pencatatan X via WA", cari dulu siapa yang SUDAH memiliki angka X. Di repo ini `/pengeluaran` sudah ada — menumpanginya jauh lebih murah daripada merekonsiliasi dua ledger selamanya.

<a id="b178"></a>
### Fix 2026-07-23 (Backup lokal dompet pribadi — menutup risiko kehilangan permanen)

- **Owner:** `lib/personal-finance-backup.js` (BARU — salin/pangkas/pulih) + `lib/cron/jobs/personal-finance-backup.js` (BARU — jadwal, default tiap 6 jam, override `config.personalFinance.backupSchedule`), didaftarkan di `lib/cron.js`.
- **Kenapa ada:** `personal_finance.sqlite` SENGAJA di luar allowlist backup Telegram (#b175 — tujuan backup itu GRUP multi-anggota). Konsekuensi yang tak disadari: TAK ADA salinan sama sekali, jadi satu kesalahan = hilang permanen. Terbukti hari ini — catatan pemilik terhapus oleh skrip pembersih data uji dan tak ada apa pun untuk memulihkannya (dipulihkan manual dari jejak `strings` + nominal yang dikonfirmasi pemilik).
- **Salinan TIDAK PERNAH meninggalkan server** (`backups/keuangan-pribadi/`, mode 0600, simpan 30 terakhir) — itu syarat yang mempertahankan alasan pengecualian aslinya. Kredensial (`personal_finance_auth.json`) ikut disalin: memulihkan catatan tapi kehilangan sandi tetap mengunci pemiliknya di luar.
- **Memakai `hotBackupSqlite`**, bukan `fs.copyFile` — menyalin file mentah saat ada transaksi berjalan bisa menghasilkan berkas rusak.
- **Bug yang ditemukan saat menguji fungsi ini sendiri:** `pulihkanDari` mengambil salinan pengaman lebih dulu, tapi stempel hanya beresolusi DETIK — salinan pengaman (DB kosong) MENIMPA backup yang sedang dipulihkan, lalu yang dikembalikan justru DB kosong itu. Ditutup dengan nama unik ber-counter.
- **Tes:** 6, termasuk siklus penuh backup→hapus→pulih, tabrakan nama dalam detik yang sama, dan `basename()` yang menahan `../../config.json` jadi sumber pemulihan.
- **PELAJARAN:** mengeluarkan sesuatu dari jalur backup bersama adalah keputusan yang BENAR untuk privasi, tapi wajib diikuti jalur backup penggantinya. "Tidak ikut backup grup" tak boleh diam-diam berarti "tidak dibackup sama sekali".

<a id="b179"></a>
### Feat 2026-07-23 (Biaya rutin usaha: bot mengingatkan, manusia mengonfirmasi + halaman /kas-usaha)

- **Owner:** `lib/recurring-expense.js` (BARU — tabel `recurring_expenses` di DB utama: definisi + status per periode) + `lib/cron/jobs/recurring-expense-reminder.js` (BARU — pengingat harian ke grup kas) + `routes/admin-kas-usaha-routes.js` & halaman `/kas-usaha` (BARU — setelan grup + CRUD biaya rutin + ringkasan).
- **Bot TIDAK memposting sendiri.** Satu baris pengeluaran adalah pernyataan bahwa uang benar-benar keluar; nominal yang meleset (listrik Rp500rb vs Rp620rb nyatanya) menghasilkan pembukuan salah yang tampak MEYAKINKAN — lebih buruk daripada kolom kosong. Bot mengingatkan, pencatatan menunggu balasan `ok` / `ok 620rb` / `lewati` di grup.
- **Idempoten per PERIODE (`YYYY-MM`), bukan timestamp/boolean** — prod restart 7-13x/hari; hanya penanda per-periode yang membuat pengingat & posting tak menggandakan tanpa bergantung state di memori. Konfirmasi ganda ditolak dan diuji.
- **Pencatatan tetap lewat `expense-manager.createExpense`** (#b177) — tak ada pembukuan kedua. Urutannya createExpense DULU baru tandai tuntas: kalau gagal, item tetap tertunda dan bisa diulang.
- **Dua tagihan menunggu → bot MEMINTA nomor, tidak menebak.** Menebak salah berarti mencatat pengeluaran untuk pos yang keliru. Balasan `ok` juga di-abaikan (handled:false) bila tak ada yang menunggu — grup itu dipakai bicara biasa.
- **Gaji SENGAJA tidak di sini** — sudah dikelola `/gaji-teknisi`; memasukkannya akan terhitung dua kali. Tanggal 29-31 pada bulan pendek jatuh ke hari terakhir, kalau tidak tagihan tgl 31 tak pernah tertagih di Februari.
- **Tes:** 24 (12 store + 10 alur WA + gerbang) plus uji end-to-end cron→konfirmasi→`expense_entries`.
- **PELAJARAN:** `renderResponseTemplate` mengembalikan fallback TANPA substitusi bila key tak ada di `response_templates.json` — key baru yang lupa didaftarkan menampilkan `${nama}` mentah ke pemakai. Guard `response-template-key-integrity` menangkapnya; jangan lewati.

<a id="b180"></a>
### Fix 2026-07-26 (Ganti sandi/nama WiFi: payload kosong dilaporkan "Berhasil" — penjaga bukti-mendarat)

- **Owner penilaian sukses:** `lib/wifi-apply-guard.js` (BARU — `isWifiChangeApplied`/`assertWifiChangeApplied`, aturan `ok && accepted !== false && applied !== false`). Dipakai 14 titik apply di `wifi-password-state-handler`, `wifi-name-state-handler`, `wifi-power-handler`, `services/wifi-management.service`; cek longgar `if (!x.ok)` DIHAPUS dari jalur itu.
- **AKAR:** `getSelectedSsids()` mengunci cabang bulk ke NAMA STEP (`CONFIRM_GANTI_SANDI_BULK`), jadi step `ASK_NEW_PASSWORD_BULK_AUTO` (dipasang `handleBulkAutoPasswordChange`) jatuh ke `[]` walau `bulk_ssids` terisi → `bulkPayload = {}`. `bulk_ssids` kini fallback TERAKHIR, lepas dari nama step.
- **GOTCHA yang membuatnya senyap:** `lib/genieacs.updateWifiSettings` balik **`ok: true`** untuk payload kosong — satu-satunya outlier; `setParameterValuesInternal`/`setWifiCredentials`/`applyBulkWifiUpdates` sudah `ok:false`+`PARSE_ERROR`. Kini diselaraskan, jadi nol-task tak bisa lagi menyamar jadi sukses di jalur mana pun.
- **Korban prod (Tanjung):** 3 pelanggan dijanjikan sandi yang tak pernah aktif — id 74 (09-07), id 58 & 89 (24-07); dibuktikan `0 task` di ACS + `PreSharedKey` masih nilai lama. Ditandai di log oleh `reason: "...(0 SSIDs)"` + `changes.ssidIds: ""`. Dander memakai kode sama (rentan, belum ada korban).
- **Jalur inline (`gantisandi <sandi>`) TIDAK terpengaruh** — memakai `user.bulk` langsung; itu sebabnya mayoritas pelanggan normal dan bug bertahan ~2 minggu.
- **Template:** key baru `wifi_password_bulk_target_missing` (kembaran `wifi_name_bulk_target_missing`) ditambahkan di commit yang sama.
- **Tes:** 4 regresi alur bulk-auto (`message/__tests__/wifi-bulk-auto-apply.test.js`, terbukti MERAH di kode lama) + 13 kontrak penjaga (`lib/__tests__/wifi-apply-guard.test.js`) + 9 guard statis pemindai repo (`message/__tests__/wifi-apply-success-judgment.test.js`).
- **Tanpa gate config** — ini perbaikan korektif, bukan fitur baru.

<a id="b181"></a>
### Beli Voucher dari Panel Pelanggan (terautentikasi, tag `buynowpanel`)

- **Owner:** `services/customer-voucher.service.js` (BARU) + endpoint `/api/customer/vouchers/{status,packages,purchase,purchase/:reff,history}` di `customerApiRouter` (`routes/public.js`). Surface anonim `/app/*` (`routes/public-anonymous.js`, [b03](#b03)) TETAP hidup dan tidak diubah — ini jalur kedua yang terautentikasi, bukan pengganti.
- **Beda dari jalur web anonim:** nomor HP diambil dari `req.customer.phone_number` (tidak pernah dari body), dan pembacaan status/riwayat di-scope ke `customerId` fail-closed di kedua sisi — pelanggan tidak bisa membaca kode voucher orang lain dengan menebak `reff`.
- **Fulfillment:** cabang `buynowpanel` di `POST /callback/payment` (`routes/public.js`), meniru pengamanan `buynowweb` (lock per-reff, verify S2S iPaymu, cross-check referenceId+amount, `recordVoucherOrphan` + `alertAdmins` saat gagal terbit, tetap mark paid karena `getvoucher` non-idempoten). Profil diambil dari `pay.prof` yang disimpan saat charge, BUKAN `checkprofvc(harga)` yang tertukar bila dua paket berharga sama.
- **Config gate:** `customerVoucher.enabled`, default **false** di `config.example.json` (deploy gelap). Rate limit 10 transaksi/15 menit per pelanggan; baca status tidak dibatasi karena panel mem-polling.
- **Template:** key baru `voucher_beli_panel` di `database/message_templates.json`, ditambahkan di commit yang sama.
- **Tes:** `services/__tests__/customer-voucher.service.test.js` (20 unit, termasuk scoping lintas-pelanggan) + `routes/__tests__/payment-callback-voucher.test.js` diperluas (12 guard statis; slice blok diperbaiki agar `buynowweb` tidak menelan cabang baru).