# Header Doc
- Purpose: Peta modul lokal untuk direktori `message` berisi file, simbol publik, dan tanggung jawab ringkas.
- Caller: Pengembang/agent saat tracing flow lintas modul di `MESSAGE MODULE MAP`.
- Deps: Analisis statis file dalam `message`, nama file, dan blok `module.exports`.
- MainFuncs: Inventaris file, simbol publik, dan peran tiap modul.
- SideEffects: Tidak ada; dokumentasi statis.

# MESSAGE MODULE MAP
- FlowDir: message
- CallerUtama: `index.js` -> `message/raf.js`
- DependensiKunci: `message/handlers/*`, `lib/wifi_template_handler.js`, template pesan, `database/response_templates.json`
- SideEffects: Tidak ada; dokumentasi statis.

| File | Ekspor/Publik | Tanggung Jawab |
| --- | --- | --- |
| `message/pay.js` | `guideQRIS` | Template panduan QRIS untuk alur pembayaran bot. |
| `message/raf.js` | `Not found` | Router pesan WhatsApp utama; intersepsi global, menerima normalized inbound context dari adapter WA, mendelegasikan state aktif lewat `conversation-state-router`, meneruskan payload lokasi tiket aktif ke owner helper lokasi, dan tidak lagi membuat proxy mirror state legacy maupun meneruskan chain `temp` melalui router sebelum dispatch intent via `intent-owner-map`, `domain-handlers`, dan `domain-services`, termasuk intent WiFi serta payment/topup yang kini turun ke service+repository owner lewat handler tipis. |
| `message/wifi.js` | `wifimenu, technicianmenu, menubelivoucher, menuvoucher, menupasang, menuowner, menupaket` | Template menu dan respon teks domain WiFi untuk bot. |

- Catatan: `router` berarti ekspor instance `express.Router()`, sedangkan `Not found` berarti simbol publik tidak terdeteksi statis atau file berisi data murni.
